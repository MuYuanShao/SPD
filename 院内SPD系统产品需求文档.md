# 院内SPD系统产品需求文档（PRD）

---

## 文档信息

| 项目 | 内容 |
|------|------|
| 产品名称 | 院内SPD系统 |
| 英文名称 | SPD Hospital Supply Chain Management Platform |
| 版本号 | V1.2.3 |
| 编写日期 | 2026-04-27 |
| 技术架构 | Spring Boot + Vue3 |
| 数据库 | MySQL 8.0 |
| 适用范围 | 医院院内非药品 SPD 物资供应链管理，覆盖医疗耗材、体外诊断试剂、设备与相关合规扩展；不包含药品采购、药房库存、处方流转与药品追溯码体系 |

---

## 目录

1. [系统概述](#1-系统概述)
2. [系统架构](#2-系统架构)
3. [基础管理模块](#3-基础管理模块)
   - 3.1 登录认证
   - 3.2 用户管理
   - 3.3 角色权限管理
   - 3.4 科室管理
4. [主数据管理模块](#4-主数据管理模块)
   - 4.1 商品目录管理
   - 4.2 待审批商品目录管理
   - 4.3 供应商管理
   - 4.4 厂家管理
5. [供应链业务模块](#5-供应链业务模块)
   - 5.1 采购管理
   - 5.2 库存管理
   - 5.3 科室申领
   - 5.4 结算对账
6. [专项管理模块](#6-专项管理模块)
   - 6.1 UDI追溯
   - 6.2 高值耗材管理
   - 6.3 冷链监控
7. [运营支撑模块](#7-运营支撑模块)
   - 7.1 报表中心
   - 7.2 系统配置
   - 7.3 SaaS化与配置中心
8. [院内SPD业务扩展模块](#8-院内spd业务扩展模块)
   - 8.1 基础信息扩展
   - 8.2 请补货与采购扩展
   - 8.3 收货与上架管理
   - 8.4 拣配出库与配送
   - 8.5 库内作业管理
   - 8.6 科室业务管理
   - 8.7 退回业务管理
   - 8.8 手术临床管理
   - 8.9 结算与发票扩展
9. [接口规范](#9-接口规范)
   - 9.1 登录API接口
   - 9.2 附件管理API接口
   - 9.3 外网平台对接API
   - 9.4 核心业务API接口规范
   - 9.5 前端接口对接规范
   - 9.6 AI服务API接口规范（预留）
10. [数据库设计](#10-数据库设计)
11. [通用规范](#11-通用规范)
12. [合规与扩展业务](#12-合规与扩展业务)
    - 12.1 多院区与组织扩展
    - 12.2 合同与招采管理
    - 12.3 供应商扩展管理
    - 12.4 库存扩展业务
    - 12.5 召回与质量闭环
    - 12.6 临床扩展业务
    - 12.7 财务与医保扩展
    - 12.8 决策支持扩展
    - 12.9 系统集成扩展

---

## 1. 系统概述

### 1.1 系统定位

院内SPD系统（Supply, Processing, Distribution）是医院非药品物资供应链管理平台，覆盖医疗耗材、体外诊断试剂、设备等物资的全生命周期管理。系统以"临床需求为导向、精细化管理为核心、全程追溯为保障"，实现医院物资供应的规范化、智能化和高效化。药品采购、药房库存、处方流转与药品追溯码体系不纳入本系统范围。

### 1.2 系统目标

- **供应保障**：确保临床物资及时、准确供应
- **成本控制**：降低库存成本，减少浪费
- **质量追溯**：实现物资全程可追溯
- **合规管理**：满足国家监管要求（UDI、冷链、高值耗材等）
- **数据决策**：提供数据分析和决策支持

### 1.3 用户角色体系

| 角色 | 职责范围 | 主要模块 |
|------|----------|----------|
| 系统管理员 | 系统配置、用户管理、权限分配 | 系统配置、用户管理、角色管理 |
| 运营管理员 | 日常运营管理、规则配置 | 商品目录、供应商、库存、采购 |
| 采购人员 | 采购计划、订单管理、供应商沟通 | 采购管理、供应商管理 |
| 库管人员 | 入库、出库、盘点、库存管理 | 库存管理、UDI追溯、冷链监控 |
| 科室人员 | 物资申领、使用登记、收货确认 | 科室申领、高值耗材管理 |
| 财务人员 | 结算对账、发票管理、付款审批 | 结算对账、报表中心 |
| 质控人员 | 质量监控、追溯审计、合规检查 | UDI追溯、冷链监控、报表中心 |
| 医生/护士 | 高值耗材使用、患者关联 | 高值耗材管理 |
| 院领导 | 运营总览、决策分析 | 报表中心 |

### 1.4 核心业务流程

```
[商品准入] → [采购计划] → [采购订单] → [到货入库] → [库存管理]
                                              ↓
[结算对账] ← [发票付款] ← [科室消耗] ← [科室申领] ← [配送出库]
```

### 1.5 版本范围与上线优先级

系统按"先闭环、再扩展、后智能"的节奏交付。若客户要求一期同时覆盖多院区、合同招采、AI、冷链 IoT、定数管理等能力，仍需在验收口径上分层，避免把所有扩展能力都定义为同等上线阻断项。

| 阶段 | 必须闭环 | 验收重点 | 可后置能力 |
|------|----------|----------|------------|
| V1.0 基础闭环 | 商品目录、供应商、采购、收货、库存、科室申领、科室消耗、结算对账、基础报表 | 单院区或单法人主体内形成采购到结算闭环，库存账和结算账一致 | AI、复杂大屏、跨院区调拨、深度医保 |
| V1.1 院内精细化 | 定数包模板、安全量、打包标签、PDA 作业、科室二级库、退回、反消耗、红冲 | 定数包事件流水、PDA 离线、科室日清、次月红冲可追溯 | 高级评分模型、跨系统自动冲销 |
| V1.2 合规与平台化 | 多院区组织、合同招采、集采执行、冷链 IoT、高值/冷链唯一码、召回质量闭环 | 多院区数据隔离、合同价/集采价执行、冷链异常处置、UDI/唯一码追溯 | 复杂医保结算、供应商门户深度协同 |
| V2.0 智能运营 | AI 问答、审批建议、智能预警、智能分析、决策大屏高级分析 | AI 只做建议和解释，不自动改变业务状态；所有结论可追溯来源 | 自动策略优化、模型训练闭环 |

### 1.6 前端菜单信息架构

前端菜单以业务域组织，不完全等同于章节编号。PRD 中相同业务域的规则以更细章节为准，原型与实施菜单应遵循下表：

| 一级菜单 | 子菜单 | 说明 |
|----------|--------|------|
| 运营与决策 | 运营驾驶舱、决策大屏、AI智能服务 | 决策大屏下含院内运营数据分析、集采数据完成情况、合同管理、手术单病种数据分析 |
| 主数据与定数 | 商品目录、定数包模板、定数安全量、多院区组织 | 商品目录下含待审批目录、医院目录 |
| 供应链业务 | 请补货任务、收货上架、拣配配送、库内作业 | 承接采购、库存、配送和 PDA 作业 |
| 专项与合规 | UDI/唯一码、冷链监控、结算红冲、PDA离线 | 承接高值、冷链、定数包红冲和离线风险 |

### 1.7 统一组织与数据隔离模型

系统统一采用 `集团/租户 → 医院 → 院区 → 科室 → 库房/货位` 的组织层级。`tenant_id` 表示系统租户或集团级隔离边界，不能直接等同于院区；院区、科室、库房必须使用独立字段表达。

| 层级 | 业务含义 | 典型数据 |
|------|----------|----------|
| 租户/集团 | SaaS 隔离和集团主数据边界 | 模块订阅、集团商品、集团供应商、集团包模板 |
| 医院 | 法人或管理主体 | 合同主体、财务核算主体、票据规则 |
| 院区 | 物理运营场所 | 院区准入商品、合同价适用范围、中心库 |
| 科室 | 临床或职能部门 | 科室二级库、申领、定数安全量、日清 |
| 库房/货位 | 库存账务最小空间 | 中心库、二级库、冷库、隔离库、虚拟库 |

配置优先级沿用 7.3.3：用户级 > 模块级 > 仓库级 > 科室级 > 院区级 > 租户级 > 系统级 > 默认值。跨院区业务不得只依赖租户字段判断，必须显式记录调出/调入院区、法人主体和审批链。

### 1.8 业务问题与产品目标

本系统优先解决医院院内非药品物资供应链在现场运行中的账实一致、临床供应、追溯合规、结算准确和配置可解释问题。所有功能设计应服务于“采购到结算闭环、库存不负数、追溯不断链、配置可落地”的产品目标。

| 现场问题 | 当前痛点 | 系统应对能力 | 衡量指标 |
|----------|----------|--------------|----------|
| 科室缺货 | 安全量不准、补货慢、来货后不能优先处理缺口 | 缺货登记、补货定时任务、来货匹配、紧急补货 | 缺货满足时长、缺货关闭率、紧急补货满足率 |
| 库存账实不一致 | PDA 离线、盘亏、错签收、预占未释放导致库存不准 | 事件流水、状态快照、禁止负库存、盘点复核、PDA 补传 | 库存一致率、负库存发生次数、PDA 补传成功率 |
| 定数包现场规则复杂 | 打包、反消耗、标签补打、解包重组、召回隔离容易混淆 | 定数包状态机、成包条件、标签留痕、解包回散货 | 定数包满足率、反消耗率、标签异常关闭率 |
| 价格与结算争议 | 订单价、目录价、批次价、调价、红冲口径不统一 | 验收批次单价、批次调价、原事实单价、红冲矩阵 | 结算争议率、调价影响清单完整率 |
| 高值/冷链追溯风险 | UID、温控、患者计费、召回闭环割裂 | 唯一码生命周期、冷链异常处置、质控闭环 | 追溯完整率、冷链异常关闭率、召回命中处理率 |
| 多院区差异化落地 | 配置太多但来源不清，实施难以解释 | 配置优先级、灰度生效、命中说明、配置审计 | 配置冲突数、配置命中可解释率 |

### 1.9 用户画像与 Jobs-to-be-done

| 用户画像 | 核心目标 | 高频任务 | 最怕的问题 | Jobs-to-be-done |
|----------|----------|----------|------------|-----------------|
| SPD 库管 | 快速收发、库存准确 | 验收、上架、打包、拣配、配送、盘点 | 库存不准、标签扫不出、任务重复 | 当临床需要物资时，能快速找到可用库存并准确配送 |
| 科室护士/科室库管 | 物资够用、操作少、责任清晰 | 签收、消耗、缺货反馈、反消耗申请 | 缺货、误消耗、反消耗流程慢 | 当科室使用耗材时，能快速消耗并保证缺货能被及时处理 |
| 财务人员 | 结算准确、红冲可追溯 | 对账、红冲、发票、付款 | 历史单价被改、供应商争议 | 当生成结算时，能按真实消耗和原事实单价完成对账 |
| 质控人员 | 风险可控、召回完整 | 召回、冷链异常、批号效期调整 | 召回漏命中、异常未关闭 | 当出现质量风险时，能定位所有受影响库存和已消耗记录 |
| 运营管理员 | 规则可配置、运营可视 | 配置、审批、驾驶舱、异常处理 | 配置冲突、任务堆积、来源不清 | 当医院规则变化时，能通过配置生效并解释命中来源 |
| 采购人员 | 采购计划可执行、价差可解释 | 采购订单、验收入库协同、价差复核 | 订单价与验收价争议 | 当目录价格变化时，能保留订单分析并以验收批次价入账 |
| 供应商 | 对账清楚、回款及时 | 配送、对账确认、发票 | 明细不一致、红冲原因不清 | 当接收结算数据时，只按明细数量和金额确认 |
| 实施/运维 | 系统稳定、配置可控 | 初始化、权限、接口、定时任务 | 配置不可解释、任务失败无日志 | 当上线和运维时，能追溯配置、任务和接口处理结果 |

### 1.10 成功指标与验收指标

| 指标类型 | 指标 | 验收口径 |
|----------|------|----------|
| 主指标 | 采购到结算闭环完成率 | P0 主流程可从商品准入、采购、验收、库存、配送、消耗到结算闭环 |
| 主指标 | 库存账实一致率 | 盘点复核后库存差异可解释，所有差异有事件流水 |
| 护栏指标 | 负库存发生次数 | 必须为 0 |
| 护栏指标 | 关键变更未留痕次数 | 必须为 0 |
| 运营指标 | 缺货登记平均满足时长 | 按科室、紧急等级、商品分类统计 |
| 运营指标 | 定数包满足率 | 按院区、科室、包族统计应补包数与实际满足包数 |
| 运营指标 | PDA 离线补传成功率 | 按设备、岗位、科室统计上传成功、失败、冲突 |
| 财务指标 | 结算争议率 | 按供应商、批次、商品统计对账差异和红冲原因 |
| 质量指标 | 召回/隔离处理关闭率 | 所有命中库存或消耗记录必须有处理结果 |
| 配置指标 | 配置命中可解释率 | 关键业务页面可查看最终命中配置及来源 |

### 1.11 本期范围与不做范围

**一期上线阻断项：**

- 商品目录、供应商、采购、验收、库存、科室申领、科室消耗、结算对账闭环。
- 系统批次、批次单价、所有权/结算模式快照、库存流水和禁止负库存。
- 定数包模板、系数、成包、标签、配送、签收、消耗、反消耗、解包回散货。
- PDA 基础作业、离线提醒、补传入账和强一致动作限制。
- 缺货登记、补货定时任务、来货匹配、部分满足和关闭规则。
- 结算取价、红冲矩阵、供应商明细结算口径。
- 基础权限、配置优先级、配置命中说明、配置审计。
- 数据初始化/迁移验收完成后才允许正式上线；商品、供应商、科室、库房、货位、期初库存、定数配置、UID/唯一码和系统策略默认值必须完成核对、签字和留痕。

**本期不做或仅试点：**

- 药品采购、药房库存、处方流转、药品追溯码体系。
- AI 自动审批、自动调价、自动结算、自动解除隔离；AI 仅做建议、解释和报告草稿。
- 复杂医保结算、DRG/DIP 自动控费闭环。
- 供应商门户深度协同和供应商自主调价闭环。
- 跨系统自动冲销 HIS/医保/财务外部账务。
- 高级模型训练、自动策略优化。
- 冷链 IoT 全院铺开可作为试点，不能阻断 V1.0/V1.1 基础闭环上线。

### 1.12 业务场景总览矩阵

| 场景 | 触发角色/系统 | 关键单据/对象 | 库存影响 | 结算影响 | 重点规则 |
|------|---------------|---------------|----------|----------|----------|
| 商品准入/变更 | 运营、供应商、质控 | 待审批目录、医院目录 | 无直接库存影响 | 影响后续采购与验收批次 | 是否定数管理为硬准入；采购单价变更影响后续验收 |
| 采购下单 | 采购、运营 | 采购订单 | 不占正式库存 | 订单价只做分析 | 订单关闭按数量履约，价差不阻断 |
| 验收入库 | 库管 | 验收单、系统批次 | 生成批次库存 | 生成批次单价，作为后续结算依据 | 审核通过日期生成批次；取最新商品目录采购单价 |
| 自动成包/待确认打包 | 库管、系统任务 | 定数包标签、打包任务 | 散货转定数包或预占散货 | 无结算影响 | 完整成包条件、余数留散货、待确认任务预占 |
| 配送签收 | 库管、科室 | 补货单、配送单、包标签 | 中心库可用转在途，签收后转科室可用 | 无结算影响 | 签收后才计入科室库存和安全量 |
| 科室消耗 | 科室 | 消耗记录、定数包事件 | 扣减科室库存 | 固化原消耗批次单价，形成应结算事实 | 定数包拆包即整包消耗 |
| 反消耗 | 科室、库管、财务 | 反消耗事件、红冲明细 | 复活原标签或生成负向库存事件 | 按原消耗单价冲回 | 日清/月结/已结算分流 |
| 解包回散货 | 授权库管/角色 | 解包事件、原标签、新散货 | 定数包 -1，散货 +N | 无结算影响 | 原标签作废，重新组包生成新标签 |
| 缺货补货 | 系统任务、库管 | 缺货登记、补货任务 | 按可用库存部分满足 | 无直接结算影响 | 缺口满足以科室签收为准 |
| 盘点盘亏 | 库管、科室、财务 | 盘点单、盘亏事件 | 扣减已有库存，不得负库存 | 按所有权/结算模式决定成本或赔付 | 盘亏计入科室成本/绩效 |
| 批次调价 | 采购、财务 | 批次调价单、价格版本 | 未结算事实库存价值变化 | 已消耗/已结算/已计费不改原价 | 生成影响清单，不覆盖历史事实 |
| 冷链异常/召回 | 质控、系统 | 异常单、召回单 | 命中库存隔离/停用 | 不直接改金额 | 命中定数包整包停用，触发补货 |
| 高值使用计费 | 医生/护士、HIS | UID、患者、计费流水 | UID 出库/使用 | 绑定患者计费后固化单价 | 撤销必须先走计费冲销 |

### 1.13 核心状态机索引

| 对象 | 主要状态 | 允许动作 | 禁止动作 | 权威章节 |
|------|----------|----------|----------|----------|
| 系统批次 | 已生成、可用、锁定、在途、隔离、已消耗、已退货、已报损 | 调价、批号效期调整、出入库、盘亏报损 | 修改系统批次号、负库存扣减 | 5.2.8 |
| 定数包标签 | 待打印/待贴标、可用、锁定、在途、科室可用、已消耗、召回、隔离、待复核、作废、报损 | 补打、配送、签收、消耗、反消耗、解包、召回 | 已消耗/作废/报损标签补打；召回/隔离状态消耗 | 8.5.7 |
| 未确认打包任务 | 待确认、预占中、需重算、已确认、已取消、已过期 | 预占、确认、取消、重算、超时释放 | 产生消耗、产生结算、形成负库存 | 8.5.7 |
| 缺货登记 | 待满足、部分满足、已满足、人工关闭 | 来货匹配、部分满足、关闭 | 出库在途即关闭、负库存满足 | 8.2.13 |
| 反消耗事件 | 申请中、审批中、已恢复、已红冲、已关闭、已驳回 | 审批、复活原标签、生成红冲 | 删除原消耗记录、按当前单价冲回 | 8.5.7 / 8.9.6 |
| PDA 离线记录 | 本机待上传、上传中、已入账、失败、冲突待复核 | 补传、重试、复核 | 上传前改正式库存、离线执行强一致动作 | 8.5.7 / 9.4.14 |
| 配置项 | 草稿、待审批、已生效、已驳回、已失效、已作废 | 新增、修改、审批、定时生效、灰度 | 同级同对象同时间段重复生效 | 7.3.3 |

### 1.14 P0 验收用例基线

| 编号 | 场景 | Given | When | Then |
|------|------|-------|------|------|
| AC-001 | 验收入库生成系统批次 | 验收单已审核通过 | 系统处理同验收单同商品同生产批号同效期明细 | 归入同一系统批次，批次号按审核日期 + 后五位序列号生成 |
| AC-002 | 验收入库取最新采购单价 | 商品目录采购单价已变化 | 验收审核通过 | 弹窗提醒价差，按最新商品目录采购单价生成批次单价，不阻断验收 |
| AC-003 | 商品目录采购单价变更 | 历史批次已生成 | 修改商品目录采购单价 | 只影响后续新验收入库批次，不改变历史批次 |
| AC-004 | 批次调价 | 存在未消耗库存和已消耗记录 | 批次调价审批通过 | 未产生结算事实库存按新价，已消耗/已结算/已计费原价不变 |
| AC-005 | 定数包自动成包 | 商品启用定数管理且验收数量满足系数 | 验收审核通过 | 按完整成包条件生成整包标签，余数保留散货 |
| AC-006 | 待确认打包任务 | 配置为待确认模式 | 系统生成打包任务 | 默认预占来源散货，确认前不计入定数包安全量 |
| AC-007 | 解包回散货 | 未消耗定数包需作废重组 | 授权用户执行解包 | 原标签作废，散货回当前实物所在库房/货位，不产生消耗和结算 |
| AC-008 | 定数包消耗 | 科室可用定数包标签存在 | 科室扫码消耗 | 整包消耗，生成包事件和明细结算事实 |
| AC-009 | 反消耗 | 已消耗定数包误操作且实物完整 | 审批通过 | 复活原标签，按原消耗单价生成反向记录 |
| AC-010 | 召回命中定数包 | 召回条件命中包内生产批号 | 系统执行召回匹配 | 整包停用，不计入安全量，后续定时任务触发补货 |
| AC-011 | 缺货登记部分满足 | 中心库库存不足 | 补货任务执行 | 可部分满足，剩余缺口保留缺货登记，满足后自动关闭 |
| AC-012 | PDA 离线补传 | PDA 离线采集扫码记录 | 网络恢复上传 | 上传成功前不入正式账，强一致动作离线期间禁止 |
| AC-013 | 盘亏 | 系统有库存但现场实物缺失 | 盘亏复核通过 | 扣减已有库存，不形成负库存，计入科室成本/绩效 |
| AC-014 | 批号效期调整 | 批号效期录错且需全量更正 | 审批通过 | 全量更新展示/追溯属性，不改变金额和结算状态 |
| AC-015 | 配置命中说明 | 业务页面存在策略判断 | 用户点击查看配置来源 | 展示最终值、来源层级、配置编号和生效时间，不记录查看日志 |

---

## 2. 系统架构

### 2.1 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (Vue3)                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 登录模块  │ │ 业务模块  │ │ 报表模块  │ │ 系统配置  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│  Element Plus + Pinia + Axios + Vue Router                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼ RESTful API
┌─────────────────────────────────────────────────────────────┐
│                       网关层 (Nginx)                         │
│  SSL终止 / 负载均衡 / 限流 / IP白名单 / 静态资源缓存          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      服务层 (Spring Boot)                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 认证服务  │ │ 业务服务  │ │ 文件服务  │ │ 报表服务  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│  Spring Security + JWT + MyBatis Plus + Validation          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      AI服务层 (预留扩展)                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 智能问答  │ │ 智能审批  │ │ 智能预警  │ │ 智能分析  │       │
│  │ 服务      │ │ 服务      │ │ 服务      │ │ 服务      │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│  Python/FastAPI + LangChain + 本地大模型(LLaMA/ChatGLM)      │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              本地大模型推理引擎 (GPU服务器)            │    │
│  │  [Ollama/vLLM] [向量数据库(Milvus)] [知识库(RAG)]    │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据层                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │  MySQL   │ │  Redis   │ │  MinIO   │ │  RabbitMQ│       │
│  │  主数据库 │ │  缓存    │ │  文件存储 │ │  消息队列 │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 网络架构

```
┌─────────────────────────────────────────────────────────────┐
│                        外网用户                               │
│              (供应商平台 / UDI平台 / 医保平台)                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      DMZ隔离区                                │
│         [API网关] [WAF] [负载均衡] [OSS外网文件服务]           │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ 内网防火墙
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      医院内网                                 │
│  [SPD后端服务] [AI服务集群] [MinIO文件存储] [MySQL] [Redis]   │
│  [消息队列] [向量数据库Milvus] [GPU服务器(Ollama/vLLM)]        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      医院内网用户                             │
│         [审批人员] [运营人员] [科室人员] [库管人员]            │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 模块依赖关系

```
基础模块
├── 登录认证（用户管理、角色权限、科室管理依赖）
├── 用户管理（依赖角色权限、科室管理）
├── 角色权限管理
└── 科室管理

主数据模块
├── 商品目录管理（依赖供应商、厂家）
├── 待审批商品目录（依赖商品目录、供应商）
├── 供应商管理
└── 厂家管理

业务模块
├── 采购管理（依赖商品目录、供应商、库存）
├── 库存管理（依赖商品目录、采购、科室申领）
├── 科室申领（依赖库存、商品目录）
└── 结算对账（依赖采购、供应商、科室申领）

专项模块
├── UDI追溯（依赖商品目录、库存、科室申领）
├── 高值耗材管理（依赖商品目录、UDI、库存、科室申领）
└── 冷链监控（依赖库存、商品目录）

支撑模块
├── 报表中心（依赖所有业务模块）
└── 系统配置（支撑所有模块）

AI服务模块（预留扩展）
├── 智能问答服务（依赖商品目录、库存、采购知识库）
├── 智能审批服务（依赖审批流程、业务规则）
├── 智能预警服务（依赖库存、效期、温度数据）
└── 智能分析服务（依赖报表中心、历史数据）
```

### 2.4 模块间数据流

```
基础管理数据流
├── 登录认证
│   └── ──→ 用户管理 [用户信息、角色、权限] (登录时同步)
├── 角色权限管理
│   └── ──→ 用户管理 [角色-权限映射] (配置变更推送)
└── 科室管理
    └── ──→ 用户管理 [科室树结构] (变更时推送)

主数据管理数据流
├── 待审批商品目录
│   └── ──→ 商品目录管理 [商品基础信息、价格、资质] (审批通过后写入)
└── 供应商管理
    └── ──→ 商品目录管理 [供应商信息、资质] (审批通过后关联)

供应链业务数据流
├── 采购管理
│   ├── ──→ 库存管理 [采购订单、到货通知] (订单确认后推送)
│   ├── ←── 库存管理 [入库单、批次信息] (入库完成后回调)
│   └── ──→ 结算对账 [订单金额、入库数量] (月度结算汇总)
├── 库存管理
│   ├── ──→ 科室申领 [实时库存量] (申领时实时查询)
│   ├── ←── 科室申领 [消耗数量、患者信息] (消耗确认后扣减)
│   └── ──→ 科室申领 [出库单、配送信息] (出库完成后推送)
├── 科室申领
│   └── ──→ 结算对账 [科室消耗金额] (月度结算汇总)
└── 结算对账
    └── ←── 采购管理 + 科室申领 [结算数据汇总]

专项管理数据流
├── UDI追溯 ←→ 高值耗材管理
│   ├── UDI追溯 ──→ 高值耗材管理 [UDI码、批次信息] (入库时绑定)
│   └── 高值耗材管理 ──→ UDI追溯 [患者关联信息] (使用时记录)
└── 冷链监控
    └── ──→ 库存管理 [温度异常告警] (实时推送)

运营支撑数据流
├── 所有业务模块
│   └── ──→ 报表中心 [汇总统计数据] (定时任务/实时查询)
└── 系统配置
    └── ──→ 所有模块 [参数、规则、流程] (配置变更时推送)

AI服务数据流（预留扩展）
├── 业务模块
│   ├── ──→ AI服务 [商品/库存/采购知识] (定时同步至向量库)
│   ├── ──→ 数据统计分析 [原始业务数据] (定时ETL抽取)
│   └── ──→ 数据统计展示 [可视化图表数据] (实时查询)
├── AI服务
│   ├── ──→ 前端 [问答结果、推荐] (用户提问时调用)
│   ├── ──→ 审批流程 [审批建议、风险评分] (审批时实时调用)
│   ├── ──→ 消息通知 [预警消息、处置建议] (定时任务触发)
│   └── ──→ 报表中心 [分析结论、趋势预测] (报表生成时调用)
├── 数据统计分析
│   ├── ──→ 数据仓库 [清洗/转换/汇总数据] (定时ETL处理)
│   ├── ──→ 数据集市 [主题域数据模型] (按业务主题聚合)
│   └── ──→ 数据统计展示 [分析结果数据] (API接口推送)
├── 数据统计展示
│   ├── ──→ 前端 [图表/仪表盘/大屏] (用户查看时加载)
│   ├── ──→ 报表中心 [固定报表数据] (定时生成)
│   └── ──→ AI服务 [训练样本数据] (模型训练时调用)
└── AI服务 ←→ 向量数据库 [知识检索] (实时查询)
```

---

## 3. 基础管理模块

### 3.1 登录认证

#### 3.1.1 页面设计

登录页面采用左右分栏卡片式布局：
- **左侧**：系统品牌展示区，包含SPD Logo、系统名称、业务插画
- **右侧**：登录表单区，包含用户名输入、密码输入、业务类型选择、登录按钮
- **背景**：深色渐变背景搭配青绿色几何装饰图形

#### 3.1.2 登录流程

```
[用户访问登录页] → [输入用户名和密码] → [选择业务类型（可选）]
    → [前端表单校验] → [调用登录API] → [后端身份认证]
    → [生成JWT Token] → [返回用户信息及权限] → [跳转首页]
```

#### 3.1.3 表单校验规则

| 字段 | 校验规则 | 错误提示 |
|------|----------|----------|
| 用户名 | 必填，长度2-20字符 | "请输入用户名" / "用户名长度2-20字符" |
| 密码 | 必填，长度6-20字符 | "请输入密码" / "密码长度6-20字符" |
| 业务类型 | 非必填，默认耗材 | "请选择业务类型" |

#### 3.1.4 业务类型选项

| 选项值 | 显示文字 | 说明 |
|--------|----------|------|
| consumable | 耗材 | 耗材供应链管理 |
| device | 设备 | 医疗设备管理 |
| reagent | 试剂 | 试剂管理 |

#### 3.1.5 安全需求

| 需求项 | 说明 |
|--------|------|
| 密码传输加密 | 前端使用RSA公钥加密密码后传输 |
| 登录次数限制 | 同一IP/账号5分钟内连续失败5次，锁定15分钟 |
| Token机制 | 使用JWT Token，有效期2小时，支持刷新 |
| 验证码 | 连续失败3次后显示图形验证码 |
| 记住密码 | 支持记住用户名（本地存储），禁止明文存储密码 |

### 3.2 用户管理

#### 3.2.1 功能概述

用户管理是系统基础权限模块的核心，用于管理系统内所有操作用户的生命周期，包括创建、查询、编辑、禁用/启用、删除、重置密码、分配角色等。

#### 3.2.2 页面布局

```
+----------------------------------------------------------+
|  用户管理                                                  |
+----------------------------------------------------------+
|  [搜索区域]                                                |
|  用户名: [________]  状态: [全部▼]  科室: [全部▼]  [查询] [重置] |
+----------------------------------------------------------+
|  [新增用户] [批量删除] [导出Excel]                          |
+----------------------------------------------------------+
|  [数据表格]                                                |
|  □ | 用户名 | 真实姓名 | 科室   | 角色     | 状态 | 操作   |
+----------------------------------------------------------+
|  共 50 条  [1][2][3][4][5]  每页: [10▼]                   |
+----------------------------------------------------------+
```

#### 3.2.3 核心功能

| 功能 | 说明 |
|------|------|
| 用户列表查询 | 支持按用户名、真实姓名、状态、科室筛选 |
| 新增用户 | 填写用户名、密码、真实姓名、手机号、所属科室、角色等 |
| 编辑用户 | 修改用户信息，用户名不可修改 |
| 删除用户 | 逻辑删除，不能删除当前登录用户和系统预置管理员 |
| 重置密码 | 将密码重置为默认密码，首次登录强制修改 |
| 分配角色 | 为用户分配一个或多个角色 |

#### 3.2.4 用户表单字段

| 字段 | 类型 | 必填 | 校验规则 |
|------|------|------|----------|
| 用户名 | 输入框 | 是 | 2-20字符，字母数字下划线，唯一 |
| 密码 | 密码框 | 是 | 6-20字符 |
| 确认密码 | 密码框 | 是 | 与密码一致 |
| 真实姓名 | 输入框 | 是 | 2-20字符 |
| 手机号 | 输入框 | 否 | 11位手机号格式 |
| 邮箱 | 输入框 | 否 | 邮箱格式 |
| 性别 | 单选 | 否 | 男/女/未知 |
| 所属科室 | 下拉选择 | 是 | 从科室管理获取 |
| 角色 | 多选下拉 | 是 | 至少选一个 |
| 状态 | 开关 | 是 | 启用/禁用 |

### 3.3 角色权限管理

#### 3.3.1 功能概述

角色管理是系统RBAC权限模型的核心，用于定义系统中的角色，并为每个角色分配菜单权限和数据权限，实现基于角色的访问控制。

#### 3.3.2 数据范围选项

| 选项值 | 显示文字 | 说明 |
|--------|----------|------|
| 1 | 全部数据 | 可查看所有数据 |
| 2 | 本部门数据 | 仅查看所属部门数据 |
| 3 | 本部门及子部门 | 查看本部门及下属部门数据 |
| 4 | 仅本人数据 | 仅查看自己创建的数据 |
| 5 | 自定义 | 指定特定部门 |

#### 3.3.3 核心功能

| 功能 | 说明 |
|------|------|
| 角色列表查询 | 支持按角色名称、角色编码、状态筛选 |
| 新增角色 | 设置角色名称、编码、数据范围、状态 |
| 编辑角色 | 修改角色信息，角色编码不可修改 |
| 删除角色 | 不能删除系统预置角色和已被用户关联的角色 |
| 分配权限 | 为角色分配菜单权限和按钮权限（权限树） |
| 数据权限 | 设置角色的数据查看范围 |

#### 3.3.4 预置角色

| 角色名称 | 角色编码 | 数据范围 | 功能权限 | 说明 |
|----------|----------|----------|----------|------|
| 系统管理员 | admin | 全部数据 | 所有菜单+按钮 | 系统超级管理员 |
| 运营管理员 | operator | 本部门及子部门 | 商品目录、供应商、库存、采购、报表查看 | 日常运营管理 |
| 采购人员 | purchaser | 本部门数据 | 采购管理、供应商管理、采购计划、订单管理 | 采购计划、订单管理 |
| 库管人员 | warehouse | 本部门数据 | 库存管理、入库、出库、盘点、UDI追溯、冷链监控 | 入库、出库、盘点 |
| 科室人员 | dept_user | 仅本人数据 | 科室申领、高值耗材使用登记、收货确认 | 科室申领、使用登记 |
| 财务人员 | finance | 本部门数据 | 结算对账、发票管理、付款审批、报表中心 | 结算对账、报表查看 |
| 质控人员 | qc | 全部数据 | UDI追溯、冷链监控、报表中心、质量监控 | 质量监控、追溯审计 |
| 医生/护士 | doctor | 仅本人数据 | 高值耗材使用、患者关联 | 高值耗材使用 |
| 院领导 | leader | 全部数据 | 报表中心、运营总览 | 运营总览、决策分析 |
| 审计人员 | auditor | 全部数据 | 操作日志、登录日志、数据查询、报表查看（只读） | 数据查询、报表查看 |
| 供应商 | supplier | 仅本人数据 | 商品申请、订单查看、配送管理、对账查询 | 外部供应商 |
| 审批人员 | approver | 本部门及子部门 | 待办审批、审批记录、流程查看 | 各业务审批节点 |

### 3.4 科室管理

#### 3.4.1 功能概述

科室管理是系统组织架构的基础模块，用于维护医院的科室/部门层级结构。科室数据作为用户归属、数据权限、业务流转的基础维度。

#### 3.4.2 页面布局

```
+----------------------------------------------------------+
|  科室管理                                                  |
+----------------------------------------------------------+
|  [新增科室] [展开全部] [折叠全部] [刷新]                    |
+----------------------------------------------------------+
|  [搜索区域]                                                |
|  科室名称: [________]  状态: [全部▼]  [查询] [重置]         |
+----------------------------------------------------------+
|  [树形表格]                                                |
|  科室名称      | 科室编码 | 负责人 | 电话      | 排序 | 状态 | 操作 |
+----------------------------------------------------------+
```

#### 3.4.3 核心功能

| 功能 | 说明 |
|------|------|
| 科室树查询 | 树形展示科室层级，支持展开/折叠 |
| 新增科室 | 支持新增顶级科室和子科室 |
| 编辑科室 | 修改科室信息，支持变更上级科室 |
| 删除科室 | 有子节点或关联用户的科室不能删除 |
| 展开/折叠 | 一键展开或折叠所有节点 |

#### 3.4.4 科室表单字段

| 字段 | 类型 | 必填 | 校验规则 |
|------|------|------|----------|
| 上级科室 | 级联选择 | 条件必填 | 新增子科室时自动填充 |
| 科室名称 | 输入框 | 是 | 2-50字符 |
| 科室编码 | 输入框 | 是 | 2-50字符，字母数字下划线，唯一 |
| 财务科室 | 输入框 | 否 | 2-50字符，关联财务核算体系 |
| 财务科室编码 | 输入框 | 否 | 2-50字符，字母数字下划线，唯一 |
| 科室地址 | 输入框 | 否 | 2-200字符 |
| 院区 | 下拉选择 | 否 | 东院区/西院区/南院区/北院区/主院区 |
| 负责人 | 输入框 | 否 | 2-20字符 |
| 联系电话 | 输入框 | 否 | 电话格式 |
| 排序 | 数字输入 | 否 | 0-999 |
| 状态 | 开关 | 是 | 启用/禁用 |

---

## 4. 主数据管理模块

### 4.1 商品目录管理

#### 4.1.1 功能概述

商品目录管理是SPD系统的核心基础数据模块，管理医院内所有可采购、可使用的医疗耗材、试剂、设备等商品信息。商品目录是采购、库存、科室申领等业务的数据基础。前端菜单按"商品目录"父节点组织，下设"待审批目录"与"医院目录"两个子节点：待审批目录承接新品准入、字段变更、资质更新和价格调整审批；医院目录维护审批通过后的院内正式可用商品。

#### 4.1.2 页面布局

```
+----------------------------------------------------------+
|  商品目录管理                                               |
+----------------------------------------------------------+
|  [统计卡片区]                                               |
|  [商品总数 2,580] [在用 2,300] [停用 280] [即将过期 45]     |
+----------------------------------------------------------+
|  [左侧分类树]  |  [右侧商品列表]                             |
|  [商品分类]     |  [搜索筛选区]                              |
|  ▼ 耗材        |  [操作按钮区]                              |
|  ▶ 试剂        |  [数据表格区]                              |
|  ▶ 设备        |  [分页区]                                  |
+----------------------------------------------------------+
```

#### 4.1.3 商品分类体系

| 一级分类 | 二级分类示例 | 三级分类示例 |
|----------|-------------|-------------|
| 耗材 | 注射类、输液类、敷料类、导管类、手术器械 | 一次性注射器、留置针、输液器、纱布、导尿管 |
| 试剂 | 生化试剂、免疫试剂、分子诊断 | 血糖试纸、尿酸试纸 |
| 设备 | 大型设备、中小型设备、办公设备 | CT、监护仪、打印机 |

#### 4.1.4 核心功能

| 功能 | 说明 |
|------|------|
| 商品列表查询 | 支持多维度搜索：名称、编码、品牌、供应商、分类、状态、价格区间、效期等 |
| 新增商品 | 分Tab页填写：基本信息、价格信息、资质信息、仓储信息 |
| 编辑商品 | 修改商品信息，需走变更审批流程（关键字段） |
| 删除商品 | 逻辑删除，已产生业务数据的商品不可删除 |
| 启用/停用 | 控制商品是否可采购和使用 |
| 导入/导出 | 支持Excel批量导入导出 |
| 批量修改 | 支持批量调整分类、状态、是否定数管理等非唯一字段，关键字段需进入待审批目录 |
| 变更记录 | 查询商品字段变更历史、操作人、审批单据和生效时间 |
| 查看附件 | 查看商品资质附件（注册证、检验报告等） |

#### 4.1.5 商品信息字段

**基本信息Tab：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 商品名称 | 输入框 | 是 | 2-100字符 |
| 商品编码 | 输入框 | 是 | 系统生成或手动输入，唯一 |
| 规格型号 | 输入框 | 是 | 2-100字符 |
| 品牌 | 输入框 | 否 | 2-50字符 |
| 生产厂家 | 输入框 | 是 | 2-100字符 |
| 供应商 | 下拉选择 | 是 | 从供应商管理获取 |
| 商品分类 | 级联选择 | 是 | 三级分类 |
| 单位 | 下拉选择 | 是 | 个/盒/箱/袋/瓶/支等 |
| 状态 | 开关 | 是 | 启用/停用 |
| 商品图片 | 图片上传 | 否 | ≤5MB，jpg/png |

**价格信息Tab：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 采购价 | 数字输入 | 是 | 元，保留2位小数 |
| 零售价 | 数字输入 | 否 | 元 |
| 最小采购量 | 数字输入 | 否 | 默认1 |
| 采购单位 | 下拉选择 | 是 | 与基本单位可能不同 |
| 换算系数 | 数字输入 | 否 | 采购单位与基本单位的换算 |

**资质信息Tab：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| UDI编码 | 输入框 | 否 | 医疗器械唯一标识 |
| 注册证号 | 输入框 | 条件必填 | 医疗器械必填 |
| 注册证有效期 | 日期选择 | 条件必填 | - |
| 生产许可证号 | 输入框 | 否 | - |
| 经营许可证号 | 输入框 | 否 | - |
| 资质附件 | 文件上传 | 否 | pdf/jpg/png |

**仓储信息Tab：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 是否高值耗材 | 开关 | 是 | 默认否 |
| 是否冷链 | 开关 | 是 | 默认否 |
| 是否定数管理 | 开关 | 是 | 表示商品是否允许纳入定数包/定数管理，默认否 |
| 储存条件 | 下拉选择 | 否 | 常温/冷藏/冷冻/避光 |

**是否定数管理变更规则：**

- 高值耗材和冷链试剂不纳入定数包模板，需根据验收入库自动生成的唯一码或填写的 UDI 进行管控。
- `是否定数管理` 是定数业务硬准入开关；业务配置不能突破该硬约束。只有 `是否定数管理 = 是` 的商品可进入定数包模板、自动成包、初始化打包和定数安全量计算。
- 新建/发布定数包模板、新打包时，系统必须校验包内商品 `是否定数管理 = 是`。
- 当 `是否定数管理` 从“是”改为“否”时，系统弹窗提示：`请确认该商品是否存在定数包库存。保存后将禁止生成新的定数包业务，历史已打包库存仍允许继续消耗。`
- 商品改为非定数管理后，历史定数包可继续消耗、反消耗、召回、盘点和结算，但不再生成新模板、新打包、自动成包、定数安全量补包和新的定数缺货任务。
- 当 `是否定数管理` 从“否”改为“是”时，只影响后续业务；历史散货库存不自动成包，需由授权人员通过“历史散货初始化打包”功能人工发起。
- 用户确认后保存并记录操作日志；系统不自动召回、不自动解包。

**采购单价变更规则：**

- 商品目录 `采购价/采购单价` 是验收入库生成系统批次单价的来源字段，不直接代表历史库存批次的当前单价。
- 商品目录采购单价变更会影响后续新验收入库批次单价，不自动改变历史已验收批次单价。
- 商品目录采购单价变更必须审批/复核并强制留痕，记录变更前价格、变更后价格、变更原因、操作人、审批/复核人和生效时间。
- 已验收数据的批次单价固化；除调价管理按批次调价或退货重新采购外，不因商品目录价格变化而改变。
- 验收入库时若最新商品目录采购单价与采购订单预估单价或单据预览价不同，系统弹窗提醒并记录验收确认人、确认时间、原预览价、最新采购单价和验收单号，但不阻断验收。

#### 4.1.6 效期预警标识

| 状态 | 颜色 | 说明 |
|------|------|------|
| 正常 | 绿色 | 效期充足 |
| 预警 | 橙色 | 临近效期（预警天数内） |
| 过期 | 红色 | 已过期 |

### 4.2 待审批商品目录管理

#### 4.2.1 功能概述

待审批商品目录管理是SPD系统中商品准入的核心流程模块。供应商提交新品申请或现有商品信息变更后，需经过审批流程才能正式进入商品目录。

前端菜单名称为"待审批目录"，与"医院目录"共同归属于"商品目录"父节点。审批通过的数据写入医院目录；驳回或退回修改的数据保留在待审批目录中继续跟踪。

#### 4.2.2 业务流程

```
[供应商提交新品申请/变更申请] → [进入待审批池] → [审批人员初审]
    → [审批人员复审/多级审批] → [商品正式入库到商品目录]
    → [驳回：退回供应商，说明原因]
    → [退回修改：退回供应商补充资料]
```

#### 4.2.3 审批状态

| 状态 | 颜色 | 说明 |
|------|------|------|
| 待初审 | 橙色 | 等待初审人员审批 |
| 初审通过 | 蓝色 | 初审已通过，等待复审 |
| 初审驳回 | 红色 | 初审被驳回 |
| 待复审 | 紫色 | 等待复审人员审批 |
| 复审通过 | 绿色 | 复审通过，即将入库 |
| 复审驳回 | 红色 | 复审被驳回 |
| 退回修改 | 黄色 | 退回供应商补充资料 |

#### 4.2.4 申请类型

| 类型 | 说明 |
|------|------|
| 新品准入 | 新商品首次进入商品目录 |
| 信息变更 | 商品信息（价格、规格等）变更 |
| 价格调整 | 采购价格调整 |
| 资质更新 | 注册证、许可证、授权文件等资质材料更新 |
| 停用申请 | 商品停用申请 |

#### 4.2.5 审批详情页

```
+----------------------------------------------------------+
|  审批详情 - SP20260427001          [关闭]                  |
+----------------------------------------------------------+
|  [审批流程时间轴]                                           |
|  ● 提交申请 — 张三 — 2026-04-27 09:30                      |
|  ● 初审中 — 待审批                                          |
|  ○ 复审 — 待处理                                            |
|  ○ 入库完成 — 待处理                                        |
+----------------------------------------------------------+
|  [商品信息]        |  [变更对比 - 信息变更时显示]            |
|  [附件资料]                                                 |
|  [审批记录]                                                 |
|  [审批操作区] [通过] [驳回] [退回修改]                       |
+----------------------------------------------------------+
```

### 4.3 供应商管理

#### 4.3.1 功能概述

供应商是SPD供应链的核心参与方，医院需要对供应商进行全生命周期管理，包括准入审批、资质管理、合同管理、绩效评估等。

#### 4.3.2 功能架构

```
供应商管理
├── 供应商列表
├── 供应商准入申请
├── 供应商审批流程
├── 供应商详情
├── 供应商编辑
├── 供应商启停
├── 资质管理
├── 合同管理
├── 联系人管理
├── 银行账户管理
├── 绩效评估
├── 供应商分级
├── 导入导出
└── 附件管理
```

#### 4.3.3 供应商审批流程

```
[提交申请] → [资质初审] → [现场考察] → [资质复审] → [合同签署] → [系统建档] → [完成]
    ↑           ↓             ↓             ↓             ↓            ↓
    └────── [退回修改] ← [退回修改] ← [退回修改] ← [退回修改] ──────┘
```

#### 4.3.4 供应商等级

| 等级 | 说明 | 合作策略 |
|------|------|----------|
| A | 优秀供应商 | 优先合作，可免检 |
| B | 良好供应商 | 正常合作 |
| C | 合格供应商 | 加强监管 |
| D | 不合格供应商 | 限期整改或淘汰 |

#### 4.3.5 供应商表单字段

| 字段 | 必填 | 校验规则 |
|------|------|----------|
| 供应商名称 | 是 | 2-100字符，不可重复 |
| 统一社会信用代码 | 是 | 18位，校验规则 |
| 供应商类型 | 是 | 生产商/配送商/代理商/经销商 |
| 营业执照 | 是 | PDF/JPG/PNG，最大10MB |
| 经营许可证 | 是 | PDF/JPG/PNG，最大10MB |
| 联系人姓名 | 是 | 2-50字符 |
| 联系电话 | 是 | 手机号或固话格式 |

### 4.4 厂家管理

#### 4.4.1 功能概述

厂家（生产商/制造商）是医疗物资的源头，医院需要建立完整的厂家档案，实现厂家信息的标准化管理。

#### 4.4.2 核心功能

| 功能 | 说明 |
|------|------|
| 厂家列表 | 支持按编码、名称、类型、状态、资质状态筛选 |
| 新增厂家 | 填写厂家基本信息、生产信息、联系人信息 |
| 编辑厂家 | 修改厂家信息 |
| 厂家详情 | 查看基础信息、资质信息、关联商品、关联供应商、操作记录 |
| 资质管理 | 管理厂家资质附件，支持到期预警 |
| 关联商品 | 查看该厂家生产的商品列表 |
| 关联供应商 | 查看代理该厂家的供应商列表 |

#### 4.4.3 厂家表单字段

| 字段 | 必填 | 校验规则 |
|------|------|----------|
| 厂家名称 | 是 | 2-100字符，不可重复 |
| 统一社会信用代码 | 是 | 18位，校验规则 |
| 厂家类型 | 是 | 生产商/品牌商/代工厂 |
| 注册地区 | 是 | 省市区三级选择 |
| 联系人姓名 | 是 | 2-50字符 |
| 联系电话 | 是 | 手机号或固话格式 |

---

## 5. 供应链业务模块

### 5.1 采购管理

#### 5.1.1 功能概述

采购管理是SPD供应链的核心环节，连接供应商和医院库存。系统支持从需求汇总、采购计划、订单生成到订单跟踪的全流程管理。

#### 5.1.2 功能架构

```
采购管理
├── 采购需求
│   ├── 需求汇总
│   ├── 需求审核
│   └── 需求转采购
├── 采购计划
│   ├── 计划生成
│   ├── 计划审核
│   └── 计划执行
├── 采购订单
│   ├── 订单创建
│   ├── 订单审核
│   ├── 订单发送
│   ├── 订单跟踪
│   └── 订单关闭
├── 到货验收
│   ├── 到货登记
│   ├── 质量验收
│   └── 差异处理
├── 采购退货
│   ├── 退货申请
│   ├── 退货审核
│   └── 退货出库
├── 采购报表
│   ├── 采购统计
│   ├── 供应商分析
│   └── 成本分析
└── 采购配置
    ├── 审批流程
    ├── 自动补货规则
    └── 采购模板
```

#### 5.1.3 需求来源

| 来源 | 说明 | 触发条件 |
|------|------|----------|
| 库存补货 | 库存低于安全库存 | 定时任务扫描 |
| 科室申领 | 科室物资消耗 | 科室发起申领 |
| 新品采购 | 新商品首次采购 | 新品准入后 |
| 临时采购 | 紧急物资需求 | 手动创建 |
| 协议采购 | 年度协议执行 | 按协议周期 |

#### 5.1.4 智能补货公式

```
建议采购量 = 安全库存 + 预测消耗量 × 采购周期 - 当前库存 - 在途数量
预测消耗量 = 近3个月平均日消耗量 × 系数
```

#### 5.1.5 采购订单页面

```
+----------------------------------------------------------+
|  采购订单 - CG20260427001                [打印] [发送] [关闭]|
+----------------------------------------------------------+
|  订单信息                                                 |
|  订单编号：CG20260427001                                  |
|  供应商：华润医药                                         |
|  采购类型：常规采购                                       |
|  下单日期：2026-04-27                                     |
|  要求到货日期：2026-05-04                                 |
|  采购人：张三                                             |
|  状态：待发送                                             |
+----------------------------------------------------------+
|  订单明细                                                 |
|  序号 | 商品编码 | 商品名称    | 规格  | 数量  | 单价  | 金额    | 要求效期 |
+----------------------------------------------------------+
|  合计：2 种商品，采购金额 ¥7,100.00                        |
|  运费：¥0.00  税费：¥923.00  订单总额：¥8,023.00           |
+----------------------------------------------------------+
```

#### 5.1.6 采购审核规则

| 条件 | 审核人 | 时限 |
|------|--------|------|
| 金额 ≤ 5万 | 采购主管 | 1工作日 |
| 5万 < 金额 ≤ 50万 | 采购主管 + 财务 | 2工作日 |
| 金额 > 50万 | 采购主管 + 财务 + 院领导 | 3工作日 |

### 5.2 库存管理

#### 5.2.1 功能概述

库存管理是SPD系统的核心模块，涉及医院医疗物资的入库、出库、盘点、调拨等全生命周期管理。

#### 5.2.2 功能架构

```
库存管理
├── 库存总览
├── 入库管理
│   ├── 采购入库
│   ├── 退货入库
│   ├── 调拨入库
│   └── 其他入库
├── 出库管理
│   ├── 科室领用出库
│   ├── 退货出库
│   ├── 调拨出库
│   └── 其他出库
├── 库存查询
├── 库存预警
├── 库存盘点
├── 库存调拨
├── 批次管理
├── 效期管理
├── 库存报表
└── 仓库设置
```

#### 5.2.3 入库业务规则

| 规则 | 说明 |
|------|------|
| 批次管理 | 同一商品不同批次分开管理 |
| 效期管理 | 先进先出（FIFO）原则 |
| 验收要求 | 到货必须验收，不合格不得入库 |
| 扫码要求 | 高值耗材必须扫码入库 |
| 冷链要求 | 冷链商品必须记录温度数据 |

#### 5.2.4 出库业务规则

| 规则 | 说明 |
|------|------|
| 先进先出 | 优先出库较早入库批次 |
| 效期预警 | 优先出库近效期商品 |
| 扫码出库 | 高值耗材必须扫码出库 |
| 审批控制 | 特殊商品出库需审批 |
| 库存校验 | 出库前校验库存是否充足 |

#### 5.2.5 入库单页面

```
+----------------------------------------------------------+
|  采购入库单 - RK20260427001              [打印] [关闭]     |
+----------------------------------------------------------+
|  入库信息                                                 |
|  入库单号：RK20260427001                                  |
|  采购订单：CG20260425001                                  |
|  供应商：华润医药                                         |
|  入库仓库：中心库                                         |
|  入库类型：采购入库                                       |
|  入库日期：2026-04-27                                     |
|  入库人：张三                                             |
+----------------------------------------------------------+
|  入库明细                                                 |
|  序号 | 商品编码 | 商品名称    | 规格  | 批次号    | 效期      | 采购数量 | 实收数量 | 库位   |
+----------------------------------------------------------+
|  合计：入库 2 种商品，实收 1498 件                         |
+----------------------------------------------------------+
```

#### 5.2.6 库存预警

| 预警类型 | 触发条件 | 级别 |
|----------|----------|------|
| 安全库存预警 | 库存低于安全库存 | 重要 |
| 效期预警 | 商品临近效期 | 紧急 |
| 呆滞库存预警 | 超过设定天数无出入库 | 一般 |
| 超储预警 | 库存超过最大库存 | 一般 |

#### 5.2.7 库存状态机与事件快照

库存余额不得由人工直接改最终数值，所有变化必须来自业务单据、定数包事件、盘点调整或系统调整事件。库存查询可使用实时快照表提升性能，但账务真相以库存流水和定数包事件流水为准。

| 状态 | 含义 | 进入来源 | 退出来源 |
|------|------|----------|----------|
| 可用 | 可被申领、拣配、配送或打包 | 合格入库、复核通过、盘盈调整 | 锁定、出库、召回、盘亏 |
| 锁定 | 已被申领、手术单、采购退货或调拨占用 | 申领提交、手术备货、退货申请 | 出库、取消、超时释放 |
| 在途 | 已出库但未被目标库房签收 | 配送出库、跨库调拨出库 | 目标签收、运输异常处理 |
| 隔离 | 不可用，待质控或库管判定 | 冷链超温、召回命中、退回待判定、不良事件 | 转可用、报损、退供应商、销毁 |
| 待复核 | 实物与系统状态需要人工核验 | 反消耗、盘点差异、离线补传冲突 | 审批通过、驳回、系统调整 |
| 待上传 | PDA 本机暂存，未入服务端账 | PDA 离线扫码/记录 | 上传成功、上传失败后重新处理 |

**事件与快照一致性规则：**

- 库存快照只用于查询和看板；事件流水、出入库流水、定数包事件流水是账务依据。
- 系统提供快照重建任务，可按商品、批号、库房、定数包标签重新计算余额。
- 快照与事件重放结果不一致时，生成库存一致性告警，禁止直接手工改余额。
- 纠错必须追加调整事件、盘盈盘亏事件、反消耗事件或重新消耗事件。
- 盘点差异关闭时必须生成盘盈/盘亏调整事件，并关联原盘点单和审批人。
- 全系统以进销存一致为底线，任何出库、配送、消耗、打包、调拨、退供应商、报损、召回处理和 PDA 补传均不得形成负库存。库存不足时只能进入缺货、待补货、待调拨、待采购、应急借用或待复核状态。

#### 5.2.8 系统批次、批号效期与批次单价

系统批次是库存、调价、退货、消耗、盘亏、报损和追溯的账务对象；供应商/生产批号与效期是批次属性，不等同于系统批次号。

**系统批次生成规则：**

| 规则 | 说明 |
|------|------|
| 生成时点 | 验收单审核通过后生成系统批次；人工保存未提交或待审核验收单不生成批次 |
| 批次号规则 | 审核通过日期 `YYYYMMDD` + 后五位自然序列号 |
| 归并判断 | 同一验收单号 + 同一耗材商品目录主键 + 同一生产批号 + 同一效期，归入同一系统批次 |
| 拆分判断 | 验收单号、商品目录主键、生产批号、效期任一不同，拆分生成不同系统批次 |
| 跨天录入 | 同一验收单跨天或多次扫码录入时，以审核通过时的验收单数据为准，不按录入日期拆批 |
| 部分验收 | 同一采购来源分次形成不同验收单或不同审核日期时，按实际审核通过的验收单生成批次 |

**所有权与结算模式快照：**

- 库存流水、库存快照、系统批次、定数包标签快照均必须固化所有权类型和结算模式。
- 所有权类型包括医院买断、供应商寄售、代管等；结算模式包括采购入库结算、科室消耗结算、实销实结、赔付结算等。
- 同一商品同一供应商也可能存在不同合同批次、所有权或结算模式，不得只依赖商品主数据判断。
- 盘亏、报损、退供应商、定数包消耗、红冲和供应商对账必须读取库存对象快照中的所有权和结算模式。
- 买断库存和寄售/实销实结库存禁止混入同一个定数包。

**批号/效期调整规则：**

- 系统批次号不可变更。
- 生产批号、效期可按批号批量调整，调整必须审批并留痕。
- 批号/效期调整为追溯属性更正，允许全量影响当前库存、已打包、已消耗、已结算等记录的展示和追溯属性。
- 批号/效期调整不改变数量、金额、单价和结算状态，不生成供应商追溯信息更正通知，不触发对账/发票重推。
- 调整记录必须保存调整前值、调整后值、调整单号、调整人、审批人、调整时间和影响范围。
- 召回查询必须支持按调整前和调整后的批号/效期都能命中；召回结果以当前更正后的批号/效期展示。
- 批号/效期可调整为与其他系统批次相同，但系统批次不合并。

**批次单价与调价规则：**

- 验收入库审核通过时，系统读取最新商品目录采购单价生成批次单价；采购订单价格仅作为计划价、预估价和数据分析依据。
- 若验收入库时商品目录最新采购单价与采购订单单价或验收预览价不同，系统弹窗提醒并留痕，不阻断验收。
- 批次单价一经验收固化，后续结算以批次单价为准；合同价、集采价只作为商品目录采购单价维护的参考。
- 商品目录采购单价变更只影响后续新验收入库批次，不自动改变历史批次。
- 商品目录采购单价维护错误导致已验收批次价格错误时，只能走调价管理按批次调价，或退货后重新采购/重新验收。
- 批次单价调整必须审批通过后生效并生成价格版本，记录原单价、新单价、原因、依据附件、审批人和影响清单。
- 批次单价调整不覆盖已消耗、已结算、已计费的数据。普通耗材一旦产生消耗数据即固化原消耗单价；验收入库即结算商品在验收结算后固化原入库单价；高值耗材绑定患者并计费后固化原计费单价。
- 未产生结算事实的数据随批次单价调整变化，包括中心库库存、锁定未出库库存、在途未签收库存、科室已签收未消耗库存、已打包未消耗定数包、未绑定患者/未计费高值耗材、冷链隔离或召回但未消耗库存。
- 已产生消耗、验收入库结算或患者计费事实的数据如需纠错，走补差、红冲、计费冲销或财务调整，不直接改原单价。
- 反消耗负向记录必须按原正向消耗记录的单价和金额反向冲回。
- 未消耗库存盘亏、报损、赔付和科室成本按盘亏/报损时有效批次单价计算。
- 科室未消耗库存价值随批次单价调整变化；已消耗成本保持原消耗单价。

**退货重新采购规则：**

- 退货重新采购必须保留原验收入库单、原系统批次、原批次单价和原业务记录。
- 原批次已发生的消耗、结算、患者绑定、反消耗、红冲记录继续挂在原批次，不迁移、不合并到新批次。
- 可退库存通过采购退货单扣减原批次；重新采购生成新采购流程、新验收单、新系统批次和新批次单价。
- 原批次与新批次通过退货重采关系关联，支持追溯但不合并账务事实。

#### 5.2.9 盘点、盘盈盘亏与责任成本

盘盈和盘亏均必须基于真实库存对象处理，不允许通过负库存或无来源库存表达差异。

| 场景 | 处理规则 |
|------|----------|
| 盘盈 | 盘点录入时不直接增加可用库存；复核确认来源后，可生成盘盈入库调整事件并增加可用库存 |
| 盘亏 | 盘亏前提是系统有库存但现场实物不存在或少于系统库存，只能针对系统当前存在的库存对象发起，不存在负库存 |
| 定数包盘点 | 以包标签为主盘点；系统无可用标签的实物包不能直接盘盈为定数包，应复核或重新打包 |
| 高值/冷链盘点 | 必须按 UID/唯一码处理，异常进入追溯、质控或安全审计 |
| 预占散货盘亏 | 扣减预占库存并触发相关未确认打包任务重算 |

盘亏复核通过后扣减系统库存，生成盘亏事件，记录责任科室、保管人、发现人、复核人、审批人、原因、库存对象、有效批次单价和损失金额。盘亏必须计入科室成本/绩效责任。盘亏是否生成供应商结算或赔付数据，按库存所有权和结算模式判断：买断库存通常只影响院内成本；寄售/实销实结库存应按配置生成供应商结算、赔付或对账处理。

### 5.3 科室申领

#### 5.3.1 功能概述

科室申领是医院内部物资流转的核心环节，连接科室消耗和SPD中心库。系统支持多种申领模式，满足不同科室的物资需求。

#### 5.3.2 功能架构

```
科室申领
├── 申领发起
│   ├── 常规申领
│   ├── 紧急申领
│   ├── 定数包申领
│   └── 模板申领
├── 申领审批
│   ├── 审批流程
│   ├── 审批操作
│   └── 审批记录
├── 拣货配送
│   ├── 拣货任务
│   ├── 复核出库
│   └── 配送跟踪
├── 科室收货
│   ├── 收货确认
│   ├── 差异处理
│   └── 退货申请
├── 申领查询
├── 定数包管理
├── 科室库存
└── 消耗统计
```

#### 5.3.3 申领类型

| 类型 | 说明 | 特点 |
|------|------|------|
| 常规申领 | 标准物资申领流程 | 需审批，按流程配送 |
| 紧急申领 | 紧急物资需求 | 绕过正常审批，优先处理，事后补批 |
| 定数包申领 | 按固定数量打包申领 | 简化操作，自动补货 |
| 模板申领 | 保存常用申领组合 | 一键快速申领 |

> 定数包申领仅作为科室端业务入口。定数包模板、安全量、打包标签、事件流水、消耗、反消耗、红冲和 PDA 离线规则以 8.2、8.5 的定数包全生命周期规则为准；本节不单独定义定数包库存和结算口径。

#### 5.3.4 申领审批规则

| 条件 | 审批人 | 时限 |
|------|--------|------|
| 金额 ≤ 1000 | 科室主任 | 即时 |
| 1000 < 金额 ≤ 5000 | 科室主任 + SPD主管 | 2小时 |
| 金额 > 5000 | 科室主任 + SPD主管 + 部门领导 | 4小时 |
| 紧急申领 | 事后补批，先配送 | 24小时内 |

#### 5.3.5 申领页面

```
+----------------------------------------------------------+
|  物资申领                                                 |
+----------------------------------------------------------+
|  申领信息                                                 |
|  申领科室：骨科（当前登录科室）                            |
|  申领类型：常规申领                                       |
|  申领日期：2026-04-27                                     |
|  期望到货：2026-04-28                                     |
|  申领人：张三                                             |
+----------------------------------------------------------+
|  申领明细                                                 |
|  序号 | 商品编码 | 商品名称    | 规格  | 库存可用 | 申领数量 | 单位  | 备注  |
+----------------------------------------------------------+
|  [添加商品] [扫码添加] [清空]                             |
+----------------------------------------------------------+
|  [保存草稿] [提交申领] [取消]                             |
+----------------------------------------------------------+
```

#### 5.3.6 定数包示例

| 定数包名称 | 包含商品 | 数量 | 适用科室 |
|------------|----------|------|----------|
| 骨科换药包 | 一次性输液器 | 10个 | 骨科 |
| | 纱布 | 20片 | |
| | 手套 | 10双 | |
| 换药包 | 纱布 | 10片 | 外科 |
| | 棉签 | 20支 | |
| | 胶带 | 2卷 | |

> 示例仅包含允许定数管理的非高值、非冷链商品。高值耗材和冷链试剂通过 UDI/唯一码独立管控，不纳入定数包模板。

### 5.4 结算对账

#### 5.4.1 功能概述

结算对账是SPD供应链管理的重要环节，涉及医院与供应商之间的货款结算、发票管理、财务对账等业务。

#### 5.4.2 功能架构

```
结算对账
├── 结算单管理
│   ├── 结算单生成
│   ├── 结算单审核
│   ├── 结算单确认
│   └── 结算单关闭
├── 对账管理
│   ├── 自动对账
│   ├── 手动对账
│   └── 差异处理
├── 发票管理
│   ├── 发票录入
│   ├── 发票认证
│   ├── 发票匹配
│   └── 发票查询
├── 付款管理
│   ├── 付款申请
│   ├── 付款审批
│   ├── 付款执行
│   └── 付款查询
├── 质保金管理
│   ├── 质保金冻结
│   ├── 质保金释放
│   └── 质保金查询
├── 结算报表
│   ├── 结算统计
│   ├── 应付分析
│   ├── 付款分析
│   └── 供应商账龄
└── 结算配置
    ├── 结算周期
    ├── 结算方式
    └── 审批流程
```

#### 5.4.3 结算单生成方式

| 方式 | 说明 |
|------|------|
| 自动结算 | 按结算周期自动生成 |
| 手动结算 | 手动选择入库单生成 |
| 实销实结 | 按科室消耗数据生成 |

#### 5.4.4 结算单状态

| 状态 | 说明 | 操作 |
|------|------|------|
| 草稿 | 草稿 | 编辑、删除、提交 |
| 待审核 | 待审核 | 审核、退回 |
| 已审核 | 已审核 | 发送供应商、取消 |
| 待供应商确认 | 待供应商确认 | 确认、驳回 |
| 已确认 | 已确认 | 发票匹配、付款 |
| 已开票 | 已开票 | 付款 |
| 部分付款 | 部分付款 | 继续付款 |
| 已付款 | 已付款 | 关闭 |
| 已关闭 | 已关闭 | - |
| 已取消 | 已取消 | - |

#### 5.4.5 结算单页面

```
+----------------------------------------------------------+
|  结算单 - JS20260427001                [导出] [打印] [关闭]|
+----------------------------------------------------------+
|  结算信息                                                 |
|  结算单号：JS20260427001                                  |
|  供应商：华润医药                                         |
|  结算周期：2026-04-01 至 2026-04-30                       |
|  结算方式：月结                                           |
|  账期：30天                                               |
|  结算金额：¥125,680.00                                    |
|  状态：待审核                                             |
+----------------------------------------------------------+
|  入库明细                                                 |
|  序号 | 入库单号  | 入库日期   | 商品编码 | 商品名称    | 数量  | 单价  | 金额    |
+----------------------------------------------------------+
|  合计：25 笔入库，结算金额 ¥125,680.00                     |
|  质保金（5%）：¥6,284.00  应付金额：¥119,396.00            |
+----------------------------------------------------------+
```

#### 5.4.6 对账结果

| 结果 | 说明 | 处理 |
|------|------|------|
| 完全匹配 | 数量、金额一致 | 自动确认 |
| 数量差异 | 入库数量与订单数量不一致 | 标记差异 |
| 价格差异 | 入库单价与订单单价不一致 | 标记差异 |
| 未匹配 | 有入库单无订单或有订单无入库 | 标记异常 |

---

## 6. 专项管理模块

### 6.1 UDI追溯

#### 6.1.1 功能概述

UDI（Unique Device Identification，医疗器械唯一标识）是医疗器械产品的"身份证"，是实现医疗器械精准识别和全程追溯的基础。国家药监局要求第三类医疗器械全部实施UDI管理。

#### 6.1.2 UDI组成

```
UDI = DI（器械标识） + PI（生产标识）

示例：(01)06901234567890(11)260427(17)280427(10)ABC123(21)XYZ789

DI（器械标识）：(01)06901234567890
  - (01) = GTIN标识符
  - 06901234567890 = 14位GTIN码

PI（生产标识）：
  - (11)260427 = 生产日期 2026-04-27
  - (17)280427 = 有效期至 2028-04-27
  - (10)ABC123 = 批次号
  - (21)XYZ789 = 序列号
```

#### 6.1.3 UDI数据元素

| 数据标识符 | 含义 | 格式 | 示例 |
|------------|------|------|------|
| (01) | GTIN | 14位数字 | 06901234567890 |
| (10) | 批次号 | 1-20位字母数字 | ABC123 |
| (11) | 生产日期 | YYMMDD | 260427 |
| (17) | 有效期 | YYMMDD | 280427 |
| (21) | 序列号 | 1-20位字母数字 | XYZ789 |
| (71) | 生产国家 | 3位数字 | 156 |

#### 6.1.4 功能架构

```
UDI追溯
├── UDI基础管理
│   ├── UDI编码规则
│   ├── UDI解析验证
│   └── UDI数据维护
├── UDI扫码操作
│   ├── 扫码入库
│   ├── 扫码出库
│   ├── 扫码盘点
│   └── 扫码查询
├── UDI追溯链
│   ├── 生产追溯
│   ├── 流通追溯
│   ├── 使用追溯
│   └── 患者关联
├── UDI库存管理
├── UDI报表
└── UDI系统对接
    ├── 国家药监局对接
    ├── 供应商UDI对接
    └── UDI数据库同步
```

#### 6.1.5 UDI验证规则

| 验证项 | 规则 | 错误处理 |
|--------|------|----------|
| GTIN格式 | 14位数字 | 提示"GTIN格式错误" |
| GTIN存在性 | 必须存在于商品目录 | 提示"未找到对应商品" |
| 生产日期 | 不能晚于当前日期 | 提示"生产日期异常" |
| 有效期 | 必须晚于当前日期 | 提示"商品已过期" |
| 批次号 | 1-20位字母数字 | 提示"批次号格式错误" |
| 序列号 | 1-20位字母数字 | 提示"序列号格式错误" |

#### 6.1.6 扫码入库流程

```
[扫描UDI码] → [解析UDI信息] → [查询商品信息] → [显示商品详情]
    → [确认入库数量] → [分配库位] → [入库确认] → [记录UDI追溯信息]
```

#### 6.1.7 UDI/唯一码生命周期

高值耗材和冷链试剂不进入定数包管理，必须以验收入库自动生成的唯一码、UDI 或人工补录的 UID 作为最小管控对象。唯一码在同一租户内必须唯一，不允许删除后复用；确需更换标签时，必须生成新旧码关联关系和更换留痕。

| 状态 | 含义 | 进入来源 | 退出来源 |
|------|------|----------|----------|
| 待验收 | 已到货但未完成验收 | 采购到货、供应商寄售到货 | 验收通过、验收拒收 |
| 已入库 | 已完成验收并建立追溯码 | 扫码验收、人工 UID 补录 | 上架、隔离、退供应商 |
| 在库 | 可被出库、调拨或手术备货 | 上架确认、退回复核通过 | 锁定、出库、召回、报损 |
| 锁定 | 已被业务单据占用 | 手术备货、科室请领、调拨申请 | 出库、取消、超时释放 |
| 已出库 | 已离开当前库房 | 配送出库、手术领用出库 | 科室签收、退回、使用确认 |
| 已使用 | 已与患者、手术或医嘱绑定 | 术中扫码、科室使用确认 | 反记账、质控追溯 |
| 已退回 | 实物退回并待复核 | 科室退回、手术未用退回 | 转在库、隔离、退供应商 |
| 隔离 | 不可用，待质控判定 | 冷链异常、召回命中、包装破损、退回待判定 | 转在库、报损、销毁、退供应商 |
| 已召回 | 被质量事件或供应商召回命中 | 召回通知、监管要求 | 退供应商、销毁、关闭召回 |
| 已报损/销毁 | 实物不可再用 | 质控判定、过期、破损 | 归档，不允许再次流转 |

**管控规则：**
- 每次状态变化都必须生成唯一码事件流水，记录操作人、时间、来源单据、前后状态和院区/库房/科室。
- 高值耗材使用时必须关联患者、手术、医生、科室和收费/医嘱同步结果；未完成关联时不得关闭使用单。
- 冷链试剂唯一码必须能关联温度记录、冷链设备、运输批次和异常处置结果。
- 标签损坏、重贴、人工补录 UID 必须强制留痕，并保留原码、新码、原因和复核人。
- 唯一码库存与财务结算、患者追溯、召回范围以事件流水为准，查询快照只用于展示和性能优化。

### 6.2 高值耗材管理

#### 6.2.1 功能概述

高值耗材（High-Value Consumables）是指单价较高、直接作用于人体、需要严格追溯的医用耗材，如心脏支架、人工关节、起搏器、介入耗材等。

#### 6.2.2 功能架构

```
高值耗材管理
├── 高值耗材目录
│   ├── 耗材分类
│   ├── 耗材准入
│   └── 耗材信息维护
├── 高值耗材库存
│   ├── 扫码入库
│   ├── 扫码出库
│   ├── 库存查询
│   └── 效期管理
├── 手术耗材管理
│   ├── 手术申请
│   ├── 耗材备货
│   ├── 术中扫码
│   └── 术后确认
├── 患者关联
│   ├── 患者信息录入
│   ├── 耗材关联
│   ├── 使用确认
│   └── 计费同步
├── 追溯查询
├── 计费管理
├── 质量监控
└── 统计报表
```

#### 6.2.3 耗材分类体系

| 一级分类 | 二级分类 | 示例 |
|----------|----------|------|
| 心血管介入 | 冠脉支架 | 药物洗脱支架 |
| | 球囊导管 | 冠脉球囊 |
| | 导丝 | 工作导丝 |
| 骨科植入 | 人工关节 | 髋关节假体 |
| | 脊柱植入 | 椎弓根螺钉 |
| | 创伤植入 | 接骨板 |
| 神经外科 | 颅内支架 | 血流导向装置 |
| | 栓塞材料 | 弹簧圈 |
| 起搏电生理 | 起搏器 | 双腔起搏器 |
| | 除颤器 | ICD |
| 外周血管 | 支架 | 外周支架 |
| | 滤器 | 下腔静脉滤器 |

#### 6.2.4 准入流程

```
[科室申请] → [医务部审核] → [设备科审核] → [伦理审查] → [院长审批] → [准入完成]
```

#### 6.2.5 高值耗材入库要求

- 必须扫描UDI码
- 必须验证注册证有效性
- 必须记录批次号和序列号
- 必须验证效期

#### 6.2.6 高值耗材出库要求

- 必须扫描UDI码
- 必须关联手术申请
- 必须关联患者信息
- 必须医生确认

#### 6.2.7 手术耗材申请页面

```
+----------------------------------------------------------+
|  手术耗材申请                                             |
+----------------------------------------------------------+
|  手术信息                                                 |
|  患者姓名：张三                                           |
|  身份证号：110101199001011234                             |
|  住院号：ZY20260001                                       |
|  手术名称：冠状动脉支架植入术                             |
|  手术医生：李主任                                         |
|  手术时间：2026-04-27 09:00                               |
|  手术科室：心内科                                         |
+----------------------------------------------------------+
```

### 6.3 冷链监控

#### 6.3.1 功能概述

冷链试剂对温度敏感，需要在规定的温度范围内储存和运输。温度超标可能导致试剂失效，严重影响检测准确性。

#### 6.3.2 功能架构

```
冷链监控
├── 实时监控
│   ├── 仓库温度监控
│   ├── 冷藏柜监控
│   ├── 运输车辆监控
│   └── 科室冰箱监控
├── 预警管理
│   ├── 温度超标预警
│   ├── 设备故障预警
│   ├── 断电预警
│   └── 预警处理
├── 数据记录
│   ├── 温度数据记录
│   ├── 异常事件记录
│   └── 处理记录
├── 追溯查询
├── 设备管理
│   ├── 设备台账
│   ├── 设备校准
│   └── 设备维护
├── 报表统计
└── 系统配置
    ├── 温度阈值配置
    ├── 预警规则配置
    └── 设备参数配置
```

#### 6.3.3 监控点类型

| 监控点 | 温度范围 | 用途 |
|--------|----------|------|
| 冷藏库 | 2-8℃ | 冷藏试剂 |
| 冷冻库 | -20℃ | 冷冻试剂 |
| 超低温库 | -80℃ | 特殊试剂 |

#### 6.3.4 监控大屏

```
+----------------------------------------------------------+
|  冷链监控中心                                             |
+----------------------------------------------------------+
|  [监控状态卡片]                                           |
|  +----------+ +----------+ +----------+ +----------+     |
|  | 监控点数 | | 正常    | | 预警    | | 异常    |     |
|  |   56    | |   52    | |    3    | |    1    |     |
|  +----------+ +----------+ +----------+ +----------+     |
+----------------------------------------------------------+
|  [仓库监控]              [运输监控]              [科室监控]|
+----------------------------------------------------------+
|  [实时温度曲线]                                           |
+----------------------------------------------------------+
```

#### 6.3.5 预警类型

| 预警类型 | 触发条件 | 级别 | 通知方式 |
|----------|----------|------|----------|
| 温度超标 | 温度超出阈值 | 紧急 | 系统+短信+电话 |
| 温度预警 | 温度接近阈值 | 重要 | 系统+短信 |
| 设备离线 | 设备断网/断电 | 紧急 | 系统+短信+电话 |
| 设备故障 | 传感器故障 | 重要 | 系统+短信 |
| 长时间未更新 | 数据超过5分钟未更新 | 一般 | 系统消息 |

#### 6.3.6 温度阈值配置

| 配置项 | 说明 |
|--------|------|
| 温度范围 | 2-8℃ |
| 上限预警 | 7.5℃（提前0.5℃预警） |
| 上限超标 | 8.0℃ |
| 下限预警 | 2.5℃（提前0.5℃预警） |
| 下限超标 | 2.0℃ |

#### 6.3.7 冷链异常处置闭环

冷链异常不能只停留在告警展示，必须形成可关闭、可追溯、可审计的处置闭环。异常来源包括温度超限、温度长时间未更新、设备离线、传感器故障、人工补录温度异常、运输交接异常。

```
[异常触发] → [自动冻结/隔离相关库存] → [通知责任人]
    → [质控判定] → [可用/继续隔离/报损/销毁/退供应商/召回]
    → [生成补货或采购建议] → [关闭异常并归档]
```

| 处置节点 | 系统要求 |
|----------|----------|
| 异常触发 | 记录设备、库位、温度值、持续时长、影响商品/批号/唯一码范围 |
| 自动隔离 | 命中库存进入隔离状态，禁止出库、消耗、结算和调拨 |
| 责任通知 | 按院区、库房、质控、设备责任人分层配置通知策略 |
| 质控判定 | 必须填写判定结论、依据、附件和复核人 |
| 库存处理 | 支持转可用、继续隔离、报损、销毁、退供应商、召回 |
| 补货联动 | 对影响安全库存的商品生成补货建议或紧急采购建议 |
| 关闭归档 | 关闭时校验所有命中唯一码/批号均有处理结果 |

**约束：**
- 冷链异常关闭前，命中的高值耗材、冷链试剂和批号库存不得参与正常业务。
- 设备离线期间补传的温度数据按服务端处理时间入账，但必须标记为离线补传数据。
- 若补传数据证明历史超温，应按当前处理时间触发异常闭环，不反向强制修改已关账数据。
- 异常处置结果需要进入追溯链路，支持按唯一码、批号、设备、库位、院区反查。

---

## 7. 运营支撑模块

### 7.1 报表中心

#### 7.1.1 功能概述

报表中心是SPD系统的数据分析和决策支持模块，通过对各业务模块数据的汇总、分析和可视化展示，帮助医院管理层了解供应链运营状况，发现问题，优化决策。

#### 7.1.2 功能架构

```
报表中心
├── 运营总览
│   ├── 供应链总览
│   ├── 关键指标看板
│   └── 运营趋势分析
├── 采购报表
│   ├── 采购统计
│   ├── 供应商分析
│   └── 价格分析
├── 库存报表
│   ├── 库存总览
│   ├── 出入库统计
│   └── 库存周转分析
├── 科室报表
│   ├── 消耗统计
│   ├── 费用分析
│   └── 申领分析
├── 财务报表
│   ├── 结算统计
│   ├── 应付分析
│   └── 成本核算
├── 质量报表
│   ├── 质量统计
│   ├── 追溯报表
│   └── 合规报表
├── 自定义报表
│   ├── 报表设计器
│   ├── 数据源配置
│   └── 报表订阅
├── 报表管理
│   ├── 报表目录
│   ├── 权限管理
│   └── 定时推送
├── 数据统计分析
│   ├── 数据仓库管理
│   │   ├── ETL任务配置
│   │   ├── 数据清洗规则
│   │   └── 数据质量监控
│   ├── 数据集市构建
│   │   ├── 主题域模型
│   │   ├── 维度建模
│   │   └── 指标定义
│   ├── 多维分析
│   │   ├── 上卷/下钻分析
│   │   ├── 切片/切块分析
│   │   └── 同比/环比分析
│   └── 智能分析
│       ├── 异常检测
│       ├── 趋势预测
│       └── 关联分析
└── 数据统计展示
    ├── 仪表盘设计器
    │   ├── 图表组件库
    │   ├── 布局配置
    │   └── 交互配置
    ├── 数据大屏
    │   ├── 供应链大屏
    │   ├── 库存监控大屏
    │   └── 运营指挥大屏
    ├── 移动端看板
    │   ├── 院长驾驶舱
    │   ├── 科室消耗看板
    │   └── 采购执行看板
    └── 报表可视化
        ├── 表格报表
        ├── 图表报表
        └── 混合报表
```

#### 7.1.3 关键指标看板

| 指标 | 说明 | 计算方式 |
|------|------|----------|
| 库存周转天数 | 库存周转效率 | 平均库存/日均消耗 |
| 缺货率 | 缺货情况 | 缺货次数/总申领次数 |
| 采购及时率 | 供应商交货及时性 | 及时交货订单/总订单 |
| 验收合格率 | 到货质量 | 合格批次/总批次 |
| 结算周期 | 平均结算时间 | 结算天数/结算单数 |
| 定数包满足率 | 定数包供应情况 | 满足次数/总需求次数 |

#### 7.1.4 供应链总览页面

```
+----------------------------------------------------------+
|  供应链运营总览                                           |
+----------------------------------------------------------+
|  [关键指标卡片]                                           |
|  +----------+ +----------+ +----------+ +----------+     |
|  | 商品品种 | | 供应商数 | | 库存金额 | | 月消耗额 |     |
|  |  2,580   | |   128    | | 856.5万  |  325.6万  |     |
|  +----------+ +----------+ +----------+ +----------+     |
+----------------------------------------------------------+
|  [趋势图表]                                               |
|  +------------------------+ +------------------------+     |
|  | 月度采购趋势            | | 月度消耗趋势          |     |
|  +------------------------+ +------------------------+     |
|  +------------------------+ +------------------------+     |
|  | 品类消耗占比            | | 供应商采购占比        |     |
|  +------------------------+ +------------------------+     |
+----------------------------------------------------------+
```

### 7.2 系统配置

#### 7.2.1 功能概述

系统配置是SPD平台的基础支撑模块，为各业务模块提供参数配置、流程配置、权限配置等功能。灵活的系统配置能够满足不同医院的个性化需求。

#### 7.2.2 功能架构

```
系统配置
├── 基础配置
│   ├── 医院信息
│   ├── 组织架构
│   ├── 科室管理
│   └── 仓库配置
├── 业务流程配置
│   ├── 审批流程
│   ├── 业务规则
│   └── 工作流配置
├── 权限配置
│   ├── 角色管理
│   ├── 菜单权限
│   ├── 数据权限
│   └── 按钮权限
├── 参数配置
│   ├── 系统参数
│   ├── 业务参数
│   └── 预警参数
├── 日志审计
│   ├── 操作日志
│   ├── 登录日志
│   └── 异常日志
├── 数据管理
│   ├── 数据字典
│   ├── 编码规则
│   └── 数据备份
└── 系统集成
    ├── 接口配置
    ├── 定时任务
    └── 消息通知
```

#### 7.2.3 审批流程配置

| 流程 | 适用业务 | 节点数 | 审批角色 |
|------|----------|--------|----------|
| 商品准入审批 | 商品目录 | 5 | 运营管理员初审 → 质控人员复审 → 院领导终审 |
| 供应商准入审批 | 供应商管理 | 5 | 运营管理员初审 → 采购人员复审 → 财务人员资质审核 → 质控人员合规审核 → 院领导终审 |
| 采购计划审批 | 采购管理 | 2 | 采购人员发起 → 运营管理员审批 |
| 采购订单审批 | 采购管理 | 3 | 采购人员发起 → 运营管理员审核 → 院领导审批(>50万) |
| 申领审批 | 科室申领 | 2 | 科室人员发起 → 科室主任审批(≤1000) / SPD主管审批(>1000) |
| 结算审批 | 结算对账 | 2 | 财务人员发起 → 运营管理员审核 |
| 付款审批 | 结算对账 | 3 | 财务人员发起 → 运营管理员审核 → 院领导审批 |
| 退货审批 | 采购管理 | 2 | 库管人员发起 → 运营管理员审批 |
| 高值耗材使用审批 | 高值耗材 | 2 | 医生发起 → 科室主任审批 |
| 价格变更审批 | 商品目录 | 3 | 运营管理员发起 → 财务人员审核 → 院领导审批 |

#### 7.2.4 医院信息配置

```
+----------------------------------------------------------+
|  医院信息配置                                             |
+----------------------------------------------------------+
|  基本信息                                                 |
|  医院名称：[XX医院]                                       |
|  医院等级：[三级甲等 ▼]                                   |
|  医院编码：[________]                                     |
|  统一社会信用代码：[________]                             |
|  医院地址：[________]                                     |
|  联系电话：[________]                                     |
+----------------------------------------------------------+
|  SPD信息                                                  |
|  SPD运营商：[________]                                    |
|  SPD服务范围：[√]耗材 [√]试剂 [√]设备                     |
|  服务模式：[托管 ▼]                                       |
|  合作开始日期：[________]                                 |
+----------------------------------------------------------+
```

### 7.3 SaaS化与配置中心

#### 7.3.1 功能概述

SaaS化与配置中心用于支撑集团医院、多院区、多租户、差异化模块订阅和运行时参数配置。系统通过租户、模块订阅、配置项、配置发布、灰度回滚和变更审计，将医院差异化规则从代码中抽离，支撑多租户隔离和配置化交付。

#### 7.3.2 核心能力

| 能力 | 说明 |
|------|------|
| 租户管理 | 维护集团、医院、院区等租户身份、部署模式和数据驻留策略 |
| 模块订阅 | 管理租户可用模块、版本、配额和功能开关 |
| 配置项管理 | 支持系统级、租户级、院区级、科室级、仓库级、模块级、用户级配置 |
| 配置发布 | 支持草稿、提交、审批、发布、灰度、回滚、归档 |
| 配置审计 | 配置变更写入配置审计、统一审计日志和审计双写队列，支持追溯 |
| 密钥引用 | 敏感配置不保存明文，仅保存 KMS/Vault 引用，发布前校验 KMS 引用状态 |

#### 7.3.3 配置优先级

```text
用户级 > 模块级 > 仓库级 > 科室级 > 院区级 > 租户级 > 系统级 > 默认值
```

权限类配置按用户/角色/岗位优先；业务策略类配置按“更细粒度优先”命中，配置中心需能展示最终生效值和来源配置。

**业务策略配置优先级：**

| 优先级 | 配置范围 |
|--------|----------|
| 1 | 商品 + 科室 |
| 2 | 商品 + 库房 |
| 3 | 商品 + 院区 |
| 4 | 商品 + 医院 |
| 5 | 商品分类 + 科室 |
| 6 | 商品分类 + 库房 |
| 7 | 商品分类 + 院区 |
| 8 | 商品分类 + 医院 |
| 9 | 科室 |
| 10 | 库房 |
| 11 | 院区 |
| 12 | 医院 |
| 13 | 租户/集团 |
| 14 | 系统默认 |

**配置填写要求：**

| 字段 | 要求 |
|------|------|
| 配置类型 | 必填，如补货策略、定数包系数、散货替代、反消耗审批、日清策略、调价策略 |
| 适用范围 | 必填，医院、院区、库房、科室、商品分类、商品等 |
| 适用对象 | 必填，选择具体院区、库房、科室或商品 |
| 配置值 | 必填，按配置类型填写 |
| 生效方式 | 必填，实时生效 / 定时生效 |
| 生效时间 | 定时生效必填，实时生效默认为当前时间 |
| 失效时间 | 可选 |
| 风险等级 | 系统按配置类型识别，可由授权人员调整 |
| 变更原因 | 必填 |
| 审批要求 | 按风险等级决定直接生效、复核或审批 |
| 配置状态 | 草稿 / 待审批 / 已生效 / 已驳回 / 已失效 / 已作废 |
| 附件依据 | 高风险配置建议必填 |

**配置冲突与生效规则：**

- 同一配置类型、同一适用范围、同一对象、同一时间段不允许存在多条生效配置。
- 上下级配置可同时存在，细粒度配置覆盖粗粒度配置。
- 未来待生效配置也参与冲突检测；已失效、已驳回、已作废配置不参与当前命中。
- 配置变更支持实时生效和定时生效。定时配置到点后，按配置类型触发重算、下一轮计算或权限变更，并记录生效日志。
- 配置审批驳回后保留草稿和驳回原因，当前生效配置不变。
- 配置按医院、院区、库房、科室、商品分类、商品等范围灰度生效；前台允许授权用户新增、修改、停用和复制配置。

**配置命中说明：**

关键业务页面需提供“查看配置来源”入口，点击后展示最终生效值、配置类型、来源层级、来源配置编号、适用对象、生效时间、失效时间、是否覆盖上级配置、上级配置值、最近变更人和审批状态。普通查看配置来源不记录查看日志。普通业务单据导出/打印默认不展示配置来源；审批类或审计导出可选择包含配置来源。

#### 7.3.4 对外交付口径

配置中心已形成 DDL、详细设计和 OpenAPI 契约：

| 交付物 | 文件 |
|--------|------|
| DDL | `docs/db/22_saas_config_center.sql` |
| 详细设计 | `docs/design/modules/7.2-7.3-saas-config.md` |
| OpenAPI | `docs/api/paths/config-center.yaml` |

#### 7.3.5 P0 配置项目录基线

以下配置项为一期上线必须具备的业务策略配置基线。配置项需支持前台新增、修改、停用、复制、实时生效、定时生效、灰度范围、冲突检测、风险等级和命中说明。

| 配置项 | 默认值/默认策略 | 适用范围 | 风险等级 | 生效动作 |
|--------|-----------------|----------|----------|----------|
| 补货任务生成模式 | 生成补货建议 | 医院/院区/库房/科室/商品 | 中 | 下一轮补货计算 |
| 补货定时任务频率 | 每日固定时间窗 | 医院/院区/库房/科室/商品 | 中 | 定时任务调度 |
| 补货数量策略 | 补到目标或最大 | 科室/商品/定数包包族 | 中 | 下一轮补货计算 |
| 缺货登记排序规则 | 紧急等级 + 登记时间 | 医院/院区/科室/商品 | 中 | 下一轮来货匹配 |
| 散货替代定数包缺货 | 默认不允许 | 医院/院区/科室/商品 | 高 | 未满足缺货登记下一轮匹配 |
| 定数包自动成包模式 | 待确认打包任务 | 库房/商品 | 高 | 后续验收入库/打包 |
| 定数包系数 | 按商品模板维护 | 商品/科室/库房 | 高 | 重算未确认打包任务 |
| 未确认打包任务超时策略 | 提醒，不自动释放 | 库房/商品 | 中 | 超时提醒/释放 |
| 标签补打权限与原因 | 必填原因并留痕 | 角色/库房/科室 | 中 | 立即生效 |
| 日清后反消耗流程 | 科室审批 + SPD 复核 | 医院/院区/科室 | 高 | 后续反消耗申请 |
| 批次单价调价审批 | 财务复核必选 | 医院/院区/商品 | 高 | 审批通过后生成价格版本 |
| 批号/效期批量调整审批 | 必须审批 | 医院/院区/商品 | 高 | 审批通过后更新追溯属性 |
| 盘亏计入科室成本 | 计入 | 医院/院区/科室 | 高 | 后续盘亏复核 |
| PDA 离线强一致限制 | 禁止审批/反消耗/召回/结算 | 系统/医院/院区 | 高 | 立即生效 |
| 召回/隔离补货触发 | 定时任务扫描触发 | 医院/院区/科室/商品 | 中 | 下一轮补货计算 |

高风险配置按风险等级进入复核或审批；审批驳回保留草稿和驳回原因。配置变更不回改已完成业务事实，只影响后续业务、未完成任务或按规则触发重算。

---

## 8. 院内SPD业务扩展模块

> 本章在通用供应链能力之上补充 **SPD 院内精细化运营**所需的业务节点，包括基础字典扩展、请补货/收货/上架/拣配/库内作业、科室业务、退回业务、手术临床、消耗结算与发票节点等，覆盖手持终端、RFID、定数包、一物一码、HIS 对照、负数账反消耗、寄售跟台等典型 SPD 场景。

### 8.1 基础信息扩展

#### 8.1.1 功能概述

基础信息扩展用于补充 SPD 院内业务所需的字典与对照数据，是上层业务流程（请货、收货、上架、出库、消耗、结算）的运行前提。

#### 8.1.2 功能架构

```
基础信息扩展
├── 产品对应医院统计分类维护
├── 产品对应计量单位维护
├── 库房管理
│   ├── 实物库房维护
│   ├── 虚拟库房维护
│   ├── 库房按科室统计要求分类
│   └── 库房上下级对照维护
├── 货位管理
│   ├── 货位维护
│   └── 散货引导货位维护
├── 库房权限
│   ├── 人员库房权限维护（列表）
│   └── 人员库房权限维护（树形）
├── 手持终端配置
│   ├── 手持岗位维护
│   ├── 手持岗位权限
│   └── 手持人员对应岗位
├── HIS对照
│   ├── HIS科室与SPD库房对照
│   └── HIS人员与SPD人员对照
├── 批号管理
│   ├── 批号新增
│   └── 批号修改
└── 系统关键配置项维护
```

#### 8.1.3 产品对应医院统计分类维护

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 统计分类编码 | 输入框 | 是 | 5-20 字符，唯一 |
| 统计分类名称 | 输入框 | 是 | 2-50 字符 |
| 上级分类 | 级联选择 | 否 | 支持多级 |
| 分类用途 | 多选 | 是 | 卫健委上报/院内成本核算/医保对账/绩效考核 |
| 关联产品 | 弹窗多选 | 否 | 支持批量绑定/解绑 |
| 状态 | 开关 | 是 | 启用/停用 |

> 与商品三级分类（耗材/试剂/设备）解耦，仅作为统计维度。一个产品可同时绑定多个统计分类。

#### 8.1.4 产品对应计量单位维护

| 字段 | 必填 | 说明 |
|------|------|------|
| 基本单位 | 是 | 与商品 `单位` 字段一致（个/盒/瓶等） |
| 采购单位 | 是 | 与基本单位换算 |
| 申领单位 | 是 | 通常等于基本单位 |
| 消耗单位 | 是 | 通常等于最小拆分单位 |
| 换算关系 | 是 | 1 箱 = N 盒 = M 个，多级换算链 |
| 是否拆零 | 是 | 拆零商品出库使用最小单位 |

**业务规则：**
- 同一产品可维护"采购→入库→申领→消耗"四级单位换算链。
- 拆包业务依赖此换算关系自动计算拆零数量。

#### 8.1.5 库房维护与虚拟库房

| 库房类型 | 说明 | 典型用途 |
|----------|------|----------|
| 实物库房（中心库） | SPD 中心仓 | 总库存、采购入库 |
| 实物库房（科室二级库） | 科室物理库房 | 科室收货、消耗扣减 |
| **虚拟库房** | 不对应物理空间 | 在途库存、隔离库存、寄售库存、定数包待拆库 |
| 应急库房 | 紧急调拨用 | 突发事件调用 |

**库房表单字段：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 库房编码 | 是 | 唯一 |
| 库房名称 | 是 | 2-50 字符 |
| 库房类型 | 是 | 实物/虚拟/应急 |
| 上级库房 | 否 | 支持多级，参与「库房上下级对照维护」 |
| 所属院区 | 是 | 主院区/东院区等 |
| 关联科室 | 否 | 二级库需关联科室 |
| 库房负责人 | 是 | 用户选择 |
| 是否参与统计 | 是 | 默认参与 |
| 统计要求分类 | 多选 | 院领导驾驶舱/科主任看板/财务核算/物流绩效 |

#### 8.1.6 货位维护与散货引导货位

**货位维护：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 货位编码 | 是 | A 区-01 排-02 列-03 层 形式 |
| 所属库房 | 是 | 关联库房 |
| 货位类型 | 是 | 整件位/散货位/高值位/冷藏位/隔离位 |
| 容量上限 | 否 | 件数或体积 |
| 关联产品 | 否 | 固定货位绑定的产品 |
| 状态 | 是 | 启用/锁定/停用 |

**散货引导货位维护：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 产品编码 | 是 | 唯一对应一个产品 |
| 引导货位 | 是 | 散货拆零优先存放位 |
| 备用货位 | 否 | 主货位满后启用 |
| 最低存量 | 是 | 低于该值触发补散货 |
| 最高存量 | 是 | 高于该值停止补散货 |

> 在「散货收货」与「申领单散货打包出库转补货」节点中，系统按引导货位自动分配上架/补散货位。

#### 8.1.7 人员库房权限维护

**列表视图：** 矩阵式表格，行为人员、列为库房，支持单元格批量勾选「查看 / 入库 / 出库 / 盘点 / 调拨 / 管理员」。

**树形视图：** 左树为「库房树」，右侧为「该库房可访问人员清单」，支持拖拽。

| 操作 | 说明 |
|------|------|
| 批量授权 | 选择多人 + 多库房一次性授权 |
| 权限继承 | 上级库房权限自动继承到下级（可覆盖） |
| 权限审计 | 每次变更产生审计日志 |
| 临时授权 | 支持设置有效期，到期自动回收 |

#### 8.1.8 手持终端配置

**手持岗位维护：**

| 岗位编码 | 岗位名称 | 默认权限 |
|----------|----------|----------|
| HD-RECV | 收货员 | 收货扫码、打包扫码 |
| HD-SHELF | 上架员 | 客户端标签上架、货位绑定 |
| HD-PICK | 拣货员 | 拣配作业、出库配送 |
| HD-COUNT | 盘点员 | 手持盘点、差异上报 |
| HD-DEPT | 科室收货员 | 科室收货、日清确认 |

**手持岗位权限：** 控制 PDA 端可见菜单与按钮（按钮级权限）。

**手持人员对应岗位：** 一名人员可绑定多个岗位，登录 PDA 后切换岗位上下文。

#### 8.1.9 HIS 对照管理

**HIS 科室与 SPD 库房对照：**

| 字段 | 说明 |
|------|------|
| HIS 科室编码 | 来自 HIS 系统 |
| HIS 科室名称 | 来自 HIS 系统 |
| SPD 库房编码 | 关联 8.1.5 库房 |
| 计费方式 | 实领实结/实销实结/月结 |
| 同步状态 | 已同步/待同步/同步失败 |

**HIS 人员与 SPD 人员对照：**

| 字段 | 说明 |
|------|------|
| HIS 工号 | 来自 HIS 系统 |
| HIS 姓名 | 来自 HIS 系统 |
| SPD 用户ID | 关联 `sys_user` |
| 角色映射 | 医生/护士/库管 → SPD 角色 |

> 高值耗材计费推送、消耗回写 HIS 均依赖此对照。

#### 8.1.10 批号管理

**批号新增：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 商品编码 | 是 | 关联商品 |
| 批号 | 是 | 1-30 字符 |
| 生产日期 | 是 | - |
| 有效期至 | 是 | - |
| 注册证号 | 否 | 自动带入 |
| 供应商 | 是 | - |
| 入库单号 | 否 | 收货时回填 |

**批号修改：** 仅允许修改"生产日期、有效期、注册证号"，不允许修改批号本身（修改时强制留痕，需运营管理员审批）。

#### 8.1.11 系统关键配置项维护

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 安全库存预警系数 | 1.2 | 实际安全库存 = 配置值 × 历史均消耗 |
| 效期预警天数 | 90 | 临近效期提醒 |
| 拆零是否自动 | 是 | 出库时自动拆零 |
| 定数包补货阈值 | 0.5 | 已消耗 ≥ 50% 触发补货 |
| 高值出库强制扫码 | 是 | - |
| 散货补货周期 | 1 天 | 散货引导货位补货扫描周期 |
| 手持自动登出 | 30 分钟 | PDA 无操作自动登出 |
| HIS 推送方式 | 实时 | 实时/定时/手动 |
| 不良事件强制上报 | 是 | - |

> 关键配置项变更需「运营管理员发起 → 院领导审批」，所有变更纳入审计日志。

### 8.2 请补货与采购扩展

#### 8.2.1 功能概述

补充 SPD 院内"请补货 → 申购 → 采购套餐 → 中心库/科室分级审批 → 请货单"的完整业务链，覆盖审计中缺失的自动请补货、定数包/一物一码安全量监控、采购套餐、申购、请货单等节点。

#### 8.2.2 功能架构

```
请补货与采购扩展
├── 自动请补货管理
│   ├── 补货策略配置
│   ├── 自动扫描任务
│   └── 请补货单生成
├── 安全量配置与监控
│   ├── 定数包模板维护
│   ├── 定数包安全量维护
│   ├── 定数包材料对应安全量监控
│   └── 一物一码材料对应安全量监控
├── 申购管理
│   ├── 申购单新建
│   ├── 申购审批
│   └── 申购转采购
├── 采购套餐维护
├── 中心库采购审批
├── 科室采购审批
└── 请货单管理
    ├── 请货单新建
    ├── 请货单审核
    ├── 请货单查询
    └── 科室申领单配送审核
```

#### 8.2.3 自动请补货管理

**补货策略配置：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 策略名称 | 是 | 中心库定时补、科室二级库实时补、定数包阈值补 |
| 触发条件 | 是 | 库存 ≤ 安全库存 / 定数包消耗率 ≥ 阈值 / 一物一码缺货 |
| 扫描频率 | 是 | 实时 / 5 分钟 / 30 分钟 / 每日 |
| 适用库房 | 是 | 多选 |
| 适用产品分类 | 否 | 不选则全部适用 |
| 自动提交 | 是 | 是：直达审批；否：草稿待人工确认 |

**请补货单生成流程：**

```
[定时任务/事件触发] → [扫描安全量] → [按补货策略计算补货量]
    → [生成请补货单（草稿）] → [按策略自动/手动提交]
    → [中心库或上级库房审批] → [转采购订单 或 转调拨单]
```

| 字段 | 说明 |
|------|------|
| 请补货单号 | BHD + YYYYMMDD + 4位序号 |
| 申请库房 | 自动带入 |
| 目标库房 | 上级库房 |
| 触发策略 | 自动带入 |
| 明细 | 商品、当前库存、安全库存、建议补货量、实际补货量 |

#### 8.2.4 定数包模板维护

定数包模板用于维护包族、模板版本、包内商品及包装数量，是后续安全量配置、打包、消耗、反消耗和红冲追溯的基础。模板发布后生成版本号，历史模板不得覆盖修改；新版本只影响后续新打包。

| 字段 | 必填 | 说明 |
|------|------|------|
| 包族编码 | 是 | 唯一，表示同一业务含义的定数包，如骨科手术包 |
| 定数包名称 | 是 | 例：骨科手术包 |
| 模板版本 | 自动 | 发布时生成，禁止覆盖旧版本 |
| 包内商品 | 是 | 商品 + 包装数量 + 单位；仅允许选择 `是否定数管理 = 是` 的商品 |
| 状态 | 是 | 草稿 / 已发布 / 已停用 |
| 适用说明 | 否 | 使用场景、打包注意事项等 |

**模板发布规则：**

- 发布时校验包内商品主数据 `是否定数管理 = 是`。
- 高值耗材和冷链试剂不纳入定数包模板。
- 已发布模板不得直接覆盖修改，变更包内商品或包装数量时发布新版本。
- 打包生成定数标签时固化当时模板版本和包内明细快照。
- 新版本发布后默认自然替换，旧版包允许继续消耗。

#### 8.2.5 定数包安全量维护

定数包安全量维护用于配置院区、科室、库房维度的定数包保有量和补货策略。安全量按包族配置，可用库存默认跨模板版本汇总，新增补货按当前启用版本打包。

| 字段 | 必填 | 说明 |
|------|------|------|
| 院区 | 是 | 多院区场景必填 |
| 科室 | 是 | 定数包使用科室 |
| 库房/货位 | 否 | 科室二级库或指定货位 |
| 包族编码 | 是 | 引用定数包模板维护中的包族编码 |
| 安全包数 | 是 | 该科室必须保有的最低包数 |
| 最大包数 | 否 | 上限，超量不再补 |
| 补货策略 | 是 | 阈值补 / 定时补 / 手动补 |
| 状态 | 是 | 启用 / 停用 |

#### 8.2.6 定数包材料对应安全量监控

| 监控视图 | 说明 |
|----------|------|
| 按定数包视图 | 列出每个定数包当前实际包数 / 安全包数 / 状态 |
| 按材料视图 | 列出该材料在所有定数包中的累计需求量 vs 库存量 |
| 状态色 | 绿色（充足）/ 橙色（预警）/ 红色（缺货） |

> 状态变更触发自动请补货任务。

#### 8.2.7 一物一码材料对应安全量监控

| 字段 | 说明 |
|------|------|
| 一物一码标识 | 每件单品唯一码（UDI/RFID/SN） |
| 材料编码 | 关联商品 |
| 当前在库件数 | 唯一码扫描计数 |
| 安全件数 | 配置阈值 |
| 监控状态 | 充足/预警/缺货 |
| 关联科室 | 高值耗材类专用 |

#### 8.2.8 申购管理

申购（科室/部门发起的非急用物资申请，区别于"申领"——申领针对已在目录的常用物资，申购可申请尚未入院或非常规物资）。

| 字段 | 必填 | 说明 |
|------|------|------|
| 申购单号 | 自动 | SG + YYYYMMDD + 序号 |
| 申购科室 | 是 | 自动带入 |
| 申购类型 | 是 | 新品申购 / 临时申购 / 试用申购 |
| 商品（库内/外购） | 是 | 库内可选商品；外购填写名称、规格、厂家、参考价 |
| 数量、用途 | 是 | - |
| 期望到货时间 | 是 | - |
| 附件 | 否 | 临床论证、价格对比 |

**申购流程：**
```
[科室发起] → [科室主任审批] → [医务/设备科论证] → [运营管理员审批]
    → [转商品准入流程或直接转采购] → [完成]
```

#### 8.2.9 采购套餐维护

| 字段 | 必填 | 说明 |
|------|------|------|
| 套餐编码 | 是 | 唯一 |
| 套餐名称 | 是 | 例：新冠急救耗材套餐 |
| 套餐用途 | 是 | 应急储备/季节性采购/项目采购 |
| 包含商品 | 是 | 商品 + 标准数量 + 供应商 |
| 默认审批流 | 是 | 引用系统配置审批流 |
| 状态 | 是 | 启用/停用 |

> 采购订单创建时可一键引用采购套餐，自动展开为多行明细。

#### 8.2.10 中心库采购审批

中心库（SPD 中心仓）发起的采购订单审批节点，与原 5.1.6 金额分级审核规则结合：

| 条件 | 中心库审批人 | 时限 |
|------|--------------|------|
| 套餐采购、合同价 | 采购主管 | 即时 |
| 非合同价、金额 ≤ 5 万 | 采购主管 + 运营管理员 | 1 工作日 |
| 5 万 < 金额 ≤ 50 万 | 采购主管 + 财务 + 运营管理员 | 2 工作日 |
| 金额 > 50 万 | 采购主管 + 财务 + 院领导 | 3 工作日 |
| 紧急采购 | 中心库主任先批，事后补流程 | 24 小时内 |

#### 8.2.11 科室采购审批

科室直接发起的采购（非中心库统一采购，例如科室专项设备耗材）需独立审批节点。

| 条件 | 科室审批人 | 后置环节 |
|------|------------|----------|
| 金额 ≤ 5000 | 科室主任 | 中心库登记备案 |
| 5000 < 金额 ≤ 5 万 | 科室主任 + 运营管理员 | 中心库代采 |
| 金额 > 5 万 | 科室主任 + 院领导 | 进入中心库采购流程 |

#### 8.2.12 请货单管理

请货单（科室/二级库 → 中心库的物资请领单据，是 SPD 院内最常见的"请货"业务对象，区别于面向供应商的采购订单）。

**请货单字段：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 请货单号 | 自动 | QH + YYYYMMDD + 序号 |
| 请货库房 | 是 | 申请方（通常是科室二级库） |
| 目标库房 | 是 | 中心库 |
| 请货类型 | 是 | 常规 / 紧急 / 定数包补给 / 散货补给 |
| 请货明细 | 是 | 商品、申请量、单位、备注 |
| 期望到货 | 是 | - |
| 备注 | 否 | - |

**请货单审核：** 由中心库审核员根据库存、效期、批号情况确认是否满足，可整单/分行通过/驳回。

**请货单查询：** 支持按"申请库房 / 状态 / 日期范围 / 商品 / 申请人"多维筛选；支持导出 Excel；支持反查关联出库单/补货单/到货回单。

**科室申领单配送审核：** 中心库出库前对申领单 / 请货单进行配送复核（拣货完成后第二道把关）：
- 数量复核、批号一致性、效期校验、定数包完整性。
- 复核通过 → 进入「出库配送」节点；不通过 → 退回拣配重新作业。

#### 8.2.13 补货定时任务、缺货登记与来货匹配

补货任务由定时任务统一扫描生成，支持按配置生成“补货建议”或“正式补货任务”。紧急补货可由人工即时创建，不必等待定时任务。补货任务生成后不锁定具体库存，拣配扫描定数包标签或散货明细时才锁定库存。

**定时任务配置：**

| 配置项 | 说明 |
|--------|------|
| 执行频率 | 支持按医院、院区、库房、科室、商品分层配置 |
| 执行模式 | 生成建议 / 直接生成正式任务 |
| 扫描范围 | 科室、库房、商品分类、商品、定数包包族 |
| 生成策略 | 补到最小 / 补到目标 / 补到最大，支持配置 |
| 合并规则 | 默认合并同科室同商品或同定数包的未执行任务；已锁定、已拣配、已出库、在途任务不合并 |
| 时间窗 | 支持每日固定时间窗、工作日/节假日策略、手动触发 |
| 失败处理 | 失败重试、任务日志、执行状态和告警 |

**触发来源：**

- 安全量不足。
- 召回、隔离、盘亏、报损导致可用库存下降。
- 反消耗恢复导致补货任务需重算。
- 科室紧急补货申请。
- 缺货登记来货匹配。

**紧急补货规则：**

- 紧急补货优先占用未锁定库存，但不抢占普通补货中已锁定未出库的库存。
- 紧急补货禁止负库存；库存不足时转调拨、紧急采购或应急借用流程。
- 应急借用必须绑定真实实物来源，不能作为系统凭空生成库存的入口。

**库存不足与缺货登记：**

| 场景 | 处理规则 |
|------|----------|
| 库存足够 | 按建议数量生成补货建议或任务 |
| 库存不足 | 支持部分满足，并记录未满足缺口 |
| 未满足缺口 | 自动生成缺货登记，来货后优先处理缺货登记对应科室/单据 |
| 禁止事项 | 不允许通过负库存表达缺货，不允许生成超出可用库存的出库任务 |

缺货登记必须明确缺货类型，至少包括定数包、散货、高值、冷链。定数包缺货按包数满足，散货按明细数量满足，高值/冷链按 UID/唯一码或明细数量满足。缺货登记排序按紧急等级和登记时间处理，支持可配置权重并允许库管人工调整且留痕。缺货满足以科室签收或确认收到为准，中心库出库在途不视为满足；缺口完全满足后自动关闭，未满足时允许人工带原因关闭。

**来货匹配规则：**

- 收货上架完成后，系统支持自动匹配缺货登记，并按配置生成补货建议或正式补货任务。
- 定数管理商品来货后，优先按配置生成打包建议或打包任务，再用已生成的定数包满足缺货登记。
- 定数包缺货默认通过打包后补包满足；是否允许散货替代定数包缺货由前台配置控制。若配置允许，散货替代可满足临床缺货登记，但是否关闭定数安全量缺口按配置执行。
- 缺货登记关闭只看缺货数量是否被满足，不取决于来源是正常补货、调拨、紧急采购还是应急借用。

### 8.3 收货与上架管理

#### 8.3.1 功能概述

补充 SPD 院内"B2B 配送 → 多模式收货 → 收货打包 → 客户端标签上架 → 上架/收货查询"完整链条，覆盖低值/高值/散货/期初等场景。

#### 8.3.2 功能架构

```
收货与上架管理
├── B2B 配送单确认
├── 多模式收货
│   ├── 根据请货单的手工收货
│   ├── 散货收货
│   ├── 高值手工收货
│   └── 期初入库
├── 收货打包
├── 客户端标签上架
├── 打包及上架查询
└── 收货查询
    ├── 收货查询（新节点-总览）
    ├── 低值收货查询
    └── 高值收货查询
```

#### 8.3.3 B2B 配送单确认

供应商通过 B2B 平台推送配送单到 SPD，库管员确认是否到货：

| 字段 | 说明 |
|------|------|
| B2B 配送单号 | 来自外部 B2B 平台 |
| 关联采购订单 | 双向关联 |
| 供应商 | - |
| 预计到货时间 | - |
| 配送明细 | 商品、批号、效期、数量、单价 |
| 配送状态 | 已发车 / 已送达 / 已确认 / 拒收 |

**操作：**
- **整单确认**：扫描配送单号，全部确认收到 → 自动生成收货单。
- **逐项确认**：按行逐项核对（数量/批号/效期），允许差异处理。
- **拒收**：填写拒收原因，回写 B2B 平台。

**B2B 配送单都在此确认**——所有 B2B 推送的配送单统一在此节点确认，避免散落到各业务模块。

#### 8.3.4 根据请货单的手工收货

科室二级库收到中心库配送的物资后，按请货单逐项手工收货：

| 字段 | 说明 |
|------|------|
| 请货单号 | 扫码或选择 |
| 配送单号 | 自动带入（来自中心库出库） |
| 收货明细 | 商品、应收量、实收量、批号、效期、差异原因 |
| 差异处理 | 短少 / 破损 / 多送 → 反馈中心库 |

**收货流程：**
```
[扫描请货单/配送单] → [核对明细] → [扫描产品码（UDI/RFID）] → [录入实收量]
    → [差异行标注] → [整单提交] → [更新二级库库存] → [生成科室收货回单]
```

#### 8.3.5 散货收货

中心库或二级库对散货（已拆包/拆零的零散物资）的专项收货：

| 场景 | 处理 |
|------|------|
| 整件拆零后剩余散货 | 扫码识别散货标签 → 上架到散货引导货位 |
| 退回散货（科室退库） | 进入隔离区域，待审核 |
| 供应商按散货发货 | 直接收货并按引导货位上架 |

**散货标识：** 系统打印散货标签，含「商品编码、批号、剩余件数、引导货位」。

#### 8.3.6 高值手工收货

高值耗材必须按件扫码收货（一物一码），不允许批量录入：

| 必录字段 | 说明 |
|----------|------|
| UDI 码 | 必扫，关联 6.1 UDI 追溯 |
| 序列号 | 来自 UDI 解析 |
| 批号 | 来自 UDI 解析或人工录入 |
| 注册证号 | 自动校验 |
| 效期 | 自动校验，过期不允许入库 |
| 货位 | 高值位 |
| 收货人 | 自动带入 |

**与一般手工收货差异：** 强制单件扫码、强制货位、强制注册证有效性校验。

#### 8.3.7 期初入库

系统上线/年度盘整时使用，导入历史库存或盘点结果到指定库房：

| 字段 | 说明 |
|------|------|
| 期初单号 | QC + YYYYMMDD + 序号 |
| 期初类型 | 上线初始化 / 盘整调整 |
| 库房 | 是 |
| 明细 | 商品、批号、效期、数量、单价、货位 |
| 来源说明 | 必填，留痕 |

**控制规则：**
- 仅运营管理员有权操作；不进入采购/财务结算流程；只更新库存。
- 操作完成后自动锁定，禁止二次修改（如需调整须走盘点单）。
- 正式上线前必须完成数据初始化/迁移验收，验收未完成不得切换生产使用。
- 初始化范围至少包括商品目录、供应商、科室、库房、货位、角色权限、配置默认值、期初库存、定数模板/系数/安全量、高值耗材和冷链试剂 UID/唯一码。
- 期初库存必须按系统批次、生产批号、效期、批次单价、所有权、结算模式、库房/货位建立账面库存；不允许用负库存、虚拟入库或无实物来源数据兜底。
- 商品启用定数管理后，历史散货库存不自动转换为定数包；如需成包，必须由授权人员发起历史散货初始化打包，并遵守完整成包条件。
- 初始化差异必须通过盘盈盘亏、库存调整或业务审批流程处理，并保留差异原因、处理人、审批人、处理时间和附件。
- 期初库存和关键主数据必须由对应库房、科室、运营或财务责任人签字确认，形成上线验收附件。

#### 8.3.8 收货打包

到货验收后将散件按发科室/规则打包，便于后续配送：

| 操作 | 说明 |
|------|------|
| 按定数包打包 | 按定数包配方自动归集，打印定数包标签 |
| 按科室打包 | 按申领/请货目的科室打包 |
| 按手术单打包 | 关联手术单的耗材打成手术包 |
| 自由打包 | 临时手动打包 |

**打包标签字段：** 包号、目的库房/科室、商品清单、打包人、打包时间、二维码（含包号）。

#### 8.3.9 客户端标签上架

收货 / 打包完成后通过 PDA（手持客户端）扫码上架：

| 流程 | 说明 |
|------|------|
| 1. 扫描标签 | PDA 扫描产品码 / 包号 |
| 2. 推荐货位 | 系统按引导货位 / 容量算法推荐 |
| 3. 扫描货位 | PDA 扫描货位码确认 |
| 4. 提交上架 | 库存绑定货位，记录操作日志 |

**异常处理：** 货位类型不匹配（如冷链商品上架到普通货位）→ 报错阻断。

#### 8.3.10 打包及上架查询

| 维度 | 说明 |
|------|------|
| 按打包单查询 | 包号、打包时间、打包人、打包内容、状态（已上架/未上架） |
| 按上架记录查询 | 货位、商品、上架人、上架时间 |
| 按异常查询 | 上架失败、滞留未上架（超过 X 小时未上架报警） |

#### 8.3.11 收货查询新节点

统一收货查询入口，整合各类收货数据（采购收货 / B2B 收货 / 请货单收货 / 散货 / 高值 / 期初）：

| 查询条件 | 说明 |
|----------|------|
| 收货类型 | 多选 |
| 来源单据 | 采购单 / B2B 配送单 / 请货单 / 期初单 |
| 商品 / 供应商 / 批号 | 多维筛选 |
| 收货时间范围 | 必填 |
| 异常标记 | 是否有差异 |

#### 8.3.12 低值收货查询

聚焦低值耗材的收货明细，重点关注「批量批次、批号、效期」：

| 字段 | 说明 |
|------|------|
| 收货单号 | - |
| 商品 / 批号 / 效期 | - |
| 数量、单价、金额 | - |
| 验收人 | - |
| 入库货位 | - |
| 关联请货单 | - |

#### 8.3.13 高值收货查询

聚焦高值耗材，重点关注「单件溯源」：

| 字段 | 说明 |
|------|------|
| UDI 码 | 唯一标识 |
| 序列号 | - |
| 注册证号 / 注册证有效期 | - |
| 批号 / 效期 | - |
| 货位（高值位） | - |
| 当前状态 | 在库 / 已分配手术 / 已使用 |
| 追溯链 | 一键跳转 6.1 UDI 追溯链路 |

### 8.4 拣配出库与配送

#### 8.4.1 功能概述

补充 SPD 院内"拣配 → 散货打包出库转补货 → RFID 附码 → 出库配送 → 库房出库明细汇总 → 补货单查询"完整链条。

#### 8.4.2 功能架构

```
拣配出库与配送
├── 拣配作业
├── 出库配送
├── 申领单散货打包出库转补货节点
├── 预留低值出库 RFID 附码节点
├── 预留高值出库 RFID 附码节点
├── 对应库房出库
├── 库房出库明细
├── 库房出库汇总
└── 补货单查询
```

#### 8.4.3 拣配作业（拣配）

| 字段 | 说明 |
|------|------|
| 拣配单号 | JP + YYYYMMDD + 序号 |
| 来源单据 | 申领单 / 请货单 / 补货单 |
| 拣配模式 | 单单拣 / 批量拣 / 波次拣 |
| 拣配明细 | 商品、应拣量、批号优先（FIFO/近效期）、推荐货位 |
| 拣配人 | 自动带入 |
| 状态 | 待拣 / 拣配中 / 已拣 / 待复核 |

**PDA 拣配流程：** 扫描拣配单 → 系统按最优路径推荐 → 扫描货位 → 扫描商品 → 录入实拣量 → 提交完成。

#### 8.4.4 出库配送

拣配复核通过后进入出库配送：

| 字段 | 说明 |
|------|------|
| 配送单号 | PS + YYYYMMDD + 序号 |
| 关联拣配单/申领单 | - |
| 起点库房 / 目的库房 | - |
| 配送方式 | 院内物流 / 气动管道 / 人工配送 |
| 配送人 | - |
| 配送状态 | 待发出 / 配送中 / 已送达 / 已签收 / 异常 |
| 签收人 | 目的库房接收人 |
| GPS / 时间戳 | 院内移动设备记录 |

#### 8.4.5 申领单散货打包出库转补货节点

申领单中的散货物品在出库时，若该项已使其引导货位散货低于安全量 → 自动生成"散货补货单"反向请求中心库补货：

```
[申领出库扫码] → [扫描散货标签] → [出库执行] → [校验散货货位库存]
    ↓ 低于阈值
[自动生成散货补货单（草稿）] → [中心库审核] → [散货收货上架]
```

> 该节点是 SPD 院内闭环关键：申领出库即触发补货，避免散货空架。

#### 8.4.6 预留低值出库 RFID 附码节点

为后续"全院 RFID 化"预留入口（当前业务可关闭，仅占位）：

| 字段 | 说明 |
|------|------|
| 节点开关 | 系统配置项 `rfid.lowvalue.attach.enabled` |
| 触发时机 | 低值耗材出库前 |
| 操作 | 扫描产品码 → 关联 RFID 标签号 → 写入 RFID 数据库 |
| 用途 | 全院射频追溯（实时定位、自动盘点） |

#### 8.4.7 预留高值出库 RFID 附码节点

| 字段 | 说明 |
|------|------|
| 节点开关 | `rfid.highvalue.attach.enabled` |
| 触发时机 | 高值耗材出库前（已含 UDI，再附 RFID 用于手术室自动识别） |
| 操作 | 扫描 UDI → 写入 RFID → UDI ↔ RFID 映射 |
| 用途 | 手术室自动盘存、丢失防护、术后自动消耗 |

#### 8.4.8 对应库房出库

按"目的库房"维度统一执行出库：一次出库可能跨多张申领单/请货单，按库房聚合：

| 操作 | 说明 |
|------|------|
| 按库房聚合 | 同一目的库房的多张单据合并出库 |
| 按时间窗 | 在配送时间窗内的所有单据一次性出库 |
| 库存扣减 | 起点库房库存扣减、目的库房在途库存增加 |

#### 8.4.9 库房出库明细

| 查询字段 | 说明 |
|----------|------|
| 出库单号 / 配送单号 / 申领单号 / 请货单号 | 多维 |
| 起点库房 / 目的库房 | - |
| 商品、批号、数量、单价、金额 | - |
| 出库时间、出库人 | - |
| 关联手术 / 关联患者 | 高值耗材 |
| 散货 / 整件标识 | - |

#### 8.4.10 库房出库汇总

| 汇总维度 | 输出 |
|----------|------|
| 按库房 + 时间段 | 出库金额、件数、品种数 |
| 按商品分类 + 时间段 | 各类商品出库占比 |
| 按目的科室 + 时间段 | 科室耗用排名 |
| 按出库人 + 时间段 | 工作量统计 |

#### 8.4.11 补货单查询

补货单（中心库 → 二级库 / 散货货位的补充单据）查询：

| 字段 | 说明 |
|------|------|
| 补货单号 | BH + YYYYMMDD + 序号 |
| 补货类型 | 二级库补货 / 散货货位补货 / 定数包补货 |
| 来源 | 自动请补货单 / 手工补货 / 散货转补货 |
| 状态 | 草稿 / 已审核 / 已出库 / 已收货 / 已关闭 |
| 触发申领单 | 关联反查（散货转补货场景） |

### 8.5 库内作业管理

#### 8.5.1 功能概述

补充库内打包/拆包、定数标签状态、出入库查询、盘点单（含手持盘点）等节点。

#### 8.5.2 功能架构

```
库内作业管理
├── 库内打包
├── 打包查询
├── 拆包
├── 拆包查询
├── 定数标签状态查询
├── 耗材出入库查询
├── 新增盘点单
└── 手持盘点
```

#### 8.5.3 库内打包

中心库或二级库内部对在库物资进行二次打包（区别于 8.3.8 收货打包，库内打包面向已上架在库的库存）：

| 场景 | 说明 |
|------|------|
| 整件转散货后再打包 | 拆零后部分组合打包 |
| 临时手术包 | 即时为某手术配置组合包 |
| 跨货位归集 | 多货位库存合并到一个虚拟包号便于配送 |

| 字段 | 说明 |
|------|------|
| 打包单号 | DB + YYYYMMDD + 序号 |
| 包号 | 唯一二维码 |
| 包内明细 | 商品、批号、数量、来源货位 |
| 打包目的 | 申领出库 / 手术备货 / 临时归集 |
| 状态 | 已打包 / 已拆包 / 已出库 |

#### 8.5.4 打包查询

| 维度 | 说明 |
|------|------|
| 按包号 | 查看包内容、状态、流转记录 |
| 按打包人/时间 | 工作量统计 |
| 按关联单据 | 反查申领/手术单关联包 |
| 按状态 | 在库已打包 / 已出库 / 已拆包 |

#### 8.5.5 拆包

将"已打包且未发生消耗"状态的包拆解还原为单件库存（适用于：包整体未使用退回、临时归集后未配送、整件转散货等）。已发生消耗的定数包不得通过普通拆包直接还原库存，必须走定数包反消耗或退回重打包流程：

| 操作 | 说明 |
|------|------|
| 扫描包号 | PDA 扫描包号 |
| 拆包确认 | 系统列出包内明细 |
| 库存还原 | 包内每件物品按原批号回库到指定货位（散货回散货位） |
| 拆包原因 | 必填：未使用退回 / 临时拆零 / 包错重打 / 其他 |

#### 8.5.6 拆包查询

| 字段 | 说明 |
|------|------|
| 拆包单号 / 原包号 | - |
| 拆包人 / 拆包时间 | - |
| 拆包原因 | - |
| 还原货位 | 每件还原后的货位 |
| 关联手术 / 关联申领 | 用于反查 |

#### 8.5.7 定数标签状态查询

每个定数包打包后生成定数标签（条码/二维码），状态全程可追溯：

| 状态 | 说明 |
|------|------|
| 已打包 | 中心库刚打包完毕 |
| 在库 | 已上架到二级库 |
| 已发出 | 配送中 |
| 已签收 | 二级库已签收 |
| 待打印/待贴标 | 已生成定数包库存但标签未打印或未贴标 |
| 已使用 | 已被消耗（拆开使用） |
| 已拆包 | 临时拆解未使用 |
| 已退库 | 退回中心库 |
| 异常 | 丢失 / 损坏 / 过期 |

| 查询字段 | 说明 |
|----------|------|
| 定数标签号 | 主键 |
| 包名 / 适用科室 | - |
| 当前状态 / 状态变更时间 | - |
| 全链路追溯 | 显示每次状态变更记录 |

##### 定数包全生命周期与反消耗规则

定数包采用"集团模板 + 院区/科室实例"模型。集团维护标准包族、包模板和商品明细，院区/科室维护适用范围、安全包数、补货阈值、任务评分权重等差异化配置。

**准入边界：**

定数包仅用于非高值耗材和非冷链试剂，不承接包内明细的最高风险属性。高值耗材和冷链试剂根据验收入库自动生成的唯一码或填写的 UDI 进行独立管控，不纳入定数包整包消耗模型。定数包模板发布和新打包时，必须校验包内商品主数据的 `是否定数管理` 字段；只有 `是否定数管理 = 是` 的商品可作为包内明细。

**模板与快照：**

| 规则 | 说明 |
|------|------|
| 包族编码 | 表示同一业务含义的定数包，如骨科手术包 |
| 模板版本 | 包模板发布后生成版本号，禁止覆盖旧版本 |
| 标签快照 | 打包生成定数标签时固化当时的包内明细、包装数量、批号、效期、价格等快照 |
| 历史追溯 | 历史定数包永远按生成标签时的快照进行消耗、反消耗、红冲和追溯 |
| 版本替换 | 新版本发布后默认自然替换；旧版包继续可用，消耗完后只补当前启用版本 |
| 安全库存 | 默认按包族汇总可用包数，自动补货按当前启用版本生成 |

**定数包定义与成包条件：**

定数包不是多品规组合包，而是同一商品的包装化库存单位。成包商品必须满足同商品编码/商品目录主键、同规格、同型号、同厂家、同供应商、同生产批号、同效期、同批次单价、同所有权、同结算模式、同库房。定数包不要求同系统批次，可在上述条件一致时跨系统批次打包；跨系统批次时必须保存来源系统批次、来源数量、货位、入库单号和批次单价明细。禁止跨库房打包，允许同库房不同货位合并打包。

定数包标签号规则为 `D + 年月日 + 后六位序列号`。打包扣减按同效期下先进先出 FIFO 执行，并记录实际来源系统批次明细。单价不同不允许混包；所有权或结算模式不同不允许混包；买断、寄售、实销实结等不同库存不得混包。

**成包模式与余数处理：**

| 场景 | 规则 |
|------|------|
| 验收入库自动成包 | 验收审核通过后按验收单数据、定数包系数和完整成包条件自动生成定数包库存和标签 |
| 待确认打包任务 | 系统生成待确认任务并默认预占来源散货，人工确认后扣减散货并生成定数包 |
| 模式配置 | 自动成包 / 生成待确认打包任务均支持前台配置 |
| 不能整除 | 按 `floor(可打包数量 / 定数包系数)` 生成整包，余数保留为散货库存 |
| 余数合并 | 余数散货可与后续来货合并打包，但必须满足完整成包条件 |
| 历史散货初始化打包 | 商品启用定数管理后，历史散货需人工发起初始化打包，必须遵守完整成包条件并保存来源系统批次明细，不产生消耗和结算 |

待确认打包任务确认前不计入定数包安全量；预占散货从可用库存中扣除，只计入库存总量，不计入定数包安全量。任务取消、超时释放或重算时释放预占散货。未确认打包任务必须具备超时提醒/释放机制，是否自动释放支持配置。预占散货发生调价、盘亏、召回或隔离时，必须校验或重算相关未确认打包任务。

定数包系数变更需要弹窗提示影响范围，是否审批/复核按风险配置。系数变更只影响后续打包和未确认打包任务；已确认成包不自动变更。预计重算只作用于未确认打包任务，并按最新系数对可用散货重新分组计算包数和余数。提示需展示原系数、新系数、影响任务数、原预计包数、新预计包数、原余数、新余数和预计来源库存变化。

**标签打印与补打：**

| 场景 | 规则 |
|------|------|
| 自动成包打印失败 | 保留定数包库存为待打印/待贴标状态，不回滚库存 |
| 待打印/待贴标 | 计入可用库存和安全量，可被补货任务选中，但出库前必须完成标签打印/贴标 |
| 原标签补打 | 支持勾选原标签打印，不生成新标签号，必须记录打印次数和打印人 |
| 补打原因 | 必选：破损、丢失、字体不清晰、打印模板有变动、无法扫码等 |
| 禁止补打 | 已消耗、已作废、已报损标签默认不允许补打 |
| 召回/隔离补打 | 允许直接补打原标签，但召回/隔离状态不允许消耗 |

补打不增加库存、不改变包状态、不改变来源批次和结算。批号/效期调整后，已打包未消耗定数包默认不强制重打标签，只留痕。

**数量与展示口径：**

| 字段 | 说明 |
|------|------|
| 定数包数量 | 本次发生的包数，如 1 包或红冲 -1 包 |
| 包装数量 | 每个定数包内某个明细的标准数量 |
| 明细数量 | 定数包数量 × 包装数量 |
| 结算金额 | 永远以明细数量、明细单价和明细金额为准 |
| 院内查询 | 同表显示定数包数量、包装数量、明细数量，用字段区分，无需展开 |
| 供应商结算 | 只推送商品明细，不出现定数包汇总行 |

**事件流水：**

定数包账务真相以不可变事件流水为准，当前状态只作为查询快照。已产生事件不得编辑或删除，纠错只能追加补偿事件。

| 事件类型 | 说明 |
|----------|------|
| 打包 | 从散货库存扣减明细，生成定数标签和包内快照 |
| 出库/配送 | 中心库向科室二级库配送 |
| 科室签收 | 科室确认收到定数包 |
| 消耗 | 拆包即整包消耗，生成正向消耗事件和明细结算数据 |
| 反消耗 | 仅用于误操作且实物完整的场景，生成反向事件 |
| 重新消耗 | 若反消耗错误，不删除反消耗记录，只追加新的正向消耗事件 |
| 退回/重打包 | 对不满足原包恢复条件的实物进行退回、拆解、重新打包并生成新标签 |
| 作废/召回 | 标签损坏、质量召回、报损等场景 |

每条事件至少记录 eventId、eventType、sourceEventId、packageLabelId、operatorId、occurredAt、businessPeriod、accountingPeriod、reasonCode、审批记录和外部推送回执。正式统计、报表、大屏和结算均以 accountingPeriod（入账期/结算月）为准；businessPeriod 仅用于追溯、审计和异常分析，不回写历史月份指标。

**反消耗与红冲：**

| 场景 | 处理规则 |
|------|----------|
| 日清前误消耗 | 可发起反消耗，记录原因和操作日志 |
| 日清后误消耗 | 进入科室审批，审批表单必须包含实物完整核验字段 |
| 已结算误消耗 | 允许反消耗；不修改历史结算单，生成次月红冲结算数据 |
| 反消耗后恢复 | 科室审批通过后复活原定数包标签并恢复原科室定数包库存 |
| 反消耗错误 | 不删除反消耗记录，只能追加新的正向消耗和补结算记录 |

科室审批必须核验原定数标签是否存在且可扫码、包装/封签是否完整、包内明细是否未替换、是否仍在原科室库位、是否涉及高值/冷链/无菌/植入类物资，并填写反消耗原因、审批人工号或电子签名。

反消耗后，一正一反记录均保留。原消耗记录不物理删除；系统通过事件链关联原消耗、反消耗、重新消耗、红冲结算明细和供应商推送明细。

已结算定数包反消耗审批通过后，必须在同一业务闭环内恢复库存并生成财务红冲结算数据，不设置“库存已恢复但财务待处理”的正常业务状态。若因系统异常导致红冲数据生成失败，应告警并重试，不作为可长期停留的业务流程节点。召回或隔离包发生反消耗时，复活后的原标签仍保持召回/隔离状态，不直接恢复为可用；召回、隔离、待复核标签均不计入安全量。

**质量召回与补货：**

| 规则 | 说明 |
|------|------|
| 召回命中 | 质量事件命中包内任一明细时，原定数包整包停用 |
| 标签处理 | 停用后如需再形成定数包，必须重新打包并生成新标签，不得复用旧标签 |
| 安全库存 | 召回/停用包不计入科室安全库存 |
| 补货动作 | 召回或停用导致低于安全库存时，自动生成补货任务，人工复核后出库或打包 |
| 任务优先级 | 召回补货与普通请领平级，统一进入任务队列 |
| 队列排序 | 平级任务按综合评分排序，而非纯创建时间排序 |

综合评分由院区配置，集团提供默认模板。评分维度包括期望到达时间、缺口比例、科室等级、风险标签等；权重总和必须为 100%。任务生成时固化公式版本和分数明细。权重修改默认不自动重排已有任务，管理员可手动重算待处理任务；已完成任务和进行中任务不重算。

**解包回散货与作废重组：**

为避免术语混淆，系统区分“拆包消耗”和“解包回散货”。拆包消耗是科室使用场景，拆包即整包消耗并生成消耗及结算事实；解包回散货是库管维护场景，用于未消耗定数包因调价、标签错误、包装破损、系数配置错误、自动成包错误等原因作废回散货，不产生消耗、不产生结算、不计入盘点差异。

| 规则 | 说明 |
|------|------|
| 操作权限 | 是否允许中心库、科室现场或指定角色操作，由菜单/按钮权限配置控制 |
| 位置归属 | 解包后散货位置以当前实物所在库房/货位为准，不自动回原来源货位 |
| 库存影响 | 定数包库存 -1，来源系统批次散货数量 + 包装数量，库存总价值不变 |
| 标签处理 | 原包标签作废；重新组包必须生成新定数包标签，不复用原标签 |
| 散货消耗 | 解包后的散货是否允许直接消耗，按商品、科室、角色和业务策略配置控制 |
| 留痕要求 | 记录原包、来源批次、解包原因、操作人、时间、当前库房/货位和新包关联 |

未消耗定数包内任一来源系统批次调价时，必须保证调整后包内来源批次单价仍一致；否则不允许直接调价生效，系统弹窗提示需先解包回散货/作废重组。自动成包错误支持通过解包成散货/作废重组纠正，不允许直接修改包标签号、商品、包装数量、来源系统批次明细、批号、效期、单价、所有权或结算模式等核心内容。

**配送、拒收与纠错：**

- 定数包出库后签收前处于在途状态；科室签收后才计入科室库存和安全量。
- 科室正常拒收时自动回中心库库存；错送、科室不需要、配送取消可回中心库可用；破损、标签不清、近效期争议、召回/停用、温控异常、数量差异等异常拒收自动回中心库但状态为待复核或隔离，不计可用。
- 错误签收通过纠错调拨或退回重配处理，保留原签收记录，不直接删除。
- 定数包补货任务生成时不锁定具体包，拣配扫描包标签时才锁定；拣配取消或锁定超时释放包库存。

**幂等、并发与 PDA 离线：**

所有会改变定数包状态的接口必须强制携带幂等键。PDA/前端生成 clientRequestId，服务端落库为 idempotencyKey；同一幂等键重复请求返回首次成功结果，不再新增事件。消耗、反消耗、重新消耗、召回停用、补货任务生成等动作均必须满足幂等要求。

同一个 packageLabelId 的状态变更采用版本号或事件序号控制并发。状态变更请求必须携带 expectedVersion；服务端校验当前版本一致后才允许追加事件并更新快照。版本冲突请求不得直接落账，应返回当前最新状态，由操作端重新处理。

PDA 离线时只允许扫码/记录类采集并保存在本机待上传队列，不改变服务端正式库存和结算数据。允许离线暂存的业务包括盘点扫码、定数包消耗扫码、科室收货扫码、上架/拣货扫码、温度人工记录、异常照片和备注。离线期间禁止审批、反消耗、召回/停用、结算/对账/红冲、主数据维护、定数包模板修改、跨库调拨确认、供应商推送等强一致操作。

PDA 端必须持续提醒当前设备已离线，并将离线采集数据显示为"本机待上传"状态；同一设备内应防止重复扫码。上传成功前不得显示为正式入账，不得影响 PC 端库存、科室库存、安全库存和结算数据。恢复网络后自动上传；上传成功后状态变为已入账，上传失败按现有业务错误提示处理，不建设独立离线异常处理台。

PC 端根据 PDA 离线时间进行提醒和业务数据展示。提醒内容至少包括设备编号、所属科室/库房、离线时长、最后在线时间、最后操作人等。后补上传数据按服务端处理时间确定 accountingPeriod，并打"离线补传"标识；离线发生时间仅用于查询和审计，不回写历史月份指标。

#### 8.5.8 耗材出入库查询

整合所有出入库流水（含期初、采购入、调拨入、退货入、申领出、消耗出、调拨出、退供应商出、盘亏盘盈等）：

| 查询字段 | 说明 |
|----------|------|
| 业务类型 | 多选 |
| 单据号 | 模糊搜索 |
| 商品 / 批号 / 效期 | - |
| 库房 / 货位 | - |
| 时间范围 | 必填 |
| 经办人 | - |
| 出入库金额 | 区间 |

支持导出 Excel、生成审计专用流水。

#### 8.5.9 新增盘点单

| 字段 | 必填 | 说明 |
|------|------|------|
| 盘点单号 | 自动 | PD + YYYYMMDD + 序号 |
| 盘点类型 | 是 | 全盘 / 抽盘 / 动态盘 / 专项盘 |
| 盘点库房 | 是 | 多选 |
| 盘点范围 | 是 | 全部 / 指定货位 / 指定商品分类 / 指定批号 |
| 盘点人员 | 是 | 多选，二人以上盲盘 |
| 计划盘点时间 | 是 | - |
| 是否封库 | 是 | 是：盘点期间禁止业务；否：动态盘 |
| 备注 | 否 | - |

**盘点流程：**
```
[新增盘点单] → [系统冻结对应库存（封库时）] → [生成盘点任务]
    → [手持盘点 或 PC 端盘点录入] → [差异分析] → [审核]
    → [生成盘盈/盘亏单调整库存] → [关闭盘点单]
```

#### 8.5.10 手持盘点

PDA 端的盘点作业：

| 操作 | 说明 |
|------|------|
| 接收盘点任务 | PDA 拉取分配给当前用户的盘点任务 |
| 扫货位 | 扫描货位码进入对应清单 |
| 扫商品/UDI | 累计件数 |
| 录入实盘量 | 与系统量对比，差异自动标红 |
| 离线模式 | 弱网环境下本地暂存，恢复后同步 |
| 拍照取证 | 异常情况强制拍照（破损/过期） |
| 提交盘点结果 | 二次复核后提交 |

### 8.6 科室业务管理

#### 8.6.1 功能概述

补充科室端 SPD 业务节点：科室收货（已在 5.3）之后的消耗、日清确认、使用反馈、不良事件上报、消耗报表汇总。

#### 8.6.2 功能架构

```
科室业务管理
├── 科室收货（详见 5.3.2）
├── 科室消耗
├── 科室日清确认
├── 耗材使用意见反馈
├── 不良事件上报
├── 科室消耗报表
└── 科室消耗汇总
```

#### 8.6.3 科室消耗

科室二级库消耗扣减节点（区别于先前申领出库）：

| 消耗模式 | 说明 |
|----------|------|
| 实领实消 | 申领即消耗，扣减二级库库存 |
| 实领实结 | 申领消耗按月统一结算 |
| 实销实结 | 实际使用扫码后才扣减并计费（先收费后消耗手术单专用） |
| 定数包消耗 | 拆开使用即整包消耗 |

| 字段 | 说明 |
|------|------|
| 消耗单号 | XH + YYYYMMDD + 序号 |
| 消耗科室 | 自动带入 |
| 消耗类型 | 申领消耗 / 手术消耗 / 损耗 / 转赠 |
| 关联单据 | 申领单 / 手术单 |
| 患者信息 | 高值耗材必填 |
| 消耗明细 | 商品、批号、数量、消耗人、消耗时间 |
| 状态 | 待确认 / 已确认 / 已结算 / 已反消耗 |

#### 8.6.4 科室日清确认

每日工作结束时科室对当日所有消耗、收货、退库、剩余库存进行盘清确认：

| 操作 | 说明 |
|------|------|
| 拉取当日数据 | 系统自动汇总当日入/出/消耗/盘点 |
| 现场盘点 | PDA 扫描定数包 / 散货数量 |
| 差异核对 | 与系统理论库存比对 |
| 异常上报 | 差异行强制说明原因 |
| 日清提交 | 科室主任电子签字 |
| 日清状态 | 未日清 / 已日清 / 异常 |

> 未日清的科室次日不允许新增申领，强制闭环。

#### 8.6.5 耗材使用意见反馈

| 字段 | 必填 | 说明 |
|------|------|------|
| 反馈编号 | 自动 | YJ + YYYYMMDD + 序号 |
| 反馈科室 / 人员 | 是 | - |
| 关联耗材 | 是 | 商品 / 批号 / 厂家 |
| 反馈类型 | 是 | 质量问题 / 使用体验 / 包装问题 / 改进建议 |
| 严重等级 | 是 | 一般 / 重要 / 紧急 |
| 描述 | 是 | 文本 + 图片附件 |
| 处理状态 | - | 待处理 / 已转厂家 / 已回复 / 已关闭 |

> 严重等级"紧急"自动同步至质控人员、运营管理员。

#### 8.6.6 不良事件上报

满足国家《医疗器械不良事件监测和再评价管理办法》要求：

| 字段 | 必填 | 说明 |
|------|------|------|
| 事件编号 | 自动 | BL + YYYYMMDD + 序号 |
| 患者信息 | 是 | 姓名、住院号、身份证（脱敏） |
| 涉事耗材 | 是 | 商品、UDI、批号、注册证号、生产厂家、供应商 |
| 事件类型 | 是 | 死亡 / 严重伤害 / 可能严重伤害 |
| 发生时间 | 是 | - |
| 事件描述 | 是 | 详细经过 |
| 已采取措施 | 是 | - |
| 上报途径 | 是 | 院内 / 已上报国家不良事件监测中心 |
| 附件 | 否 | 病历、照片 |

**上报流程：**
```
[科室上报] → [科室主任审核] → [医务部审核] → [质控人员审核]
    → [上报国家监测平台 / 内部留档] → [跟踪处理]
```

#### 8.6.7 科室消耗报表

| 报表 | 说明 |
|------|------|
| 科室日消耗 | 当日各品种消耗量与金额 |
| 科室月消耗 | 月度消耗趋势 |
| 科室耗材排名 | TopN 高耗品种 |
| 患者人均消耗 | 高值耗材专用 |
| 同比 / 环比 | 与上期对比 |

#### 8.6.8 科室消耗汇总

跨科室、跨时间维度的消耗汇总：

| 维度 | 输出 |
|------|------|
| 全院按科室 | 各科室消耗金额、占比 |
| 按耗材分类 | 各类耗材科室消耗分布 |
| 按医生 | 医生使用耗材排行（高值） |
| 按手术类型 | 不同手术耗材成本 |

### 8.7 退回业务管理

#### 8.7.1 功能概述

补充科室退库、负数账反消耗、散货库存隔离、隔离退供应商等节点。

#### 8.7.2 功能架构

```
退回业务管理
├── 科室退库申请
├── 科室退库审核
├── 负数账反消耗
├── 散货库存隔离
├── 隔离退供应商申请
└── 隔离退供应商审核
```

#### 8.7.3 科室退库申请

科室二级库 → 中心库的退库申请（区别于"采购退货：库房 → 供应商"）：

| 字段 | 必填 | 说明 |
|------|------|------|
| 退库单号 | 自动 | TK + YYYYMMDD + 序号 |
| 退库科室 | 是 | 自动带入 |
| 退库类型 | 是 | 多余退库 / 临近效期退库 / 错领退库 / 损坏退库 / 不良事件退库 |
| 退库明细 | 是 | 商品、批号、数量、原申领单号 |
| 退库原因 | 是 | 文本 |
| 附件 | 否 | 照片 / 单据 |

#### 8.7.4 科室退库审核

中心库审核员审核退库申请：

| 审核项 | 规则 |
|--------|------|
| 商品状态 | 完整可二次使用 → 直接退库；破损/过效 → 走报损 |
| 批号一致性 | 批号必须与原申领批号一致 |
| 时效性 | 退库申请须在领用后 N 天内（系统配置） |
| 数量合理性 | 不能超过原申领量 |

| 审核结果 | 后置动作 |
|----------|----------|
| 通过-入库 | 退回中心库可用库存 |
| 通过-隔离 | 退入隔离库存（待进一步处理） |
| 驳回 | 退回科室自行处理 |

#### 8.7.5 负数账反消耗

针对"已消耗但实际未使用"的散货、手术材料或按明细结算物资进行反向冲销（科室申领消耗后退回，需冲销原消耗记录）。定数包误操作且实物完整恢复原包的反消耗不走本节行级部分反冲规则，按 8.5.7 "定数包全生命周期与反消耗规则"处理。

| 场景 | 说明 |
|------|------|
| 手术取消 | 消耗已记账但手术未做 |
| 错误消耗 | 误扫导致重复消耗 |
| 退货回冲 | 退回中心库后需冲销原消耗 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 反消耗单号 | 自动 | FX + YYYYMMDD + 序号 |
| 关联消耗单 | 是 | 必关联原消耗单 |
| 反冲明细 | 是 | 行级反冲，可全行/部分数量 |
| 反冲原因 | 是 | 必填 |
| 关联退库单 | 否 | 与 8.7.3 退库单联动 |
| 财务处理 | 自动 | 自动生成红字凭证 |

**审批：** 反消耗对账期已结算的消耗须经财务复核。

**与定数包反消耗的区别：**

| 类型 | 适用对象 | 数量口径 | 库存恢复 | 结算处理 |
|------|----------|----------|----------|----------|
| 散货负数账反消耗 | 散货、手术材料、可明细退回物资 | 行级全量/部分数量 | 退回可用或隔离库存 | 已结算走财务复核/红字凭证 |
| 定数包反消耗恢复 | 误操作且实物完整的定数包 | 整包一正一反事件 | 科室审批后恢复原科室定数包库存 | 已结算生成次月红冲明细，不修改历史结算单 |

#### 8.7.6 散货库存隔离

| 触发条件 | 说明 |
|----------|------|
| 退回散货 | 8.7.4 审核选择"通过-隔离" |
| 厂家通知召回 | 同批号自动转入隔离 |
| 不良事件涉及 | 同批号同 UDI 自动隔离 |
| 临近过期 | 系统配置阈值内自动隔离 |

| 操作 | 说明 |
|------|------|
| 库存标识 | 隔离库存独立显示，不参与可用库存 |
| 隔离原因 | 必录 |
| 隔离货位 | 物理隔离位 |
| 处理方向 | 销毁 / 退供应商 / 解除隔离 |

#### 8.7.7 隔离退供应商申请

| 字段 | 必填 | 说明 |
|------|------|------|
| 申请单号 | 自动 | LF + YYYYMMDD + 序号 |
| 退货供应商 | 是 | 自动带入 |
| 退货原因 | 是 | 召回 / 不良事件 / 质量问题 / 临近效期 |
| 退货明细 | 是 | 隔离库存中的批号/数量/UDI |
| 附件 | 否 | 召回函、检测报告 |

#### 8.7.8 隔离退供应商审核

| 节点 | 审批人 |
|------|--------|
| 初审 | 库管主任 |
| 复审 | 质控人员（关键质量问题） |
| 终审 | 运营管理员 |

审核通过后：库存自隔离区出库 → 生成退货出库单 → 财务发起退货红字结算 → 同步通知供应商。

### 8.8 手术临床管理

#### 8.8.1 功能概述

补充手术全流程节点：开始/取消手术、领出/归还手术材料、寄售/跟台归集、临时单合并、手术单确认、术后调整、未确认查询、耗用汇总、先收费后消耗确认。

#### 8.8.2 功能架构

```
手术临床管理
├── 手术单管理
│   ├── 开始/取消手术
│   ├── 手术单确认
│   ├── 手术单查询
│   └── 先收费后消耗方式手术单确认
├── 手术材料管理
│   ├── 领出手术材料
│   └── 归还手术材料
├── 寄售/跟台归集
├── 临时手术单向接口手术单合并
├── 术后处理
│   ├── 术后调整预留节点
│   └── 手术未确认耗材查询
└── 手术耗用汇总
```

#### 8.8.3 开始 / 取消手术

| 操作 | 说明 |
|------|------|
| 开始手术 | 扫描手术单/患者腕带 → 状态：进行中；锁定关联耗材库存 |
| 取消手术 | 状态：已取消；释放锁定耗材；如已领出材料 → 触发归还流程 |
| 暂停 | 状态：暂停（手术中断），保留锁定 |

#### 8.8.4 领出手术材料

| 字段 | 说明 |
|------|------|
| 领料单号 | LL + YYYYMMDD + 序号 |
| 关联手术单 | 是 |
| 领料类型 | 计划领料 / 临时加领 |
| 领料明细 | UDI 扫码（高值）/ 批量（低值）/ 手术包 |
| 领料人 | 巡回护士 |
| 状态 | 已领出 / 已归还 / 已使用 |

#### 8.8.5 归还手术材料

| 操作 | 说明 |
|------|------|
| 扫码归还 | 扫描 UDI / 包号 → 归还到手术包待清点 |
| 部分归还 | 一张领料单可分多次归还 |
| 归还原因 | 未使用 / 损坏 / 错领 |
| 库存变化 | 完整可用 → 回手术备货库；损坏 → 隔离 |

#### 8.8.6 寄售 / 跟台归集

寄售 / 跟台耗材（供应商提供，使用后再结算）的归集管理：

| 业务对象 | 说明 |
|----------|------|
| 寄售耗材 | 供应商寄存在医院的耗材，使用后才入采购 |
| 跟台耗材 | 厂家代表跟台带来的耗材 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 归集单号 | 自动 | JX + YYYYMMDD + 序号 |
| 关联手术单 | 是 | - |
| 寄售/跟台类型 | 是 | 寄售 / 跟台 |
| 供应商 | 是 | - |
| 厂家代表 | 跟台必填 | 入院登记 |
| 耗材明细 | 是 | UDI、批号、数量、规格 |
| 状态 | - | 已带入 / 已使用 / 已带出 / 已转采购 |

**业务流程：**
```
[术前供应商带入耗材登记] → [手术中扫码使用] → [术后清点]
    → [已用部分→自动转采购入库+消耗] → [未用部分→带出登记]
    → [生成应付/应收明细]
```

#### 8.8.7 临时手术单向接口手术单合并

来自 HIS 的"临时手术单"（手术中临时安排）与 SPD 接口推送的"标准手术单"合并：

| 场景 | 处理 |
|------|------|
| HIS 推送临时手术 | SPD 自动建立临时手术单（标记为 TEMP） |
| 转为正式 | 接口手术单到达后，按患者ID + 手术时间匹配 |
| 自动合并 | 系统提示匹配项，运营/手术室确认后合并 |
| 合并后处理 | 临时单的耗材使用自动归并到正式单 |

| 字段 | 说明 |
|------|------|
| 临时单号 | TEMP + YYYYMMDD + 序号 |
| 接口手术单号 | 来自 HIS |
| 合并状态 | 待匹配 / 已匹配 / 已合并 / 已驳回 |
| 操作日志 | 记录合并人、时间 |

#### 8.8.8 手术单确认

手术结束后对手术单耗材使用做最终确认：

| 字段 | 说明 |
|------|------|
| 手术单号 | - |
| 实际使用耗材 | UDI 扫码记录 |
| 未用归还 | 自动从领料中扣除 |
| 患者关联 | 强制 |
| 主刀医生确认 | 电子签字 |
| 巡回护士复核 | 电子签字 |
| 计费同步 HIS | 自动 |

确认后状态变为"已确认"，触发：消耗扣减 + HIS 计费 + UDI 追溯写入。

#### 8.8.9 手术单查询

| 查询字段 | 说明 |
|----------|------|
| 手术单号 / 患者 / 主刀医生 / 手术名称 | 多维 |
| 手术日期范围 | 必填 |
| 状态 | 待手术 / 进行中 / 已完成 / 已确认 / 已取消 |
| 耗材使用情况 | 已用品类、金额 |
| 是否含高值 / 寄售 / 跟台 | - |
| 是否已确认 | - |

#### 8.8.10 术后调整预留节点

预留接口，用于术后修正手术单耗材数据（医保审核驳回、患者投诉、错扫等）：

| 操作 | 说明 |
|------|------|
| 调整类型 | 数量增减 / 商品增删 / 替换批号 / 退回未用 |
| 审批 | 主刀医生 + 运营管理员 |
| 后置 | 自动联动负数账反消耗 + HIS 重新计费 |
| 留痕 | 强制留痕，永久不可删除 |

#### 8.8.11 手术未确认耗材查询

定时巡查"手术已结束但耗材未确认"的异常：

| 查询字段 | 说明 |
|----------|------|
| 手术结束时间 | 距今 ≥ N 小时（系统配置默认 4 小时） |
| 未确认明细 | 已领出但未扫码确认的 UDI |
| 责任人 | 巡回护士 / 主刀医生 |
| 提醒方式 | 系统消息 / 短信 |

#### 8.8.12 手术耗用汇总

| 维度 | 输出 |
|------|------|
| 按手术类型 | 各类手术平均耗材成本 |
| 按主刀医生 | 医生耗材消耗排行 |
| 按科室 | 科室手术耗材消耗 |
| 按时间段 | 月/季/年趋势 |
| 高值耗材占比 | 高值 / 低值耗材消耗结构 |

#### 8.8.13 先收费后消耗方式手术单确认

医保审核要求"先收费、后消耗"的手术单（区别于一般"实领实消"）：

| 流程 | 说明 |
|------|------|
| 1. 计划领料 | 按手术单计划领出，库存「锁定」但不消耗 |
| 2. 推送 HIS 收费 | 按计划耗材推送 HIS 自动计费 |
| 3. 手术中使用 | UDI 扫码记录实际使用 |
| 4. 术后确认 | 实际使用 = 已收费 → 直接消耗；< 已收费 → 触发负数账反消耗+退费；> 已收费 → 补费再消耗 |
| 5. 消耗扣减 | 仅按实际使用量扣减库存 |

> 该确认节点必须在医保规则下闭环，差异行强制人工确认。

### 8.9 结算与发票扩展

#### 8.9.1 功能概述

补充消耗结算单、发票节点、付款申请节点等。

#### 8.9.2 功能架构

```
结算与发票扩展
├── 消耗结算单
│   ├── 生成消耗结算单
│   ├── 消耗结算单审核
│   └── 消耗结算单查询
├── 发票管理（新发票节点）
│   ├── 新增发票
│   ├── 发票认证
│   ├── 发票匹配
│   └── 发票冲红
└── 付款管理（付款申请节点）
    ├── 付款申请
    ├── 付款审批
    └── 付款执行
```

#### 8.9.3 生成消耗结算单

实销实结模式（按科室实际消耗结算给供应商）：

| 字段 | 必填 | 说明 |
|------|------|------|
| 消耗结算单号 | 自动 | XS + YYYYMMDD + 序号 |
| 供应商 | 是 | - |
| 结算周期 | 是 | 月/季 |
| 结算范围 | 是 | 库房消耗 / 手术消耗 / 全部 |
| 消耗明细 | 自动 | 抓取期间内消耗记录（含批号、数量、单价） |
| 应结金额 | 自动 | Σ(数量 × 单价) |
| 含税金额 | 自动 | - |
| 关联反消耗 | 自动 | 自动扣减期内已发生的负数账反消耗 |
| 状态 | - | 草稿 / 待审核 / 已审核 / 已发供应商 / 已确认 / 已开票 / 已付款 |

**生成方式：**
- **自动生成**：定时任务按结算周期触发，按消耗记录生成。
- **手动生成**：财务选择"消耗范围 + 时间段 + 供应商"手动生成。
- **混合**：自动生成草稿 → 财务复核后发出。

**结算取价规则：**

- 普通耗材产生消耗数据时，即固化消耗批次单价并形成供应商应结算事实；月度结算单只是汇总确认载体，不是价格可直接变更边界。
- 定数包拆包整包消耗时，按包内来源系统批次明细生成消耗明细，并固化对应批次单价。
- 验收入库即结算的商品，验收入库后即形成结算事实，后续退货/冲销按原验收入库结算单价反向处理。
- 高值耗材绑定患者并计费后形成计费事实，必须先走计费冲销/退费，再处理 UID 库存状态。
- 反消耗负向记录按原正向消耗记录的单价和金额冲回，不按当前批次单价。
- 批次单价调整不覆盖已消耗、已结算、已计费原单价；已产生事实的数据通过补差、红冲或财务调整处理。
- 采购订单价格只作为订单分析价格。订单完成按数量履约为主，订单价与验收入库批次价差异只进入院内价差记录、复核和分析，不阻断订单关闭，不强制供应商单独确认。

#### 8.9.4 新的发票节点

供应商基于消耗结算单或采购入库单开具发票，财务在 SPD 内做发票登记、认证、匹配：

| 字段 | 必填 | 说明 |
|------|------|------|
| 发票编号 | 是 | 唯一 |
| 发票代码 | 是 | - |
| 发票类型 | 是 | 增值税专票 / 普票 / 数电票 |
| 开票日期 | 是 | - |
| 供应商 | 是 | - |
| 关联结算单 | 是 | 多张结算单可对应一张发票 |
| 价税合计 | 是 | 与结算单金额一致 |
| 税额 | 是 | - |
| 认证状态 | 自动 | 已认证 / 待认证 / 认证失败（对接税务） |
| 匹配状态 | 自动 | 完全匹配 / 金额差异 / 数量差异 / 未匹配 |
| 附件 | 否 | 发票影像（PDF/JPG） |

**发票冲红：** 已认证发票如需作废，必须开具红字发票冲销，原发票与红字发票均纳入档案。

#### 8.9.5 发票申请付款节点

财务基于已认证、已匹配的发票发起付款申请：

| 字段 | 必填 | 说明 |
|------|------|------|
| 付款申请单号 | 自动 | FK + YYYYMMDD + 序号 |
| 关联发票 | 是 | 一张或多张发票 |
| 申请付款金额 | 是 | 默认 = 发票应付金额 - 质保金 |
| 质保金扣留 | 自动 | 按合同条款 |
| 申请付款日期 | 是 | - |
| 付款方式 | 是 | 银行转账 / 承兑汇票 / 票据贴现 |
| 收款账户 | 自动 | 来自供应商银行账户 |
| 申请理由 / 备注 | 否 | - |
| 附件 | 否 | 合同、入库单、消耗单 |

**付款审批流程：**
```
[财务发起] → [运营管理员审核] → [财务总监审批]
    → [金额>50万：院领导审批] → [出纳执行付款]
    → [回写付款记录、更新结算单状态] → [推送供应商]
```

| 状态 | 说明 |
|------|------|
| 草稿 | 财务编辑中 |
| 待审核 | 已提交，等待审核 |
| 已审核 | 进入审批流 |
| 已审批 | 待出纳执行 |
| 部分付款 | 分期付款时 |
| 已付款 | 全额完成 |
| 已驳回 | 流程中止 |

#### 8.9.6 红冲矩阵与跨期处理

已结算数据不得被静默修改。发生定数包反消耗、科室退回、质量召回、价格追溯调整、发票冲红等跨期事项时，系统必须基于当前入账期生成红冲或调整数据，并保留原正向记录与反向记录。

| 业务阶段 | 处理方式 | 审批/复核要求 | 对供应商展示 | 院内查询展示 |
|----------|----------|---------------|--------------|--------------|
| 未生成结算单 | 在原业务期间生成负数消耗/退回记录，重新汇总结算草稿 | 按业务单据配置 | 明细负数行 | 明细 + 定数包数量字段 |
| 已生成未审核结算单 | 结算草稿重新计算，保留重算日志 | 财务或运营复核 | 明细正负行 | 明细 + 定数包数量字段 |
| 已审核/已发供应商未确认 | 当前入账期生成红冲明细，关联原结算单 | 供应商确认或对账复核 | 明细红冲行 | 明细 + 定数包数量字段 |
| 已确认未开票 | 当前入账期生成红冲结算数据，进入下次对账 | 财务复核 | 明细红冲行 | 明细 + 定数包数量字段 |
| 已开票未付款 | 生成发票冲红申请，红冲进入当前入账期 | 财务审批 + 发票复核 | 明细红冲行 + 发票状态 | 明细 + 定数包数量字段 |
| 已付款 | 生成次月红冲结算数据，可形成应付抵扣、退款应收或供应商往来调整 | 财务审批 + 对账确认 | 明细红冲行 | 明细 + 定数包数量字段 |
| 已推送 HIS/医保/财务外部系统 | 不自动改外部结果，生成待外部确认任务，收到冲销回执后关闭 | 接口复核 + 财务确认 | 明细红冲行 | 外部回执状态 + 明细 + 定数包数量字段 |

**展示与入账规则：**
- 推送给供应商的数据只展示明细，不出现定数包总行。
- 院内查询以明细数量、定数包数量、定数包包装数量字段区分，不需要展开为额外总行。
- 红冲数据以当前处理时间确定 `accountingPeriod`，不按原业务发生时间反向改账。
- 一正一反记录都必须保留，红冲记录必须关联原消耗、原定数包事件、原结算单、原发票和审批流程。
- 已结算定数包反消耗审批通过后，先恢复原科室定数包库存，再在当前入账期生成红冲结算数据。
- 对账、月结、发票、付款任一节点已关闭时，后续纠错只能走红冲/调整流程，不允许直接删除或覆盖原记录。
- 批号/效期批量调整不改变金额和结算状态，不生成供应商追溯信息更正通知，不触发已推送供应商数据重推。

---

## 9. 接口规范

### 9.1 登录API接口

#### 9.1.1 接口清单

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 |
|------|----------|----------|----------|------|
| 1 | 用户登录 | POST | `/api/v1/auth/login` | 用户名密码登录 |
| 2 | 用户登出 | POST | `/api/v1/auth/logout` | 退出登录 |
| 3 | 刷新Token | POST | `/api/v1/auth/refresh` | 刷新访问令牌 |
| 4 | 获取公钥 | GET | `/api/v1/auth/public-key` | 获取RSA公钥用于密码加密 |
| 5 | 获取验证码 | GET | `/api/v1/auth/captcha` | 获取图形验证码 |

#### 9.1.2 用户登录接口

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| username | string | 是 | 用户名 | "admin" |
| password | string | 是 | RSA加密后的密码（Base64编码） | "encrypted_password" |
| bizType | string | 否 | 业务类型：consumable/device/reagent，默认consumable | "consumable" |
| captcha | string | 条件必填 | 图形验证码（连续失败3次后需要） | "a3f8" |
| captchaKey | string | 条件必填 | 验证码标识 | "uuid-string" |
| rememberMe | boolean | 否 | 是否记住登录，默认false | true |

**响应参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| code | integer | 状态码：200成功，其他失败 |
| message | string | 提示信息 |
| data | object | 响应数据 |
| data.accessToken | string | 访问令牌（JWT） |
| data.refreshToken | string | 刷新令牌 |
| data.expiresIn | integer | accessToken有效期（秒） |
| data.tokenType | string | 令牌类型，固定"Bearer" |
| data.userInfo | object | 用户信息 |
| data.userInfo.userId | long | 用户ID |
| data.userInfo.username | string | 用户名 |
| data.userInfo.realName | string | 真实姓名 |
| data.userInfo.avatar | string | 头像URL |
| data.userInfo.deptName | string | 所属科室 |
| data.userInfo.roles | array | 角色列表 |
| data.userInfo.permissions | array | 权限标识列表 |

**错误码说明：**

| 错误码 | 说明 | 处理建议 |
|--------|------|----------|
| 200 | 成功 | - |
| 400 | 请求参数错误 | 检查请求参数格式 |
| 401 | 用户名或密码错误 | 提示用户重新输入 |
| 402 | 验证码错误 | 刷新验证码，提示重新输入 |
| 403 | 账号已被锁定 | 提示联系管理员 |
| 404 | 账号已被禁用 | 提示联系管理员 |
| 429 | 登录过于频繁 | 提示稍后再试 |
| 500 | 服务器内部错误 | 提示稍后重试 |

### 9.2 附件管理API接口

#### 9.2.1 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        外网用户                               │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   外网API网关 (Nginx)                        │
│              SSL终止 / WAF / 限流 / IP白名单                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   外网文件服务 (OSS)                         │
│              临时Token / 预签名URL / 防盗链                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ 内网同步
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   内网文件服务 (MinIO)                       │
│              持久存储 / 缩略图 / 病毒扫描                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   SPD后端服务 (Spring Boot)                  │
│              附件元数据管理 / 权限校验 / 审计日志              │
└─────────────────────────────────────────────────────────────┘
```

#### 9.2.2 存储策略

| 存储位置 | 用途 | 访问方式 | 有效期 |
|----------|------|----------|--------|
| MinIO (内网) | 主存储，所有附件持久化 | 内网直接访问 | 永久 |
| OSS (外网) | 外网访问镜像，按需同步 | 预签名URL | 15分钟 |
| CDN | 静态资源加速 | 缓存URL | 按配置 |
| 本地缓存 | 热点文件缓存 | 本地路径 | 按LRU策略 |

#### 9.2.3 内网接口清单

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 |
|------|----------|----------|----------|------|
| 1 | 附件列表 | GET | `/api/v1/attachment/list` | 查询附件列表 |
| 2 | 附件上传 | POST | `/api/v1/attachment/upload` | 上传附件 |
| 3 | 附件删除 | DELETE | `/api/v1/attachment/{id}` | 删除附件 |
| 4 | 附件预览 | GET | `/api/v1/attachment/{id}/preview` | 预览附件 |
| 5 | 附件下载 | GET | `/api/v1/attachment/{id}/download` | 下载附件 |
| 6 | 批量下载 | POST | `/api/v1/attachment/batch-download` | 批量下载 |
| 7 | 缩略图 | GET | `/api/v1/attachment/{id}/thumbnail` | 获取缩略图 |
| 8 | 同步到OSS | POST | `/api/v1/attachment/{id}/sync-oss` | 手动同步到OSS |

### 9.3 外网平台对接API

#### 9.3.1 认证方式

外网平台接入采用 **API Key + 请求签名** 的认证方式：

**签名生成步骤：**

1. 拼接签名字符串：
```
StringToSign = HTTP_METHOD + "\n" +
               URI + "\n" +
               X-Timestamp + "\n" +
               MD5(Body) + "\n" +
               X-Nonce
```

2. 使用HMAC-SHA256计算签名：
```
Signature = Base64(HMAC-SHA256(AppSecret, StringToSign))
```

**请求Header：**

| Header | 说明 | 示例 |
|--------|------|------|
| X-AppId | 应用ID | `spd_supplier_001` |
| X-Timestamp | 请求时间戳（秒） | `1716786600` |
| X-Nonce | 随机字符串（16位） | `a1b2c3d4e5f67890` |
| X-Signature | 请求签名 | `Base64(HMAC-SHA256(...))` |
| Content-Type | 内容类型 | `application/json` |

#### 9.3.2 接口清单

**商品目录相关：**

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 |
|------|----------|----------|----------|------|
| 1 | 获取商品列表 | GET | `/product/list` | 查询已准入商品 |
| 2 | 获取商品详情 | GET | `/product/{id}` | 商品详细信息 |
| 3 | 提交新品申请 | POST | `/product/apply` | 供应商提交新品 |
| 4 | 查询申请状态 | GET | `/product/apply/{applyNo}` | 查询审批进度 |
| 5 | 修改申请信息 | PUT | `/product/apply/{applyNo}` | 退回修改后更新 |
| 6 | 撤回申请 | DELETE | `/product/apply/{applyNo}` | 撤回待审批申请 |

**附件相关：**

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 |
|------|----------|----------|----------|------|
| 7 | 获取临时Token | POST | `/attachment/token` | 获取附件访问Token |
| 8 | 获取预览URL | GET | `/attachment/{id}/preview-url` | 附件预览URL |
| 9 | 获取下载URL | GET | `/attachment/{id}/download-url` | 附件下载URL |
| 10 | 查询附件列表 | GET | `/attachment/list` | 查询授权附件 |
| 11 | 获取上传凭证 | POST | `/attachment/upload-token` | 获取OSS上传凭证 |
| 12 | 上传回调 | POST | `/attachment/callback` | OSS上传回调 |

### 9.4 核心业务API接口规范

#### 9.4.1 用户管理API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 用户列表 | GET | `/api/v1/user/list` | 分页查询用户 | user:view |
| 2 | 用户详情 | GET | `/api/v1/user/{id}` | 查询用户详情 | user:view |
| 3 | 新增用户 | POST | `/api/v1/user` | 创建用户 | user:create |
| 4 | 编辑用户 | PUT | `/api/v1/user/{id}` | 修改用户 | user:update |
| 5 | 删除用户 | DELETE | `/api/v1/user/{id}` | 删除用户 | user:delete |
| 6 | 重置密码 | POST | `/api/v1/user/{id}/reset-password` | 重置密码 | user:update |
| 7 | 分配角色 | PUT | `/api/v1/user/{id}/roles` | 分配角色 | user:update |
| 8 | 启用/禁用 | PUT | `/api/v1/user/{id}/status` | 修改状态 | user:update |

#### 9.4.2 角色权限API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 角色列表 | GET | `/api/v1/role/list` | 分页查询角色 | role:view |
| 2 | 角色详情 | GET | `/api/v1/role/{id}` | 查询角色详情 | role:view |
| 3 | 新增角色 | POST | `/api/v1/role` | 创建角色 | role:create |
| 4 | 编辑角色 | PUT | `/api/v1/role/{id}` | 修改角色 | role:update |
| 5 | 删除角色 | DELETE | `/api/v1/role/{id}` | 删除角色 | role:delete |
| 6 | 分配权限 | PUT | `/api/v1/role/{id}/permissions` | 分配权限 | role:update |
| 7 | 权限树 | GET | `/api/v1/permission/tree` | 获取权限树 | role:view |

#### 9.4.3 科室管理API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 科室树 | GET | `/api/v1/dept/tree` | 获取科室树 | dept:view |
| 2 | 科室列表 | GET | `/api/v1/dept/list` | 分页查询科室 | dept:view |
| 3 | 新增科室 | POST | `/api/v1/dept` | 创建科室 | dept:create |
| 4 | 编辑科室 | PUT | `/api/v1/dept/{id}` | 修改科室 | dept:update |
| 5 | 删除科室 | DELETE | `/api/v1/dept/{id}` | 删除科室 | dept:delete |

#### 9.4.4 商品目录API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 商品列表 | GET | `/api/v1/product/list` | 分页查询商品 | product:view |
| 2 | 商品详情 | GET | `/api/v1/product/{id}` | 查询商品详情 | product:view |
| 3 | 新增商品 | POST | `/api/v1/product` | 创建商品 | product:create |
| 4 | 编辑商品 | PUT | `/api/v1/product/{id}` | 修改商品 | product:update |
| 5 | 删除商品 | DELETE | `/api/v1/product/{id}` | 删除商品 | product:delete |
| 6 | 启用/停用 | PUT | `/api/v1/product/{id}/status` | 修改状态 | product:update |
| 7 | 导入商品 | POST | `/api/v1/product/import` | Excel导入 | product:create |
| 8 | 导出商品 | GET | `/api/v1/product/export` | Excel导出 | product:view |
| 9 | 商品分类树 | GET | `/api/v1/product/category/tree` | 分类树 | product:view |
| 10 | 批量修改 | PUT | `/api/v1/product/batch` | 批量修改分类、状态、是否定数管理等字段 | product:update |
| 11 | 变更记录 | GET | `/api/v1/product/{id}/change-log` | 查询字段变更、审批单和生效记录 | product:view |
| 12 | 待审批目录 | GET | `/api/v1/product/pending/list` | 查询新品准入、字段变更、资质更新、价格调整申请 | product:approve |
| 13 | 新增审批申请 | POST | `/api/v1/product/pending` | 创建待审批目录申请 | product:create |
| 14 | 审批目录申请 | PUT | `/api/v1/product/pending/{id}/approve` | 审批通过/驳回/退回修改 | product:approve |

#### 9.4.5 库存管理API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 库存列表 | GET | `/api/v1/inventory/list` | 分页查询库存 | inventory:view |
| 2 | 库存详情 | GET | `/api/v1/inventory/{id}` | 查询库存详情 | inventory:view |
| 3 | 入库操作 | POST | `/api/v1/inventory/in` | 入库 | inventory:in |
| 4 | 出库操作 | POST | `/api/v1/inventory/out` | 出库 | inventory:out |
| 5 | 库存盘点 | POST | `/api/v1/inventory/check` | 盘点 | inventory:check |
| 6 | 库存预警 | GET | `/api/v1/inventory/warning` | 预警列表 | inventory:view |
| 7 | 效期预警 | GET | `/api/v1/inventory/expiry` | 效期预警 | inventory:view |
| 8 | 库存调整 | POST | `/api/v1/inventory/adjust` | 库存调整 | inventory:update |

#### 9.4.6 采购管理API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 采购计划列表 | GET | `/api/v1/purchase/plan/list` | 查询计划 | purchase:view |
| 2 | 新增采购计划 | POST | `/api/v1/purchase/plan` | 创建计划 | purchase:create |
| 3 | 采购订单列表 | GET | `/api/v1/purchase/order/list` | 查询订单 | purchase:view |
| 4 | 新增采购订单 | POST | `/api/v1/purchase/order` | 创建订单 | purchase:create |
| 5 | 订单审批 | PUT | `/api/v1/purchase/order/{id}/approve` | 审批订单 | purchase:approve |
| 6 | 订单到货 | POST | `/api/v1/purchase/order/{id}/receive` | 确认到货 | purchase:update |
| 7 | 退货申请 | POST | `/api/v1/purchase/return` | 申请退货 | purchase:return |

#### 9.4.7 科室申领API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 申领列表 | GET | `/api/v1/apply/list` | 查询申领 | apply:view |
| 2 | 新增申领 | POST | `/api/v1/apply` | 创建申领 | apply:create |
| 3 | 申领审批 | PUT | `/api/v1/apply/{id}/approve` | 审批申领 | apply:approve |
| 4 | 申领出库 | POST | `/api/v1/apply/{id}/out` | 确认出库 | apply:out |
| 5 | 申领签收 | POST | `/api/v1/apply/{id}/receive` | 确认签收 | apply:receive |
| 6 | 消耗登记 | POST | `/api/v1/apply/{id}/consume` | 消耗登记 | apply:consume |

#### 9.4.8 结算对账API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 结算单列表 | GET | `/api/v1/settlement/list` | 查询结算单 | settlement:view |
| 2 | 生成结算单 | POST | `/api/v1/settlement` | 生成结算 | settlement:create |
| 3 | 结算审批 | PUT | `/api/v1/settlement/{id}/approve` | 审批结算 | settlement:approve |
| 4 | 发票登记 | POST | `/api/v1/settlement/{id}/invoice` | 登记发票 | settlement:invoice |
| 5 | 付款申请 | POST | `/api/v1/settlement/{id}/payment` | 申请付款 | settlement:payment |
| 6 | 对账明细 | GET | `/api/v1/settlement/{id}/detail` | 对账明细 | settlement:view |

#### 9.4.9 UDI追溯API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | UDI解析 | POST | `/api/v1/udi/parse` | 解析UDI码 | udi:view |
| 2 | UDI绑定 | POST | `/api/v1/udi/bind` | 绑定UDI | udi:create |
| 3 | UDI追溯 | GET | `/api/v1/udi/trace/{udiCode}` | 追溯链路 | udi:view |
| 4 | UDI校验 | GET | `/api/v1/udi/verify/{udiCode}` | 校验UDI | udi:view |

#### 9.4.10 高值耗材API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 高值耗材列表 | GET | `/api/v1/highvalue/list` | 查询高值耗材 | highvalue:view |
| 2 | 使用登记 | POST | `/api/v1/highvalue/use` | 登记使用 | highvalue:create |
| 3 | 患者关联 | PUT | `/api/v1/highvalue/{id}/patient` | 关联患者 | highvalue:update |
| 4 | 使用审批 | PUT | `/api/v1/highvalue/{id}/approve` | 审批使用 | highvalue:approve |
| 5 | 计费推送 | POST | `/api/v1/highvalue/{id}/charge` | 推送HIS | highvalue:update |

#### 9.4.11 冷链监控API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 温度记录 | GET | `/api/v1/coldchain/temperature` | 查询温度记录 | coldchain:view |
| 2 | 温度告警 | GET | `/api/v1/coldchain/alarm` | 查询告警 | coldchain:view |
| 3 | 设备列表 | GET | `/api/v1/coldchain/device/list` | 查询设备 | coldchain:view |
| 4 | 温度上报 | POST | `/api/v1/coldchain/temperature` | 设备上报温度 | coldchain:create |

#### 9.4.12 报表中心API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 库存报表 | GET | `/api/v1/report/inventory` | 库存统计 | report:view |
| 2 | 消耗报表 | GET | `/api/v1/report/consumption` | 消耗统计 | report:view |
| 3 | 采购报表 | GET | `/api/v1/report/purchase` | 采购统计 | report:view |
| 4 | 供应商报表 | GET | `/api/v1/report/supplier` | 供应商绩效 | report:view |
| 5 | 成本报表 | GET | `/api/v1/report/cost` | 成本分析 | report:view |
| 6 | 导出报表 | GET | `/api/v1/report/export` | 导出Excel | report:export |

#### 9.4.13 定数包管理API

所有会改变定数包状态的接口必须携带 `Idempotency-Key` 请求头和 `expectedVersion` 参数，服务端按定数包标签维度进行幂等与并发控制。

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 定数包模板列表 | GET | `/api/v1/fixed-package/template/list` | 查询包族、模板版本、状态 | fixedpkg:view |
| 2 | 新增模板草稿 | POST | `/api/v1/fixed-package/template` | 创建定数包模板草稿 | fixedpkg:create |
| 3 | 发布模板版本 | POST | `/api/v1/fixed-package/template/{id}/publish` | 校验是否定数管理并发布新版本 | fixedpkg:update |
| 4 | 安全量列表 | GET | `/api/v1/fixed-package/safety/list` | 查询院区/科室/库房安全包数 | fixedpkg:view |
| 5 | 保存安全量 | PUT | `/api/v1/fixed-package/safety` | 保存安全包数、最大包数和补货策略 | fixedpkg:update |
| 6 | 打包生成标签 | POST | `/api/v1/fixed-package/instance/pack` | 扣减散货库存，生成定数标签和快照 | fixedpkg:pack |
| 7 | 标签状态查询 | GET | `/api/v1/fixed-package/instance/{labelId}` | 查询当前状态和版本快照 | fixedpkg:view |
| 8 | 事件链查询 | GET | `/api/v1/fixed-package/instance/{labelId}/events` | 查询不可变事件流水 | fixedpkg:view |
| 9 | 定数包消耗 | POST | `/api/v1/fixed-package/instance/{labelId}/consume` | 拆包即整包消耗，生成正向事件 | fixedpkg:consume |
| 10 | 反消耗申请 | POST | `/api/v1/fixed-package/instance/{labelId}/reverse` | 误操作且实物完整的反消耗申请 | fixedpkg:reverse |
| 11 | 反消耗审批 | PUT | `/api/v1/fixed-package/reversal/{id}/approve` | 科室审批并恢复原科室定数包库存 | fixedpkg:approve |
| 12 | 重新消耗 | POST | `/api/v1/fixed-package/instance/{labelId}/reconsume` | 反消耗错误时追加新的正向事件 | fixedpkg:consume |
| 13 | 召回停用 | POST | `/api/v1/fixed-package/instance/{labelId}/recall` | 质量事件命中后整包停用 | fixedpkg:update |
| 14 | 补货任务评分 | POST | `/api/v1/fixed-package/replenishment/score` | 按院区权重计算待处理任务分数 | fixedpkg:view |

#### 9.4.14 PDA离线与补传API

PDA 离线数据上传成功前只作为本机待上传数据，不改变正式库存、安全库存和结算数据。后补数据按服务端处理时间确定 `accountingPeriod`，并打"离线补传"标识。

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | PDA设备心跳 | POST | `/api/v1/pda/device/heartbeat` | 上报设备、岗位、最后在线时间和离线时长 | pda:heartbeat |
| 2 | PDA离线列表 | GET | `/api/v1/pda/device/offline` | PC端查询离线设备提醒 | pda:view |
| 3 | 待上传摘要 | POST | `/api/v1/pda/offline/summary` | 上报本机待上传数量和最早离线时间 | pda:upload |
| 4 | 离线批量补传 | POST | `/api/v1/pda/offline/upload` | 批量上传扫码/记录类采集数据 | pda:upload |
| 5 | 补传结果查询 | GET | `/api/v1/pda/offline/upload/{batchId}` | 查询本批上传处理结果 | pda:view |
| 6 | 离线补传明细 | GET | `/api/v1/pda/offline/records` | 查询带离线补传标识的业务明细 | pda:view |

#### 9.4.15 数据统计分析API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | ETL任务列表 | GET | `/api/v1/analysis/etl/list` | 查询ETL任务 | analysis:view |
| 2 | 新增ETL任务 | POST | `/api/v1/analysis/etl` | 创建ETL任务 | analysis:create |
| 3 | 编辑ETL任务 | PUT | `/api/v1/analysis/etl/{id}` | 修改ETL任务 | analysis:update |
| 4 | 删除ETL任务 | DELETE | `/api/v1/analysis/etl/{id}` | 删除ETL任务 | analysis:delete |
| 5 | 执行ETL任务 | POST | `/api/v1/analysis/etl/{id}/execute` | 手动执行ETL | analysis:execute |
| 6 | ETL任务日志 | GET | `/api/v1/analysis/etl/{id}/log` | 查询执行日志 | analysis:view |
| 7 | 数据质量报告 | GET | `/api/v1/analysis/quality` | 数据质量检测 | analysis:view |
| 8 | 多维分析 | POST | `/api/v1/analysis/olap` | 多维数据分析 | analysis:view |
| 9 | 智能分析 | POST | `/api/v1/analysis/insight` | 智能洞察分析 | analysis:view |
| 10 | 趋势预测 | POST | `/api/v1/analysis/forecast` | 数据趋势预测 | analysis:view |

#### 9.4.16 数据统计展示API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 仪表盘列表 | GET | `/api/v1/dashboard/list` | 查询仪表盘 | dashboard:view |
| 2 | 新增仪表盘 | POST | `/api/v1/dashboard` | 创建仪表盘 | dashboard:create |
| 3 | 编辑仪表盘 | PUT | `/api/v1/dashboard/{id}` | 修改仪表盘 | dashboard:update |
| 4 | 删除仪表盘 | DELETE | `/api/v1/dashboard/{id}` | 删除仪表盘 | dashboard:delete |
| 5 | 仪表盘详情 | GET | `/api/v1/dashboard/{id}` | 查询仪表盘详情 | dashboard:view |
| 6 | 仪表盘数据 | GET | `/api/v1/dashboard/{id}/data` | 获取仪表盘数据 | dashboard:view |
| 7 | 数据大屏数据 | GET | `/api/v1/dashboard/screen/{type}` | 获取大屏数据 | dashboard:view |
| 8 | 移动端看板 | GET | `/api/v1/dashboard/mobile/{type}` | 移动端看板数据 | dashboard:view |
| 9 | 图表组件库 | GET | `/api/v1/dashboard/components` | 获取图表组件列表 | dashboard:view |
| 10 | 导出可视化 | POST | `/api/v1/dashboard/export` | 导出可视化报表 | dashboard:export |

#### 9.4.17 审计整改新增接口覆盖清单

以下接口为审计整改后新增或需补强的接口能力。详细 OpenAPI 需在 `docs/api/` 中同步维护。

| 能力域 | 接口能力 | 说明 | 权限要求 |
|--------|----------|------|----------|
| 业务配置 | 配置命中说明查询 | 按业务页面、对象、配置类型返回最终生效值和来源配置 | config:view |
| 业务配置 | 配置冲突检测 | 新增/修改/定时生效前检测同级同对象同时间段冲突 | config:validate |
| 缺货登记 | 缺货登记列表/详情 | 查询缺货类型、缺口数量、满足数量、紧急等级、登记时间 | shortage:view |
| 缺货登记 | 人工关闭缺货登记 | 未满足时带原因关闭，关闭后不参与来货匹配 | shortage:close |
| 来货匹配 | 来货匹配缺货登记 | 收货上架后按优先级匹配缺货登记并生成建议/任务 | shortage:match |
| 定数包打包 | 待确认打包任务重算 | 系数、调价、盘亏、召回等变化后释放预占并重算包数/余数 | fixedpkg:update |
| 定数包打包 | 预占散货查询 | 查询未确认打包任务占用的来源系统批次和数量 | fixedpkg:view |
| 定数包解包 | 解包回散货 | 未消耗定数包作废回散货，不产生消耗和结算 | fixedpkg:update |
| 标签打印 | 原标签补打 | 选择补打原因，记录打印人和打印次数 | fixedpkg:print |
| 批次调价 | 批次调价影响清单 | 查询调价影响的未消耗库存、待确认任务、定数包等 | price:view |
| 批号效期 | 批号/效期批量调整 | 审批通过后全量更新追溯属性并保留变更记录 | batch:update |
| PDA 离线 | 补传冲突复核 | 对补传后库存不足、状态冲突、重复扫码进行复核 | pda:review |

---

### 9.5 前端接口对接规范

#### 9.5.1 Axios封装

```typescript
// src/utils/request.ts
import axios, { AxiosInstance, AxiosRequestConfig, AxiosResponse } from 'axios'
import { ElMessage } from 'element-plus'
import { useUserStore } from '@/store/modules/user'

const request: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api/v1',
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
request.interceptors.request.use(
  (config: AxiosRequestConfig) => {
    const userStore = useUserStore()
    if (userStore.token) {
      config.headers = config.headers || {}
      config.headers.Authorization = `Bearer ${userStore.token}`
    }
    if (config.method === 'get') {
      config.params = { ...config.params, _t: Date.now() }
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  (response: AxiosResponse) => {
    const { code, message, data } = response.data
    if (code === 200) {
      return data
    }
    if (code === 401) {
      const userStore = useUserStore()
      return userStore.refreshToken().then(() => {
        return request(response.config)
      }).catch(() => {
        userStore.logout()
        window.location.href = '/login'
        return Promise.reject(new Error('登录已过期'))
      })
    }
    ElMessage.error(message || '请求失败')
    return Promise.reject(new Error(message))
  },
  (error) => {
    const { response } = error
    if (response) {
      const status = response.status
      const message = response.data?.message || '服务器错误'
      switch (status) {
        case 400:
          ElMessage.error('请求参数错误')
          break
        case 401:
          ElMessage.error('未授权，请重新登录')
          break
        case 403:
          ElMessage.error('拒绝访问')
          break
        case 404:
          ElMessage.error('请求的资源不存在')
          break
        case 429:
          ElMessage.error('请求过于频繁，请稍后再试')
          break
        case 500:
          ElMessage.error(message)
          break
        default:
          ElMessage.error(`请求失败: ${status}`)
      }
    } else {
      ElMessage.error('网络连接失败，请检查网络')
    }
    return Promise.reject(error)
  }
)

export default request
```

---

### 9.6 AI服务API接口规范（预留）

#### 9.6.1 智能问答API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 智能问答 | POST | `/api/v1/ai/chat` | 自然语言问答 | ai:chat |
| 2 | 知识库检索 | POST | `/api/v1/ai/retrieval` | 向量检索 | ai:retrieval |
| 3 | 问答历史 | GET | `/api/v1/ai/chat/history` | 查询历史记录 | ai:chat |
| 4 | 清除历史 | DELETE | `/api/v1/ai/chat/history` | 清除历史记录 | ai:chat |

**智能问答请求参数：**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| question | string | 是 | 用户问题 | "骨科常用的钢板有哪些品牌？" |
| sessionId | string | 否 | 会话ID，保持上下文 | "sess_001" |
| context | array | 否 | 历史对话上下文 | [{"role":"user","content":"..."}] |
| knowledgeBase | string | 否 | 指定知识库：product/inventory/purchase/all | "product" |

**智能问答响应参数：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| code | integer | 状态码 |
| message | string | 提示信息 |
| data | object | 响应数据 |
| data.answer | string | AI回答内容 |
| data.sources | array | 引用来源（知识库文档） |
| data.confidence | float | 置信度（0-1） |
| data.sessionId | string | 会话ID |

#### 9.6.2 智能审批API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 审批建议 | POST | `/api/v1/ai/approve/suggest` | 获取审批建议 | ai:approve |
| 2 | 风险评分 | POST | `/api/v1/ai/approve/risk` | 获取风险评分 | ai:approve |
| 3 | 相似审批 | GET | `/api/v1/ai/approve/similar` | 查询相似审批记录 | ai:approve |

#### 9.6.3 智能预警API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 预警分析 | POST | `/api/v1/ai/warning/analyze` | AI分析预警 | ai:warning |
| 2 | 处置建议 | POST | `/api/v1/ai/warning/suggest` | 获取处置建议 | ai:warning |
| 3 | 趋势预测 | GET | `/api/v1/ai/warning/trend` | 预测未来趋势 | ai:warning |

#### 9.6.4 智能分析API

| 序号 | 接口名称 | 请求方式 | 接口路径 | 说明 | 权限要求 |
|------|----------|----------|----------|------|----------|
| 1 | 数据洞察 | POST | `/api/v1/ai/analysis/insight` | 数据洞察分析 | ai:analysis |
| 2 | 报告生成 | POST | `/api/v1/ai/analysis/report` | 智能生成报告 | ai:analysis |
| 3 | 异常检测 | POST | `/api/v1/ai/analysis/anomaly` | 异常数据检测 | ai:analysis |

#### 9.6.5 AI服务对接规范

**本地大模型部署方式：**

```
┌─────────────────────────────────────────┐
│           AI服务 (Python/FastAPI)        │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│  │ LangChain│ │ 向量检索 │ │ 提示工程 │   │
│  └─────────┘ └─────────┘ └─────────┘   │
│  ┌─────────────────────────────────────┐│
│  │      本地大模型推理引擎              ││
│  │  [Ollama] 或 [vLLM] 或 [TGI]       ││
│  │  模型：LLaMA3 / ChatGLM3 / Qwen     ││
│  │  硬件：GPU服务器 (NVIDIA A100/V100) ││
│  └─────────────────────────────────────┘│
└─────────────────────────────────────────┘
                    │
                    │ HTTP/REST
                    ▼
┌─────────────────────────────────────────┐
│         SPD后端服务 (Spring Boot)        │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│  │ AI客户端 │ │ 缓存层  │ │ 限流器  │   │
│  │ (Feign) │ │ (Redis) │ │ (Sentinel)│  │
│  └─────────┘ └─────────┘ └─────────┘   │
└─────────────────────────────────────────┘
```

**对接要点：**

| 要点 | 说明 |
|------|------|
| 通信协议 | HTTP/RESTful API，JSON格式 |
| 鉴权方式 | API Key + IP白名单 |
| 超时设置 | 连接超时5s，读取超时60s |
| 重试策略 | 失败重试3次，指数退避 |
| 降级策略 | AI服务不可用时，返回默认逻辑结果 |
| 数据隔离 | AI服务不直接访问业务数据库，通过API获取数据 |
| 隐私保护 | 患者信息脱敏后传入AI服务 |

#### 9.6.6 AI使用边界与审计

AI 服务只提供问答、分析、预警解释、审批辅助和报告草稿，不得直接替代人工完成审批、反消耗、召回、结算、付款、库存状态变更等强一致业务动作。

| 场景 | AI可做 | AI不可做 | 人工要求 |
|------|-------|----------|----------|
| 智能问答 | 检索制度、流程、商品资料并给出来源 | 直接修改主数据或业务单据 | 用户确认后自行操作 |
| 智能审批 | 给出风险点、相似历史、建议结论 | 自动通过/驳回审批 | 审批人明确点击确认 |
| 智能预警 | 解释异常原因、推荐处置路径 | 自动关闭告警或解除库存隔离 | 责任人处理并复核 |
| 智能分析 | 生成趋势洞察、异常排行、预测建议 | 直接生成采购订单或结算单 | 业务人员选择采纳 |
| 报告生成 | 生成运营报告草稿 | 对外发送未经复核的报告 | 管理人员审阅发布 |

**审计规则：**
- 每次 AI 调用必须记录调用人、院区、业务场景、输入摘要、输出摘要、模型名称、模型版本、知识库版本、数据时间范围和响应耗时。
- 涉及患者、医生、供应商报价、合同价格等敏感数据时，必须按最小必要原则脱敏后传入 AI 服务。
- AI 输出必须标识引用来源，包括制度文件、商品主数据、结算数据、库存数据或分析模型；无来源时显示为“仅供参考”。
- 低置信度、数据缺失、来源冲突时，AI 必须返回风险提示，不得给出确定性结论。
- 供应商端可见内容不得由 AI 自动发布，必须经过院内人员复核。
- AI 审计日志纳入安全审计留存，不允许业务用户删除；管理员只能按合规策略归档。

---

## 10. 数据库设计

### 10.0 当前数据库交付基线

当前数据库设计以 `docs/db/init.sql`、`docs/db/README.md`、`docs/db/TABLES_INDEX.md` 和 `docs/db/ER_RELATIONS.md` 为权威入口。v1.2.3 基线覆盖第 7 章运营支撑、第 8 章院内 SPD 业务扩展、第 12 章合规与扩展业务及安全运行期支撑。

| 交付项 | 当前口径 |
|--------|----------|
| 全量 DDL | `docs/db/init.sql` 按 00-23 加载 |
| 表清单 | 以 `docs/db/TABLES_INDEX.md` 和当前 DDL 为准，随业务规则迭代更新 |
| 报表中心 | `20_report_center.sql` |
| 核心主表补齐 | `21_core_main_tables.sql` |
| SaaS 配置中心 | `22_saas_config_center.sql` |
| 安全运行期 | `23_security_runtime_hardening.sql`，包含 KMS 密钥引用与审计双写队列 |
| Flyway 迁移 | `db/migration/` 当前为 baseline-only 骨架，生产启用按环境执行 baseline 或展开 canonical DDL |

### 10.1 ER图

```
+----------------+       +----------------+       +----------------+
|   sys_user     |       |   sys_role     |       | sys_permission   |
+----------------+       +----------------+       +----------------+
| PK user_id     |       | PK role_id     |       | PK perm_id       |
|    username    |       |    role_name   |       |    perm_name     |
|    password    |       |    role_code   |       |    perm_code     |
|    real_name   |       |    description |       |    description   |
|    status      |       |    status      |       |    status        |
|    ...         |       |    ...         |       |    ...           |
+----------------+       +----------------+       +----------------+
       | 1:N                    | N:M                    | N:M
       |                        |                        |
       v                        v                        v
+----------------+       +----------------+       +----------------+
| sys_user_role  |       | sys_role_perm  |       | sys_dept       |
+----------------+       +----------------+       +----------------+
| PK id          |       | PK id          |       | PK dept_id     |
| FK user_id     |       | FK role_id     |       |    dept_name   |
| FK role_id     |       | FK perm_id     |       |    parent_id   |
+----------------+       +----------------+       |    ...         |
                                                   +----------------+
                                                          | 1:N
                                                          v
                                                   +----------------+
                                                   | sys_login_log  |
                                                   +----------------+
                                                   | PK log_id      |
                                                   | FK user_id     |
                                                   |    login_ip    |
                                                   |    login_time  |
                                                   |    status      |
                                                   +----------------+
```

### 10.2 用户表（sys_user）

```sql
CREATE TABLE `sys_user` (
  `user_id`           BIGINT UNSIGNED     NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username`          VARCHAR(50)         NOT NULL                COMMENT '用户名',
  `password`          VARCHAR(100)        NOT NULL                COMMENT '密码（BCrypt加密）',
  `real_name`         VARCHAR(50)         DEFAULT NULL            COMMENT '真实姓名',
  `phone`             VARCHAR(20)         DEFAULT NULL            COMMENT '手机号',
  `email`             VARCHAR(100)        DEFAULT NULL            COMMENT '邮箱',
  `avatar`            VARCHAR(255)        DEFAULT NULL            COMMENT '头像URL',
  `gender`            TINYINT             DEFAULT 0               COMMENT '性别：0-未知 1-男 2-女',
  `dept_id`           BIGINT UNSIGNED     DEFAULT NULL            COMMENT '所属科室ID',
  `status`            TINYINT             NOT NULL DEFAULT 1      COMMENT '状态：0-禁用 1-启用',
  `login_fail_count`  INT                 DEFAULT 0               COMMENT '连续登录失败次数',
  `lock_time`         DATETIME            DEFAULT NULL            COMMENT '锁定时间',
  `last_login_time`   DATETIME            DEFAULT NULL            COMMENT '最后登录时间',
  `last_login_ip`     VARCHAR(50)         DEFAULT NULL            COMMENT '最后登录IP',
  `create_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '创建人ID',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '更新人ID',
  `update_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted`           TINYINT             NOT NULL DEFAULT 0      COMMENT '删除标记：0-未删除 1-已删除',
  PRIMARY KEY (`user_id`),
  UNIQUE KEY `uk_username` (`username`),
  UNIQUE KEY `uk_phone` (`phone`),
  KEY `idx_dept_id` (`dept_id`),
  KEY `idx_status` (`status`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

### 10.3 角色表（sys_role）

```sql
CREATE TABLE `sys_role` (
  `role_id`           BIGINT UNSIGNED     NOT NULL AUTO_INCREMENT COMMENT '角色ID',
  `role_name`         VARCHAR(50)         NOT NULL                COMMENT '角色名称',
  `role_code`         VARCHAR(50)         NOT NULL                COMMENT '角色编码',
  `description`       VARCHAR(200)        DEFAULT NULL            COMMENT '角色描述',
  `data_scope`        TINYINT             DEFAULT 1               COMMENT '数据范围：1-全部 2-本部门 3-本部门及子部门 4-仅本人',
  `status`            TINYINT             NOT NULL DEFAULT 1      COMMENT '状态：0-禁用 1-启用',
  `sort_order`        INT                 DEFAULT 0               COMMENT '显示排序',
  `create_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '创建人ID',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '更新人ID',
  `update_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted`           TINYINT             NOT NULL DEFAULT 0      COMMENT '删除标记',
  PRIMARY KEY (`role_id`),
  UNIQUE KEY `uk_role_code` (`role_code`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='角色表';
```

### 10.4 预置角色数据

```sql
INSERT INTO `sys_role` (`role_name`, `role_code`, `description`, `data_scope`, `status`, `sort_order`) VALUES
('系统管理员', 'admin', '系统超级管理员，拥有所有权限', 1, 1, 1),
('运营管理员', 'operator', '日常运营管理，库存、采购等操作', 3, 1, 2),
('采购人员', 'purchaser', '采购计划、订单管理、供应商沟通', 2, 1, 3),
('库管人员', 'warehouse', '入库、出库、盘点、库存管理', 2, 1, 4),
('科室人员', 'dept_user', '科室申领、使用登记', 4, 1, 5),
('财务人员', 'finance', '结算对账、发票管理、付款审批', 2, 1, 6),
('质控人员', 'qc', '质量监控、追溯审计、合规检查', 1, 1, 7),
('医生/护士', 'doctor', '高值耗材使用、患者关联', 4, 1, 8),
('院领导', 'leader', '运营总览、决策分析', 1, 1, 9),
('审计人员', 'auditor', '数据查询、报表查看，无操作权限', 1, 1, 10),
('供应商', 'supplier', '外部供应商，查看订单和配送', 4, 1, 11),
('审批人员', 'approver', '各业务审批节点处理', 3, 1, 12);
```

### 10.5 权限表（sys_permission）

```sql
CREATE TABLE `sys_permission` (
  `perm_id`           BIGINT UNSIGNED     NOT NULL AUTO_INCREMENT COMMENT '权限ID',
  `parent_id`         BIGINT UNSIGNED     DEFAULT 0               COMMENT '父权限ID，0为顶级',
  `perm_name`         VARCHAR(50)         NOT NULL                COMMENT '权限名称',
  `perm_code`         VARCHAR(100)        NOT NULL                COMMENT '权限标识，如：user:view',
  `perm_type`         TINYINT             NOT NULL DEFAULT 1      COMMENT '权限类型：1-菜单 2-按钮 3-接口',
  `path`              VARCHAR(200)        DEFAULT NULL            COMMENT '路由路径',
  `component`         VARCHAR(200)        DEFAULT NULL            COMMENT '组件路径',
  `icon`              VARCHAR(50)         DEFAULT NULL            COMMENT '菜单图标',
  `sort_order`        INT                 DEFAULT 0               COMMENT '显示排序',
  `status`            TINYINT             NOT NULL DEFAULT 1      COMMENT '状态：0-禁用 1-启用',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted`           TINYINT             NOT NULL DEFAULT 0      COMMENT '删除标记',
  PRIMARY KEY (`perm_id`),
  UNIQUE KEY `uk_perm_code` (`perm_code`),
  KEY `idx_parent_id` (`parent_id`),
  KEY `idx_perm_type` (`perm_type`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='权限表';
```

### 10.6 附件表（sys_attachment）

```sql
CREATE TABLE `sys_attachment` (
  `attachment_id`     BIGINT UNSIGNED     NOT NULL AUTO_INCREMENT COMMENT '附件ID',
  `biz_type`          VARCHAR(50)         NOT NULL                COMMENT '业务类型：pending/product/supplier',
  `biz_id`            BIGINT UNSIGNED     NOT NULL                COMMENT '业务ID',
  `file_name`         VARCHAR(255)        NOT NULL                COMMENT '原始文件名',
  `file_ext`          VARCHAR(20)         NOT NULL                COMMENT '文件扩展名',
  `file_type`         VARCHAR(20)         NOT NULL                COMMENT '文件类型：pdf/image/video/doc',
  `file_size`         BIGINT UNSIGNED     NOT NULL                COMMENT '文件大小（字节）',
  `file_path`         VARCHAR(500)        NOT NULL                COMMENT '文件存储路径',
  `file_url`          VARCHAR(500)        NOT NULL                COMMENT '文件访问URL',
  `thumbnail_path`    VARCHAR(500)        DEFAULT NULL            COMMENT '缩略图路径（图片类型）',
  `thumbnail_url`     VARCHAR(500)        DEFAULT NULL            COMMENT '缩略图访问URL',
  `category`          VARCHAR(50)         DEFAULT 'other'         COMMENT '附件分类：qualification/image/document/other',
  `description`       VARCHAR(500)        DEFAULT NULL            COMMENT '附件描述',
  `valid_date`        DATE                DEFAULT NULL            COMMENT '有效期（资质附件）',
  `storage_type`      TINYINT             NOT NULL DEFAULT 1      COMMENT '存储类型：1-MinIO 2-OSS',
  `is_synced`         TINYINT             NOT NULL DEFAULT 0      COMMENT '是否已同步到OSS：0-否 1-是',
  `download_count`    INT                 DEFAULT 0               COMMENT '下载次数',
  `create_by`         BIGINT UNSIGNED     NOT NULL                COMMENT '上传人ID',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '上传时间',
  `update_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted`           TINYINT             NOT NULL DEFAULT 0      COMMENT '删除标记',
  PRIMARY KEY (`attachment_id`),
  KEY `idx_biz_type_id` (`biz_type`, `biz_id`),
  KEY `idx_file_type` (`file_type`),
  KEY `idx_category` (`category`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='附件表';

### 10.7 AI知识库表（ai_knowledge_base）

```sql
CREATE TABLE `ai_knowledge_base` (
  `kb_id`             BIGINT UNSIGNED     NOT NULL AUTO_INCREMENT COMMENT '知识库ID',
  `kb_name`           VARCHAR(100)        NOT NULL                COMMENT '知识库名称',
  `kb_code`           VARCHAR(50)         NOT NULL                COMMENT '知识库编码',
  `kb_type`           VARCHAR(20)         NOT NULL                COMMENT '类型：product/inventory/purchase/rule',
  `description`       VARCHAR(500)        DEFAULT NULL            COMMENT '知识库描述',
  `vector_collection` VARCHAR(100)        NOT NULL                COMMENT '向量集合名称（Milvus）',
  `embedding_model`   VARCHAR(100)        DEFAULT 'bge-large-zh'  COMMENT '向量模型',
  `chunk_size`        INT                 DEFAULT 500             COMMENT '分块大小',
  `chunk_overlap`     INT                 DEFAULT 50              COMMENT '分块重叠大小',
  `status`            TINYINT             NOT NULL DEFAULT 1      COMMENT '状态：0-禁用 1-启用',
  `create_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '创建人ID',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '更新人ID',
  `update_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`kb_id`),
  UNIQUE KEY `uk_kb_code` (`kb_code`),
  KEY `idx_kb_type` (`kb_type`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='AI知识库表';
```

### 10.8 AI知识库文档表（ai_knowledge_doc）

```sql
CREATE TABLE `ai_knowledge_doc` (
  `doc_id`            BIGINT UNSIGNED     NOT NULL AUTO_INCREMENT COMMENT '文档ID',
  `kb_id`             BIGINT UNSIGNED     NOT NULL                COMMENT '所属知识库ID',
  `doc_title`         VARCHAR(200)        NOT NULL                COMMENT '文档标题',
  `doc_content`       LONGTEXT            NOT NULL                COMMENT '文档内容（原始文本）',
  `doc_source`        VARCHAR(200)        DEFAULT NULL            COMMENT '文档来源',
  `doc_type`          VARCHAR(20)         DEFAULT 'text'          COMMENT '文档类型：text/pdf/doc/excel',
  `chunk_count`       INT                 DEFAULT 0               COMMENT '分块数量',
  `vector_status`     TINYINT             DEFAULT 0               COMMENT '向量化状态：0-未处理 1-处理中 2-已完成 3-失败',
  `version`           INT                 DEFAULT 1               COMMENT '版本号',
  `status`            TINYINT             NOT NULL DEFAULT 1      COMMENT '状态：0-禁用 1-启用',
  `create_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '创建人ID',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '更新人ID',
  `update_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`doc_id`),
  KEY `idx_kb_id` (`kb_id`),
  KEY `idx_vector_status` (`vector_status`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='AI知识库文档表';
```

### 10.9 AI问答会话表（ai_chat_session）

```sql
CREATE TABLE `ai_chat_session` (
  `session_id`        VARCHAR(64)         NOT NULL                COMMENT '会话ID',
  `user_id`           BIGINT UNSIGNED     NOT NULL                COMMENT '用户ID',
  `session_title`     VARCHAR(200)        DEFAULT NULL            COMMENT '会话标题（自动摘要）',
  `kb_type`           VARCHAR(20)         DEFAULT 'all'           COMMENT '关联知识库类型',
  `message_count`     INT                 DEFAULT 0               COMMENT '消息数量',
  `status`            TINYINT             NOT NULL DEFAULT 1      COMMENT '状态：0-已结束 1-进行中',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`session_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_status` (`status`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='AI问答会话表';
```

### 10.10 AI问答消息表（ai_chat_message）

```sql
CREATE TABLE `ai_chat_message` (
  `message_id`        BIGINT UNSIGNED     NOT NULL AUTO_INCREMENT COMMENT '消息ID',
  `session_id`        VARCHAR(64)         NOT NULL                COMMENT '会话ID',
  `role`              VARCHAR(20)         NOT NULL                COMMENT '角色：user/assistant/system',
  `content`           LONGTEXT            NOT NULL                COMMENT '消息内容',
  `sources`           JSON                DEFAULT NULL            COMMENT '引用来源（JSON数组）',
  `confidence`        DECIMAL(3,2)        DEFAULT NULL            COMMENT '置信度',
  `tokens_used`       INT                 DEFAULT NULL            COMMENT 'Token消耗量',
  `model_name`        VARCHAR(50)         DEFAULT NULL            COMMENT '使用的模型',
  `response_time`     INT                 DEFAULT NULL            COMMENT '响应时间（毫秒）',
  `status`            TINYINT             NOT NULL DEFAULT 1      COMMENT '状态：0-失败 1-成功',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`message_id`),
  KEY `idx_session_id` (`session_id`),
  KEY `idx_role` (`role`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='AI问答消息表';
```

### 10.11 AI审批建议表（ai_approve_suggest）

```sql
CREATE TABLE `ai_approve_suggest` (
  `suggest_id`        BIGINT UNSIGNED     NOT NULL AUTO_INCREMENT COMMENT '建议ID',
  `biz_type`          VARCHAR(50)         NOT NULL                COMMENT '业务类型：product/purchase/apply',
  `biz_id`            BIGINT UNSIGNED     NOT NULL                COMMENT '业务ID',
  `approve_type`      VARCHAR(50)         NOT NULL                COMMENT '审批类型',
  `risk_score`        DECIMAL(3,2)        DEFAULT NULL            COMMENT '风险评分（0-1）',
  `suggest_result`    VARCHAR(20)         DEFAULT NULL            COMMENT '建议结果：approve/reject/return',
  `suggest_reason`    TEXT                DEFAULT NULL            COMMENT '建议理由',
  `similar_cases`     JSON                DEFAULT NULL            COMMENT '相似案例（JSON）',
  `model_name`        VARCHAR(50)         DEFAULT NULL            COMMENT '使用的模型',
  `status`            TINYINT             NOT NULL DEFAULT 1      COMMENT '状态：0-失效 1-有效',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`suggest_id`),
  KEY `idx_biz_type_id` (`biz_type`, `biz_id`),
  KEY `idx_approve_type` (`approve_type`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='AI审批建议表';

### 10.12 数据仓库ETL任务表（dw_etl_task）

```sql
CREATE TABLE `dw_etl_task` (
  `task_id`           BIGINT UNSIGNED     NOT NULL AUTO_INCREMENT COMMENT '任务ID',
  `task_name`         VARCHAR(100)        NOT NULL                COMMENT '任务名称',
  `task_code`         VARCHAR(50)         NOT NULL                COMMENT '任务编码',
  `task_type`         VARCHAR(20)         NOT NULL                COMMENT '任务类型：full/inc/realtime',
  `source_table`      VARCHAR(200)        NOT NULL                COMMENT '源数据表',
  `target_table`      VARCHAR(200)        NOT NULL                COMMENT '目标数据表',
  `transform_rules`   JSON                DEFAULT NULL            COMMENT '转换规则（JSON）',
  `cron_expression`   VARCHAR(50)         DEFAULT NULL            COMMENT '定时表达式',
  `last_execute_time` DATETIME            DEFAULT NULL            COMMENT '上次执行时间',
  `last_status`       TINYINT             DEFAULT NULL            COMMENT '上次执行状态：0-失败 1-成功',
  `last_error_msg`    TEXT                DEFAULT NULL            COMMENT '上次错误信息',
  `status`            TINYINT             NOT NULL DEFAULT 1      COMMENT '状态：0-禁用 1-启用',
  `create_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '创建人ID',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '更新人ID',
  `update_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`task_id`),
  UNIQUE KEY `uk_task_code` (`task_code`),
  KEY `idx_task_type` (`task_type`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='数据仓库ETL任务表';
```

### 10.13 数据集市主题表（dw_subject）

```sql
CREATE TABLE `dw_subject` (
  `subject_id`        BIGINT UNSIGNED     NOT NULL AUTO_INCREMENT COMMENT '主题ID',
  `subject_name`      VARCHAR(100)        NOT NULL                COMMENT '主题名称',
  `subject_code`      VARCHAR(50)         NOT NULL                COMMENT '主题编码',
  `subject_type`      VARCHAR(20)         NOT NULL                COMMENT '主题类型：inventory/purchase/consumption/finance',
  `description`       VARCHAR(500)        DEFAULT NULL            COMMENT '主题描述',
  `fact_table`        VARCHAR(100)        NOT NULL                COMMENT '事实表名',
  `dimension_tables`  JSON                DEFAULT NULL            COMMENT '维度表列表（JSON）',
  `metrics`           JSON                DEFAULT NULL            COMMENT '指标定义（JSON）',
  `status`            TINYINT             NOT NULL DEFAULT 1      COMMENT '状态：0-禁用 1-启用',
  `create_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '创建人ID',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '更新人ID',
  `update_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`subject_id`),
  UNIQUE KEY `uk_subject_code` (`subject_code`),
  KEY `idx_subject_type` (`subject_type`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='数据集市主题表';
```

### 10.14 仪表盘配置表（dashboard_config）

```sql
CREATE TABLE `dashboard_config` (
  `dashboard_id`      BIGINT UNSIGNED     NOT NULL AUTO_INCREMENT COMMENT '仪表盘ID',
  `dashboard_name`    VARCHAR(100)        NOT NULL                COMMENT '仪表盘名称',
  `dashboard_type`    VARCHAR(20)         NOT NULL                COMMENT '类型：dashboard/screen/mobile',
  `layout_config`     JSON                NOT NULL                COMMENT '布局配置（JSON）',
  `widgets`           JSON                NOT NULL                COMMENT '组件列表（JSON）',
  `data_sources`      JSON                DEFAULT NULL            COMMENT '数据源配置（JSON）',
  `refresh_interval`  INT                 DEFAULT 300             COMMENT '自动刷新间隔（秒）',
  `is_default`        TINYINT             DEFAULT 0               COMMENT '是否默认仪表盘：0-否 1-是',
  `status`            TINYINT             NOT NULL DEFAULT 1      COMMENT '状态：0-禁用 1-启用',
  `create_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '创建人ID',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_by`         BIGINT UNSIGNED     DEFAULT NULL            COMMENT '更新人ID',
  `update_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`dashboard_id`),
  KEY `idx_dashboard_type` (`dashboard_type`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='仪表盘配置表';
```

### 10.15 数据质量监控表（dw_quality_log）

```sql
CREATE TABLE `dw_quality_log` (
  `log_id`            BIGINT UNSIGNED     NOT NULL AUTO_INCREMENT COMMENT '日志ID',
  `check_date`        DATE                NOT NULL                COMMENT '检查日期',
  `table_name`        VARCHAR(100)        NOT NULL                COMMENT '表名',
  `check_item`        VARCHAR(100)        NOT NULL                COMMENT '检查项',
  `check_type`        VARCHAR(20)         NOT NULL                COMMENT '检查类型：completeness/accuracy/consistency/timeliness',
  `check_result`      TINYINT             NOT NULL                COMMENT '检查结果：0-异常 1-正常',
  `error_count`       INT                 DEFAULT 0               COMMENT '异常数据量',
  `error_samples`     JSON                DEFAULT NULL            COMMENT '异常样本（JSON）',
  `threshold_value`   DECIMAL(5,2)        DEFAULT NULL            COMMENT '阈值',
  `actual_value`      DECIMAL(5,2)        DEFAULT NULL            COMMENT '实际值',
  `create_time`       DATETIME            NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`log_id`),
  KEY `idx_check_date` (`check_date`),
  KEY `idx_table_name` (`table_name`),
  KEY `idx_check_result` (`check_result`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='数据质量监控表';
```

### 10.16 审计整改数据库落地清单

当前数据库交付基线以 `docs/db/` 下 DDL 为准。以下对象为 PRD 审计整改后必须在 DDL、ER 关系、表清单和迁移脚本中同步校验或补齐的业务能力。

| 能力域 | 数据对象/字段 | 落地要求 |
|--------|---------------|----------|
| 系统批次 | 系统批次号、验收单号、商品目录主键、生产批号、效期、批次单价、所有权、结算模式 | 支持按验收审核日期生成批次，固化价格和所有权快照 |
| 批次调价 | 批次调价单、价格版本、影响清单 | 记录原单价、新单价、审批、影响库存和未结算事实范围 |
| 批号效期调整 | 调整单、原批号/效期、新批号/效期、影响范围 | 支持全量追溯属性更正，保留旧值用于召回双向检索 |
| 缺货登记 | 缺货类型、缺口数量、已满足数量、紧急等级、登记时间、关闭原因 | 支持部分满足、自动关闭、人工带原因关闭 |
| 补货任务 | 任务来源、合并关系、未满足缺口、锁定状态 | 支持建议/正式任务、部分满足、缺货登记关联 |
| 定数包标签 | 标签号、打印状态、打印次数、补打原因、来源系统批次明细 | 支持待打印/待贴标、原标签补打、来源追溯 |
| 未确认打包任务 | 预占散货明细、任务状态、超时策略、重算记录 | 支持预占、取消释放、调价/盘亏/召回触发重算 |
| 解包回散货 | 原标签、解包原因、当前库房/货位、恢复散货明细、新包关联 | 不产生消耗和结算，原标签作废 |
| PDA 离线补传 | 设备、离线时长、待上传数量、补传批次、处理结果、冲突状态 | 上传前不入正式账，按处理时间入账 |
| 配置中心 | 配置类型、适用范围、对象、风险等级、生效时间、来源说明 | 支持冲突检测、实时/定时生效、命中说明 |
| 盘亏责任 | 责任科室、保管人、原因、有效批次单价、损失金额 | 盘亏计入科室成本/绩效，按所有权决定供应商结算/赔付 |

---

## 11. 通用规范

### 11.1 响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 11.2 通用错误码

| code | 说明 |
|------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 认证失败 |
| 403 | 无权限访问 |
| 404 | 资源不存在 |
| 429 | 请求过于频繁 |
| 500 | 服务器内部错误 |
| 503 | 服务暂时不可用 |

### 11.3 编码规范

| 编码对象 | 规则 | 示例 |
|----------|------|------|
| 商品编码 | PRD + 6位序号 | PRD000001 |
| 供应商编码 | SUP + 3位序号 | SUP001 |
| 厂家编码 | MFR + 3位序号 | MFR001 |
| 采购订单 | CG + YYYYMMDD + 3位序号 | CG20260427001 |
| 入库单 | RK + YYYYMMDD + 3位序号 | RK20260427001 |
| 出库单 | CK + YYYYMMDD + 3位序号 | CK20260427001 |
| 申领单 | SL + YYYYMMDD + 3位序号 | SL20260427001 |
| 结算单 | JS + YYYYMMDD + 3位序号 | JS20260427001 |
| 采购计划 | JH + YYYYMMDD + 3位序号 | JH20260427001 |
| 审批申请 | SP + YYYYMMDD + 3位序号 | SP20260427001 |

### 11.4 状态颜色规范

| 用途 | 色值 |
|------|------|
| 页面背景 | #f5f7fa |
| 卡片背景 | #ffffff |
| 主按钮 | #00d4aa |
| 危险操作 | #f56c6c |
| 启用状态 | #67c23a |
| 停用状态 | #909399 |
| 效期正常 | #67c23a |
| 效期预警 | #e6a23c |
| 效期过期 | #f56c6c |
| 高值耗材标识 | #f56c6c |
| 冷链标识 | #409eff |

### 11.5 相关文档索引

| 文档名称 | 说明 |
|----------|------|
| 登录页面产品需求文档 | 登录页面详细设计 |
| 用户管理产品需求文档 | 用户管理模块详细设计 |
| 角色管理产品需求文档 | 角色权限模块详细设计 |
| 科室管理产品需求文档 | 科室管理模块详细设计 |
| 商品目录管理产品需求文档 | 商品目录模块详细设计 |
| 待审批商品目录管理产品需求文档 | 商品审批流程详细设计 |
| 供应商管理产品需求文档 | 供应商管理模块详细设计 |
| 厂家管理产品需求文档 | 厂家管理模块详细设计 |
| 库存管理产品需求文档 | 库存管理模块详细设计 |
| 采购管理产品需求文档 | 采购管理模块详细设计 |
| 科室申领产品需求文档 | 科室申领模块详细设计 |
| 结算对账产品需求文档 | 结算对账模块详细设计 |
| UDI追溯产品需求文档 | UDI追溯模块详细设计 |
| 高值耗材管理产品需求文档 | 高值耗材模块详细设计 |
| 冷链监控产品需求文档 | 冷链监控模块详细设计 |
| 报表中心产品需求文档 | 报表中心模块详细设计 |
| 数据统计分析产品需求文档 | 数据统计分析模块详细设计 |
| 数据统计展示产品需求文档 | 数据统计展示模块详细设计 |
| 系统配置产品需求文档 | 系统配置模块详细设计 |
| MySQL用户表设计文档 | 数据库详细设计 |
| 前端接口对接规范 | 前端开发规范 |
| 登录API接口规范 | 登录接口详细规范 |
| 附件管理API接口规范 | 附件接口详细规范 |
| 外网平台对接API规范 | 外网对接详细规范 |

---

## 12. 合规与扩展业务

> 本章在前 11 章基础上补全多角色二轮审计中识别的合规与扩展业务节点，覆盖多院区、招采合同、供应商资质、库存调拨/报损/盘亏盈、召回闭环、临床申领模板与植入物、财务医保 DRG-DIP、决策大屏与移动审批、系统集成（SSO/HIS/LIS/省国家平台）等 P0/P1 缺失项。

### 12.1 多院区与组织扩展

#### 12.1.1 功能概述

支持总院 + N 分院的多院区组织模型，满足跨院区数据隔离、统一总账、跨院区调拨、院领导合并报表等需求。

#### 12.1.2 多院区组织模型

```
集团总院
├── 院区A（总院）
│   ├── 中心库（一级库）
│   ├── 临床科室（二级库 × N）
│   └── 手术室（手术备货库）
├── 院区B（分院1）
│   ├── 中心库
│   └── 临床科室
└── 院区C（分院2）
    ├── 中心库
    └── 临床科室
```

| 维度 | 隔离规则 |
|------|----------|
| 商品目录 | 全集团共享，价格 / 准入按院区差异化 |
| 供应商 | 全集团共享，配送范围按院区授权 |
| 合同 | 院区独立或集团统一签约，价格生效范围明确 |
| 库存 | 院区独立，不可越级查看 |
| 用户 | 默认所属一个院区，可授权多院区 |
| 审批 | 院区内部闭环，跨院区调拨触发双院区审批 |
| 财务 | 按法人主体决定独立核算或集团合并 |

#### 12.1.3 院区基础信息维护

| 字段 | 必填 | 说明 |
|------|------|------|
| 院区编码 | 是 | YQ + 3 位 |
| 院区名称 | 是 | - |
| 院区类型 | 是 | 总院 / 分院 / 院外驻点 / 联盟成员 |
| 上级院区 | 否 | 形成树状结构 |
| 法人主体 | 是 | 独立法人 / 共用集团法人 |
| 财务核算 | 是 | 独立核算 / 集团核算 |
| 院区地址 | 是 | - |
| 启用状态 | 是 | 启用 / 停用 |
| 启用日期 | 是 | - |

#### 12.1.4 跨院区库存调拨

跨院区调拨与同院区库房间调拨（12.4.3）边界明确：跨院区涉及法人主体、票据流转，必须双院区审批。

| 字段 | 必填 | 说明 |
|------|------|------|
| 调拨单号 | 自动 | KQ + YYYYMMDD + 序号 |
| 调出院区 / 库房 | 是 | - |
| 调入院区 / 库房 | 是 | - |
| 调拨原因 | 是 | 紧急借调 / 库存均衡 / 集采分发 / 退回总院 |
| 调拨明细 | 是 | 商品、批号、数量、UDI |
| 价格处理 | 是 | 内部成本价 / 调拨价 / 不计价（同法人时） |
| 票据需求 | 是 | 是否需开内部调拨单 / 跨法人发票 |
| 物流方式 | 是 | 院区班车 / 第三方物流 / 自取 |

**审批流程：**
```
[调出院区运营] → [调出院区主任] → [调入院区运营] → [调入院区主任]
    → [集团总部审核（金额>阈值或跨法人）] → [出库] → [物流]
    → [调入院区收货] → [双方对账]
```

#### 12.1.5 院区数据隔离与共享规则

| 数据类型 | 默认范围 | 集团合并规则 |
|----------|----------|--------------|
| 库存 | 院区内 | 集团驾驶舱可查看汇总 |
| 消耗 | 院区内 | 月报合并按院区维度展示 |
| 合同 | 院区内或集团 | 集团合同对所有院区生效 |
| 报表 | 院区内 | 院领导可切换"本院/全集团" |
| 审计日志 | 全集团统一 | 按院区过滤查询 |
| 用户权限 | 院区内 | 可显式授权多院区 |

---

### 12.2 合同与招采管理

#### 12.2.1 功能概述

补全采购前置环节：招标、集采、询价比价、合同管理、价格对照与两票制流向追溯，与第 5.1 采购管理形成完整链条。

#### 12.2.2 功能架构

```
合同与招采管理
├── 招标管理
│   ├── 公开招标 / 邀请招标
│   ├── 竞争性谈判 / 竞争性磋商
│   └── 单一来源采购
├── 集采订单管理
│   ├── 国家集采
│   ├── 省级集采
│   └── GPO 联盟采购
├── 询价比价
├── 合同管理
│   ├── 框架协议 / 年度合同 / 项目合同 / 附加协议
│   ├── 价格变更
│   └── 合同到期预警
├── 集采价 / 挂网价对照
└── 两票制流向追溯
```

#### 12.2.3 招标管理

| 字段 | 必填 | 说明 |
|------|------|------|
| 招标编号 | 自动 | ZB + YYYYMMDD + 序号 |
| 招标类型 | 是 | 公开 / 邀请 / 竞争性谈判 / 竞争性磋商 / 单一来源 |
| 招标名称 | 是 | - |
| 标的范围 | 是 | 商品类别、品规清单 |
| 预算金额 | 是 | - |
| 报名截止时间 | 是 | - |
| 开标时间 | 是 | - |
| 评标专家 | 是 | 至少 5 人，奇数 |
| 评标办法 | 是 | 综合评分法 / 最低评标价法 / 性价比法 |
| 招标文件 | 是 | PDF 附件，需双方签章 |
| 中标结果 | 自动 | 中标供应商 / 中标价 / 中标公告 |
| 状态 | - | 草稿 / 公示中 / 评标中 / 已中标 / 已签约 / 流标 |

**招标流程：**
```
[需求科室申报] → [运营管理员立项] → [院级招标办审核]
    → [发布招标公告] → [供应商报名/递交标书]
    → [开标/评标（专家库抽取）] → [中标公示 N 天]
    → [异议处理] → [发中标通知书] → [合同签订]
    → [纳入采购目录]
```

#### 12.2.4 集采订单管理

针对国家 / 省级带量采购、GPO 联盟采购：

| 字段 | 必填 | 说明 |
|------|------|------|
| 集采批次号 | 是 | 国家或省级文件号 |
| 集采类型 | 是 | 国家集采 / 省级集采 / 跨省联盟 / GPO |
| 集采品种 | 是 | 关联商品目录 |
| 中标供应商 | 是 | 多家中标时按医院选择 |
| 中标价 | 是 | 系统强制使用，不可修改 |
| 协议采购量 | 是 | 医院上报量 |
| 协议周期 | 是 | 通常 1-3 年 |
| 完成进度 | 自动 | 已采购量 / 协议量 |
| 完成预警 | 自动 | 进度 < 80% 触发预警，距期末 < 90 天 |
| 替代品规则 | 是 | 集采产品缺货时是否允许替代 |

> 合同/集采价格作为商品目录采购单价维护和采购分析的参考依据。实际结算以验收入库生成的批次单价及已形成事实的原单价为准；未完成协议量到期前 90 天触发强制催采。

#### 12.2.5 询价比价

| 字段 | 必填 | 说明 |
|------|------|------|
| 询价单号 | 自动 | XJ + YYYYMMDD + 序号 |
| 询价品种 | 是 | 商品 + 规格 |
| 询价数量 | 是 | - |
| 邀请供应商 | 是 | 至少 3 家 |
| 报价截止时间 | 是 | - |
| 评比维度 | 是 | 价格 / 货期 / 售后 / 资质 / 历史合作 |
| 评分权重 | 是 | 各维度权重之和 = 100% |
| 比价结果 | 自动 | 推荐供应商 + 综合得分 + 理由 |
| 决策依据 | 是 | 文本 + 附件 |
| 留档期限 | 自动 | ≥ 5 年 |

#### 12.2.6 合同管理

| 合同类型 | 说明 |
|----------|------|
| 框架协议 | 长期合作基础合同，规定通用条款 |
| 年度合同 | 一年期采购合同，含品种、价格、量 |
| 项目合同 | 一次性大额项目（设备 / 试剂打包） |
| 附加协议 | 价格调整、品种增减等补充 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 合同编号 | 自动 | HT + YYYYMMDD + 序号 |
| 合同类型 | 是 | 见上 |
| 签约方 | 是 | 院方 / 供应商双方 |
| 签订日期 | 是 | - |
| 生效日期 | 是 | - |
| 失效日期 | 是 | - |
| 合同金额 | 是 | 含税金额 |
| 付款条款 | 是 | 月结 / 季结 / 货到付款 / 验收后 N 天 |
| 质保金 | 是 | 比例 / 期限 |
| 违约罚则 | 是 | 违约责任 |
| 关联招标 | 否 | 招标合同必填 |
| 关联集采 | 否 | 集采合同必填 |
| 适用院区 | 是 | 单院区 / 集团统一 |
| 状态 | - | 草稿 / 待签 / 生效 / 即将到期 / 已终止 / 已归档 |
| 合同文件 | 是 | 双方签章 PDF + 电子签章证书 |

#### 12.2.7 合同价格变更

| 场景 | 处理 |
|------|------|
| 集采降价 | 关联外网采购平台获取的集采目录，按目录更新时间与合同价格更新时间对比生成变价公告，经采购管理员确认后价格生效 |
| 协商调价 | 双方签附加协议，新价格生效需运营总监审批 |
| 招标重定 | 新招标中标后旧合同终止，新合同启用 |
| 应急涨价 | 原厂涨价文件 + 院领导特批 |

变更记录强制留痕，旧价格历史可追溯，对未付款订单按"订单日期价格"或"调整后价格"二选一处理。外网采购平台目录更新后，系统需按目录更新时间、物料编码、供应商、规格型号与合同明细进行匹配，对存在价格差异的合同形成待确认变价清单，并在确认后生成公告展示。

#### 12.2.8 合同到期预警

| 阶段 | 提前 N 天 | 通知对象 |
|------|-----------|----------|
| 一级提醒 | 90 天 | 采购管理员 |
| 二级提醒 | 60 天 | 采购管理员 + 运营总监 |
| 三级提醒 | 30 天 | 采购总监 + 院领导 |
| 紧急预警 | 7 天 | 全采购团队 + 强制日报 |

到期前未续签的合同自动进入"宽限期"（默认 15 天），宽限期内不允许新增订单，仅允许已下未收订单的尾款结算。

#### 12.2.9 集采价 / 挂网价对照

| 字段 | 说明 |
|------|------|
| 商品编码 | - |
| 集采价 | 国家 / 省级文件价 |
| 挂网价 | 阳光采购挂网价 |
| 中标价 | 院内招标价 |
| 历史成交价 | 最近 N 次实际成交均价 |
| 价格偏离预警 | 实际采购价高于挂网价 → 红色，高于集采价 → 严重红色 |
| 价格生效时间 | 起止时间 |

#### 12.2.10 两票制流向追溯

| 字段 | 必填 | 说明 |
|------|------|------|
| 一票（厂家→流通） | 是 | 厂家发票影像 / 编码 |
| 二票（流通→医院） | 是 | 流通商发票影像 / 编码 |
| 流通商资质 | 是 | 经营许可证、营业执照 |
| 流向匹配 | 自动 | 商品 / 批号 一票二票一致性校验 |
| 异常处理 | 是 | 流向断链触发隔离不入账 |
| 留档期限 | 自动 | ≥ 5 年 |

---

### 12.3 供应商扩展管理

#### 12.3.1 功能概述

补全供应商门户、绩效 KPI、资质证照预警、价格调整、客诉与回复等供应商全生命周期能力，与 4.3 供应商管理形成完整链条。

#### 12.3.2 功能架构

```
供应商扩展管理
├── 供应商门户（独立 Web 端）
├── 供应商绩效 KPI 评分
├── 资质证照预警
├── 价格调整申请
├── 客诉与回复
└── 黑名单管理
```

#### 12.3.3 供应商门户

外网独立 Web 端，供应商自助使用：

| 功能模块 | 说明 |
|----------|------|
| 商品准入申请 | 提交新品申请、查看审批进度（与 4.2 联动） |
| 订单管理 | 查看医院采购订单、确认接单、维护货期 |
| 配送管理 | 主动创建 B2B 配送单（区别于 8.3.3 院方确认侧）、上传物流单号 |
| 物流跟踪 | 实时定位、ETA 预测、异常上报 |
| 对账中心 | 双向对账、差异确认、电子对账单 |
| 发票管理 | 上传发票、查询认证状态、查询付款进度 |
| 资质中心 | 上传 / 续期资质证照、到期提醒 |
| 客诉中心 | 接收客诉、回复处理、上传整改报告 |
| 价格调整申请 | 申请涨价 / 降价 / 新增品规 |
| 公告通知 | 接收医院发布的公告、政策变更 |

**安全要求：**
- API Key + 签名认证（详见 9.3）
- 强制 IP 白名单
- 敏感操作（提交报价 / 发票）双因子认证
- 全部操作纳入审计日志

#### 12.3.4 供应商绩效 KPI 评分

| KPI 指标 | 权重 | 计算方式 |
|----------|------|----------|
| 到货及时率 | 25% | (实际及时到货数 / 应到货数) × 100% |
| 数量准确率 | 15% | (1 - 数量差异 / 应到数量) × 100% |
| 质量合格率 | 25% | (合格件数 / 总验收件数) × 100% |
| 退货率 | 10% | (退货金额 / 总采购金额) × 100% |
| 售后响应时间 | 10% | 平均响应小时数（越短越高分） |
| 资质合规性 | 10% | 证照齐全且未过期 = 100，缺失或过期 = 0 |
| 客诉处理及时率 | 5% | (按时回复客诉数 / 总客诉数) × 100% |
| 综合得分 | - | Σ(指标得分 × 权重) |

| 等级 | 得分区间 | 处理 |
|------|----------|------|
| A | ≥ 90 | 优先合作 / 续签优先 |
| B | 80–89 | 正常合作 |
| C | 70–79 | 警告 / 限期整改 |
| D | < 70 | 暂停合作 / 启动黑名单评估 |

**评分周期：** 月度滚动评分 + 季度复审 + 年度总评。

#### 12.3.5 资质证照预警

| 证照类型 | 必备 | 预警阈值 |
|----------|------|----------|
| 营业执照 | 是 | 到期前 90/60/30/7 天 |
| 医疗器械经营许可证 | 是 | 到期前 90/60/30/7 天 |
| 体外诊断试剂经营资质 | 试剂供应商必需 | 到期前 90/60/30/7 天 |
| GSP 认证证书 | 是 | 到期前 90/60/30/7 天 |
| 三证合一 / 五证合一 | 是 | - |
| 医疗器械注册证（按品种） | 是 | 到期前 180/90/30 天 |
| 生产许可证（厂家） | 是 | 到期前 180/90/30 天 |
| ISO 体系认证 | 否 | 到期前 90 天 |

**预警动作：**
- 一级预警 → 系统消息 + 邮件给供应商 + 站内提醒
- 二级预警 → 短信 + 邮件 + 系统消息
- 三级预警 → 暂停发起新订单
- 已过期 → 强制冻结，已生效订单走特批

#### 12.3.6 价格调整申请

| 字段 | 必填 | 说明 |
|------|------|------|
| 申请单号 | 自动 | TJ + YYYYMMDD + 序号 |
| 申请类型 | 是 | 涨价 / 降价 / 新增品规 / 停产替代 |
| 关联合同 | 是 | - |
| 调整商品 | 是 | 商品 + 规格 |
| 现行价 / 申请价 | 是 | - |
| 涨幅 / 降幅 | 自动 | (申请价 - 现行价) / 现行价 |
| 调整原因 | 是 | 文本 + 附件（原厂涨价函 / 集采降价文件） |
| 生效日期 | 是 | - |
| 状态 | - | 待审 / 通过 / 驳回 |

**审批分级：**
- 涨幅 ≤ 5% → 采购管理员
- 5% < 涨幅 ≤ 10% → 采购总监 + 财务
- 涨幅 > 10% → 院领导特批

#### 12.3.7 客诉与回复

| 字段 | 必填 | 说明 |
|------|------|------|
| 客诉单号 | 自动 | KS + YYYYMMDD + 序号 |
| 客诉来源 | 是 | 关联 8.6.5 意见反馈 / 8.6.6 不良事件 / 直接录入 |
| 涉事供应商 | 是 | - |
| 涉事商品 / 批号 | 是 | - |
| 客诉等级 | 是 | 一般 / 重要 / 紧急 |
| 处理时限 | 自动 | 一般 7 天 / 重要 3 天 / 紧急 24 小时 |
| 供应商回复 | 是 | 整改报告 + 附件 |
| 院方确认 | 是 | 已闭环 / 不接受 / 需复查 |
| 影响处理 | - | 是否扣 KPI / 是否进入黑名单 |

#### 12.3.8 黑名单管理

| 字段 | 必填 | 说明 |
|------|------|------|
| 黑名单编号 | 自动 | HM + YYYYMMDD + 序号 |
| 供应商 | 是 | - |
| 加入原因 | 是 | KPI 评级 D / 严重客诉 / 资质造假 / 商业贿赂 |
| 加入依据 | 是 | 文件 / 决议 + 附件 |
| 期限 | 是 | 一年 / 三年 / 永久 |
| 加入审批 | 是 | 院领导 + 法务 + 院级招标办 |
| 解除条件 | 否 | - |
| 关联院区 | 是 | 单院区 / 集团 |

> 黑名单生效后，所有采购、招标、合同、配送均自动屏蔽该供应商。

---

### 12.4 库存扩展业务

#### 12.4.1 功能概述

补全库房间调拨、货位移库、报损报废、盘亏盘盈调账、借货、临时入库、FIFO/FEFO 强制规则等关键库存动作，覆盖第 5.2 库存管理之外的全部库存调整场景。

#### 12.4.2 功能架构

```
库存扩展业务
├── 库房间调拨（同院区）
├── 货位移库 / 移位
├── 报损报废单
├── 盘亏盘盈调账单
├── 借货管理（借出 / 借入）
├── 临时入库
├── 库存调账单
└── FIFO / FEFO 强制规则
```

#### 12.4.3 库房间调拨

同院区不同库房间的物资调拨（中心库 ↔ 二级库 / 二级库 ↔ 二级库）。

| 字段 | 必填 | 说明 |
|------|------|------|
| 调拨单号 | 自动 | KB + YYYYMMDD + 序号 |
| 调出库房 | 是 | - |
| 调入库房 | 是 | - |
| 调拨类型 | 是 | 库存均衡 / 应急借调 / 业务转移 / 错发纠正 |
| 调拨明细 | 是 | 商品、批号、数量、UDI |
| 调拨理由 | 是 | 文本 |
| 价格处理 | 自动 | 同院区不计价（成本价同步） |
| 状态 | - | 草稿 / 待出库 / 在途 / 已收货 / 已确认 |

**审批：** 5 万以下库管主任审批；5–20 万运营主任 + 财务；20 万以上院领导。

#### 12.4.4 货位移库 / 移位

同库房内物资从一个货位移动到另一个货位，不涉及科室 / 库房变化。

| 字段 | 必填 | 说明 |
|------|------|------|
| 移位单号 | 自动 | YW + YYYYMMDD + 序号 |
| 库房 | 是 | - |
| 原货位 | 是 | - |
| 目标货位 | 是 | - |
| 移位明细 | 是 | 商品、批号、数量 |
| 移位原因 | 是 | 库位优化 / 货位空间 / 临近过期前置 |
| 操作人 | 是 | - |

> 移位不影响库存数量，仅更新货位字段，但纳入审计日志。PDA 端支持扫"原货位"→ 扫"商品"→ 扫"目标货位"三步完成。

#### 12.4.5 报损报废单

针对实物损耗（破损 / 过期 / 污染 / 霉变 / 召回销毁等），需独立审批流：

| 字段 | 必填 | 说明 |
|------|------|------|
| 报损单号 | 自动 | BS + YYYYMMDD + 序号 |
| 报损库房 | 是 | - |
| 报损类型 | 是 | 破损 / 过期 / 污染 / 召回销毁 / 损耗 / 检验失败 |
| 报损明细 | 是 | 商品、批号、UDI、数量、原值 |
| 责任认定 | 是 | 厂家责任 / 物流责任 / 院方责任 / 不可抗力 |
| 处置方式 | 是 | 销毁 / 退供应商 / 报废回收 |
| 现场照片 | 是 | 必传 ≥ 3 张 |
| 涉及金额 | 自动 | Σ(数量 × 成本价) |

**审批分级：**
- ≤ 1 千 → 库管主任
- 1 千–1 万 → 运营主任 + 财务
- > 1 万 → 院领导 + 财务总监 + 质控主任

**后置动作：** 自动生成红字凭证 → 减少库存 → 财务核销 → 实物销毁需视频留档。

#### 12.4.6 盘亏盘盈调账单

| 触发 | 说明 |
|------|------|
| 盘点差异 | 5.2 库存盘点 / 8.5.9 新盘点单 完成后差异行 |
| 长期未匹配 | 系统库存与实物长期不一致 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 调账单号 | 自动 | TZ + YYYYMMDD + 序号 |
| 关联盘点单 | 是 | - |
| 差异类型 | 是 | 盘亏 / 盘盈 |
| 差异明细 | 是 | 商品、批号、账面 / 实盘 / 差异 |
| 差异原因 | 是 | 计量误差 / 错收发 / 失窃 / 错领 / 其他 |
| 责任人 | 是 | 必填 |
| 处理建议 | 是 | 直接调账 / 追责赔偿 / 报警立案 |
| 调账金额 | 自动 | Σ(差异数量 × 成本价) |

**审批分级：**
- 单笔差异 ≤ 5 千 → 运营主任 + 财务
- 5 千–5 万 → 运营总监 + 财务总监 + 审计
- > 5 万 → 院领导 + 法务 + 审计 + 必要时报警

#### 12.4.7 借货管理

应急场景下不通过正式申领 / 调拨流程，先借出后补单。

| 借货类型 | 说明 |
|----------|------|
| 院内借出 | 本院某库房应急借给其他库房 |
| 兄弟医院借出 | 借给兄弟单位（罕见情况） |
| 兄弟医院借入 | 从兄弟单位借入 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 借货单号 | 自动 | JH + YYYYMMDD + 序号 |
| 借货类型 | 是 | 见上 |
| 借出 / 借入方 | 是 | - |
| 借货明细 | 是 | 商品、批号、数量、UDI |
| 借期 | 是 | 必填，逾期触发预警 |
| 还货方式 | 是 | 同品规归还 / 等价折现 |
| 状态 | - | 已借出 / 已部分归还 / 已归还 / 已折现 / 逾期 |

> 院内借出的库存数量不直接扣减，而进入"借出冻结"状态，归还时解冻；兄弟医院借入须经法务备案。

#### 12.4.8 临时入库

无单到货应急（如供应商先送货后补单、紧急配送、捐赠物资）：

| 字段 | 必填 | 说明 |
|------|------|------|
| 临时入库单号 | 自动 | LR + YYYYMMDD + 序号 |
| 入库类型 | 是 | 无单应急 / 捐赠 / 援助物资 / 退还借货 |
| 入库库房 | 是 | - |
| 入库明细 | 是 | 商品、批号、数量 |
| 来源说明 | 是 | 必填 |
| 限期补单 | 是 | 默认 7 天内必须补正式入库单 |
| 库存状态 | 自动 | 隔离库存（不可出库），补单后转可用 |

**强制约束：** 限期未补单的临时入库自动通知运营总监 + 库管主任，超 30 天自动转报损。

#### 12.4.9 FIFO / FEFO 强制规则

| 规则 | 说明 |
|------|------|
| FIFO（先进先出） | 默认拣货顺序按入库时间升序 |
| FEFO（近效期先出） | 高值耗材、试剂、冷链耗材必须 FEFO |
| 强制规则 | 拣配作业（8.4.3）/ 出库 / 申领出库均按规则自动选批 |
| 例外审批 | 跳过近效期批号需 PDA 上提示 + 库管主任审批 |
| 异常监控 | 系统记录所有 FIFO/FEFO 例外，月度报表 |

| 商品类别 | 默认规则 | 可调整 |
|----------|----------|--------|
| 普通耗材 | FIFO | 可改 FEFO |
| 高值耗材 | FEFO | 不可改 |
| 试剂 | FEFO | 不可改 |
| 冷链耗材 | FEFO | 不可改 |
| 设备耗材 | FIFO | 可改 |

---

### 12.5 召回与质量闭环

#### 12.5.1 功能概述

补全召回完整闭环（厂家通知 → 库存筛查 → 已用追踪 → 患者通知 → 处置）、不良事件后续追踪、批号偏差处理、临床应用风险评估等质量合规节点。

#### 12.5.2 功能架构

```
召回与质量闭环
├── 召回管理（完整闭环）
│   ├── 召回通知接收
│   ├── 库存全院筛查
│   ├── 已使用追踪
│   ├── 患者通知
│   └── 处置闭环
├── 不良事件后续追踪
├── 批号偏差处理
├── 临床应用风险评估
└── 法规符合性 Checklist
```

#### 12.5.3 召回管理（完整闭环）

| 阶段 | 操作 |
|------|------|
| 1. 召回通知接收 | 厂家通过供应商门户提交召回函 / 国家不良事件中心推送 / 手工录入 |
| 2. 自动筛查 | 系统按"商品 + 批号 + UDI"全集团库存筛查（含中心库 / 二级库 / 隔离库 / 借出冻结） |
| 3. 库存隔离 | 命中库存自动转入"召回隔离库"，禁止任何出库 |
| 4. 已使用追踪 | 通过 6.1 UDI 追溯链，定位已发出 / 已消耗 / 已植入患者 |
| 5. 患者通知 | 高值植入耗材：自动列出涉事患者清单，推送至 HIS 患者档案 + 主管医生 + 医务部 |
| 6. 处置 | 退供应商 / 销毁 / 替换重发 |
| 7. 闭环报告 | 生成召回总结报告，上报国家不良事件监测中心 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 召回单号 | 自动 | ZH + YYYYMMDD + 序号 |
| 召回类型 | 是 | 主动召回 / 被动召回（监管要求）|
| 召回级别 | 是 | 一级（致死/重伤）/ 二级（暂时性损害）/ 三级（不太可能造成损害） |
| 召回原因 | 是 | 文本 + 召回函附件 |
| 涉事商品 / 批号 / UDI | 是 | 多行 |
| 全院命中库存 | 自动 | 自动汇总 |
| 已发出未使用 | 自动 | 二级库 / 借出冻结 |
| 已使用已植入 | 自动 | UDI 追溯命中患者数 |
| 处置方式 | 是 | 退供应商 / 销毁 / 替换重发 |
| 上报状态 | 是 | 已上报国家中心 / 待上报 / 院内闭环 |
| 状态 | - | 草稿 / 处理中 / 已闭环 / 已上报 |

**响应时限：**
- 一级召回：4 小时内库存隔离 + 24 小时内患者通知
- 二级召回：24 小时内库存隔离 + 72 小时内患者通知
- 三级召回：72 小时内库存隔离 + 7 天内患者通知

#### 12.5.4 不良事件后续追踪闭环

补全 8.6.6 的后续节点：

| 阶段 | 说明 |
|------|------|
| 1. 上报后跟踪 | 国家平台反馈编号、处理状态自动同步 |
| 2. 厂家整改通知 | 厂家提交整改报告（通过供应商门户）|
| 3. 院内复评 | 质控部门评估整改有效性 |
| 4. 是否触发召回 | 严重事件升级为 12.5.3 召回 |
| 5. 患者随访 | 高值耗材植入患者强制随访 |
| 6. 闭环归档 | 永久留档 ≥ 10 年 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 跟踪单号 | 自动 | GZ + 不良事件编号 |
| 国家平台反馈号 | 否 | 上报后回填 |
| 厂家整改报告 | 是 | 上传 PDF |
| 院内复评结果 | 是 | 已闭环 / 不接受 / 需进一步整改 |
| 是否升级召回 | 是 | 是 / 否 |
| 随访记录 | 否 | 患者随访明细 |

#### 12.5.5 批号偏差处理

| 场景 | 处理 |
|------|------|
| 不同批号合并使用 | 默认禁止；如必须合并须批号偏差申请，运营 + 质控双签 |
| 批号信息异常（错印 / 模糊） | 入库强制扫码识别失败 → 转隔离 → 厂家确认后释放 |
| 批号变更（厂家通知） | 旧批号库存自动标记，新批号入库 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 偏差单号 | 自动 | PC + YYYYMMDD + 序号 |
| 商品 + 批号 | 是 | - |
| 偏差类型 | 是 | 见上 |
| 偏差描述 | 是 | 文本 |
| 处理建议 | 是 | 隔离 / 退供应商 / 释放使用 |
| 审批 | 是 | 库管主任 + 质控员 |

#### 12.5.6 临床应用风险评估

针对高风险耗材（高值植入物 / 进口耗材 / 新准入耗材）的入院前评估：

| 评估维度 | 权重 | 内容 |
|----------|------|------|
| 临床有效性 | 30% | 文献证据、临床试验数据 |
| 安全性 | 30% | 不良事件历史、不良反应率 |
| 经济性 | 20% | 性价比 / 医保覆盖 |
| 替代性 | 10% | 是否有同类产品 |
| 合规性 | 10% | 注册证、生产许可、监管历史 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 评估单号 | 自动 | PG + YYYYMMDD + 序号 |
| 商品 | 是 | - |
| 申请科室 | 是 | - |
| 评估专家 | 是 | 临床 + 药学 + 设备 + 院感 ≥ 5 人 |
| 评估结果 | 是 | 通过准入 / 限制使用 / 不予准入 |
| 限制条件 | 否 | 仅特定科室 / 仅特定病种 / 用量上限 |
| 复评周期 | 是 | 一年 / 三年 / 永久 |

#### 12.5.7 法规符合性 Checklist

| 法规 | 适用 | 检查项 |
|------|------|--------|
| 医疗器械监督管理条例 | 全部耗材 | 注册证、经营许可、生产许可 |
| UDI 实施细则 | 国家三级 / 二级 UDI 强制品种 | UDI 完整性、唯一性、可追溯性 |
| 医疗器械经营质量管理规范 / GSP 类质量管理要求 | 试剂 / 高值耗材 / 冷链耗材 | 储存条件、流通规范 |
| 医疗器械不良事件监测和再评价管理办法 | 全部耗材 | 上报时限、记录留档 |
| 医疗器械召回管理办法 | 全部耗材 | 召回响应时限、闭环要求 |
| 医疗保障基金使用监督管理条例 | 医保耗材 | 医保规则符合性、计费合规 |
| 医院感染管理办法 | 一次性 / 介入耗材 | 院感控制流程 |
| 个人信息保护法 / HIPAA 类要求 | 患者数据 | 脱敏、最小必要、留痕 |

> 系统在每个相关业务节点自动校验对应法规要求，未通过强制阻断 + 提示。

---

### 12.6 临床扩展业务

#### 12.6.1 功能概述

补全申领模板、紧急申领、跨科室借用、病区流转、患者押金、CSSD 对接、植入物专项档案等临床端节点，与 5.3 科室申领、8.6 科室业务、8.8 手术临床形成完整链路。

#### 12.6.2 功能架构

```
临床扩展业务
├── 申领模板 / 套餐
├── 紧急申领绿色通道
├── 跨科室借用
├── 病区流转管理（科室二级库 → 病区 → 床头柜）
├── 患者押金管理
├── CSSD 消毒供应中心对接
└── 植入物专项档案
```

#### 12.6.3 申领模板 / 套餐

科室常用申领套餐，一键带出明细：

| 字段 | 必填 | 说明 |
|------|------|------|
| 模板编号 | 自动 | MB + YYYYMMDD + 序号 |
| 模板名称 | 是 | 如"ICU 日常套餐"、"手术室基础包" |
| 适用科室 | 是 | 多选 |
| 模板类型 | 是 | 周常套餐 / 月常套餐 / 紧急套餐 / 项目套餐 |
| 模板明细 | 是 | 商品、规格、默认数量 |
| 周期 | 否 | 周 / 月 / 季 |
| 自动触发 | 否 | 是否到期自动生成申领 |
| 状态 | - | 启用 / 停用 |

> 申领单创建时支持"引用模板"，自动展开为多行明细，可调整数量。

#### 12.6.4 紧急申领绿色通道

| 场景 | 说明 |
|------|------|
| 急救场景 | 如心梗、脑卒中等抢救物资 |
| 突发公共卫生事件 | 大规模急需 |
| 手术中临时缺货 | 手术中追加 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 申领单号 | 自动 | JJ + YYYYMMDD + 序号 |
| 紧急级别 | 是 | 一级（生命危急）/ 二级（病情紧急）/ 三级（业务紧急） |
| 患者信息 | 是 | 姓名 / 住院号 / 病情简述 |
| 申领明细 | 是 | 商品、数量 |
| 主管医生 | 是 | 必填 |
| 状态 | - | 已发起 / 处理中 / 已发出 / 已签收 |

**响应时限：**
- 一级 → 5 分钟内出库 + 30 分钟内送达
- 二级 → 30 分钟内出库 + 1 小时内送达
- 三级 → 2 小时内出库 + 4 小时内送达

> 紧急申领可"先发后审"，系统自动通知科室主任 + 中心库主任 + 运营管理员，事后 24 小时内补审批。

#### 12.6.5 跨科室借用

A 科室向 B 科室借用耗材（区别于 12.4.7 库房间借货）：

| 字段 | 必填 | 说明 |
|------|------|------|
| 借用单号 | 自动 | KS + YYYYMMDD + 序号 |
| 借出科室 | 是 | - |
| 借入科室 | 是 | - |
| 借用类型 | 是 | 应急 / 计划内 / 错领纠正 |
| 借用明细 | 是 | 商品、批号、数量、UDI |
| 借期 | 是 | 默认 7 天，紧急时 24 小时 |
| 还货方式 | 是 | 同品规归还 / 由中心库补发 |
| 状态 | - | 已发起 / 已确认 / 已交付 / 已归还 / 已折现 |

**审批：** 借出科室主任同意 → 借入科室主任同意 → 自动生成两科室间的临时调拨。

#### 12.6.6 病区流转管理

科室二级库 → 病区 / 楼层 → 床头柜的多级流转：

| 流转层级 | 库存可见性 | 备注 |
|----------|------------|------|
| 科室二级库 | 库管员 / 科室主任 | 总账 |
| 病区分库 | 病区护士长 | 按病区拆分 |
| 床头柜 | 责任护士 | 按床位定数包 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 流转单号 | 自动 | LZ + YYYYMMDD + 序号 |
| 流转方向 | 是 | 二级库 → 病区 / 病区 → 床头柜 |
| 流转明细 | 是 | 商品、批号、数量 |
| 责任人 | 是 | 接收人 |
| 床头柜定数包 | 否 | 床位号、定数包码 |

> 床头柜定数包扫码使用即消耗（实领实消模式），与 8.6.3 科室消耗联动。

#### 12.6.7 患者押金管理

针对高值耗材（如心脏支架、人工关节）使用前的押金管理：

| 字段 | 必填 | 说明 |
|------|------|------|
| 押金单号 | 自动 | YJ + YYYYMMDD + 序号 |
| 患者信息 | 是 | 姓名、住院号 |
| 关联耗材 | 是 | 商品、UDI、单价 |
| 押金金额 | 是 | 默认 = 耗材定价 × 押金比例（如 50%） |
| 缴纳方式 | 是 | 现金 / 银行卡 / HIS 划账 |
| 缴纳时间 | 是 | - |
| 状态 | - | 已缴纳 / 已使用冲销 / 已退还 / 已没收 |

**冲销规则：**
- 耗材成功植入 → 押金自动转为耗材货款
- 取消手术未使用 → 押金全额退还
- 损坏丢失 → 没收押金，开正式收据

#### 12.6.8 CSSD 消毒供应中心对接

复用器械（含植入器械、手术器械包）的消毒回收周转：

| 业务对象 | 说明 |
|----------|------|
| 复用器械 | 手术器械包、骨科钢板器械、内镜等 |
| CSSD 流转 | 使用 → 回收 → 清洗 → 消毒灭菌 → 包装 → 发放 → 使用 |

| 字段 | 必填 | 说明 |
|------|------|------|
| CSSD 单号 | 自动 | CS + YYYYMMDD + 序号 |
| 器械包编号 | 是 | 唯一码 |
| 器械明细 | 是 | 含件数清点 |
| 流转环节 | 是 | 回收 / 清洗 / 消毒 / 包装 / 发放 |
| 操作人 | 是 | 各环节责任人 |
| 灭菌方式 | 是 | 高温高压 / 环氧乙烷 / 等离子 |
| 灭菌指标 | 是 | 物理 / 化学 / 生物三重指标 |
| 灭菌结果 | 是 | 合格 / 不合格 |
| 失效日期 | 自动 | 按灭菌方式计算 |

**对接接口：** SPD ↔ CSSD 系统通过标准接口同步器械流转 + 失效预警，确保器械安全可追溯。

#### 12.6.9 植入物专项档案

永久植入耗材（如心脏起搏器、人工关节、骨科钢板）的终身追溯：

| 字段 | 必填 | 说明 |
|------|------|------|
| 档案编号 | 自动 | ZR + 患者ID + UDI |
| 患者信息 | 是 | 姓名、住院号、身份证、联系方式 |
| 植入耗材 | 是 | 商品、UDI、批号、生产厂家、注册证 |
| 植入手术 | 是 | 关联手术单 |
| 植入医生 | 是 | 主刀 + 助手 |
| 植入部位 | 是 | 详细部位描述 |
| 植入日期 | 是 | - |
| 预期使用年限 | 是 | 厂家标定 |
| 随访计划 | 自动 | 1 月 / 3 月 / 6 月 / 1 年 / 每年 |
| 随访记录 | - | 随访结果（可通过 HIS 同步） |
| 召回响应 | 自动 | 关联 12.5.3 召回，命中自动通知 |
| 取出 / 失效 | 否 | 取出日期、原因 |

**留档期限：** 永久（≥ 患者生命周期）。

**患者出院后：** 自动生成患者卡（PDF + 电子卡），含 UDI、保修期、随访建议。

---

### 12.7 财务与医保扩展

#### 12.7.1 功能概述

补全多账套、预算管理、成本中心、保证金质保金、月结反结、DRG / DIP 病种付费、医保目录、医保驳回处理等财务与医保闭环节点，与 5.4 结算对账、8.9 结算与发票形成完整链条。

#### 12.7.2 功能架构

```
财务与医保扩展
├── 多账套
├── 预算管理（科室年度耗材预算）
├── 成本中心 / 成本归集
├── 保证金 / 备货金 / 质保金
├── 月结 / 反结 / 结账锁定
├── DRG / DIP 病种付费
├── 医保目录管理
└── 医保驳回处理
```

#### 12.7.3 多账套

| 字段 | 必填 | 说明 |
|------|------|------|
| 账套编码 | 自动 | ZT + 3 位 |
| 账套名称 | 是 | 如"总院账套"、"分院 A 账套"、"医保账套" |
| 账套类型 | 是 | 法人主体 / 业务模块 / 合并报表 |
| 关联院区 | 是 | 单个或多个 |
| 关联科室 | 否 | 仅本账套核算的科室 |
| 启用日期 | 是 | - |
| 财务系统对接 | 是 | 金蝶 / 用友 / SAP / 自有 |
| 默认结算周期 | 是 | 月 / 季 / 年 |

> 多账套间允许内部往来调拨，自动生成往来凭证。

#### 12.7.4 预算管理

科室年度耗材预算控制：

| 字段 | 必填 | 说明 |
|------|------|------|
| 预算编号 | 自动 | YS + YYYY + 序号 |
| 预算年度 | 是 | - |
| 科室 | 是 | - |
| 预算类型 | 是 | 总额预算 / 品类预算 / 月度预算 |
| 预算金额 | 是 | - |
| 已使用金额 | 自动 | 实时统计消耗 |
| 已锁定金额 | 自动 | 已下单未到货 |
| 可用金额 | 自动 | 预算 - 已使用 - 已锁定 |
| 调整记录 | - | 中途追加 / 削减 |

**预警阈值：**
- 使用率 70% → 提醒科室主任
- 使用率 90% → 强制审批通过运营总监
- 使用率 100% → 冻结申领，须院领导特批超支

**预算类型：**
- 普通耗材预算
- 高值耗材预算（独立）
- 试剂预算（独立）
- 设备耗材预算（独立）

#### 12.7.5 成本中心 / 成本归集

| 成本中心维度 | 说明 |
|--------------|------|
| 按科室 | 默认维度 |
| 按医生 | 高值耗材按主刀医生归集 |
| 按手术类型 | 按手术编码归集（DRG / DIP 准备） |
| 按病种 | 按 ICD-10 编码归集 |
| 按项目 | 临床研究项目专项 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 归集单号 | 自动 | GB + YYYYMMDD + 序号 |
| 归集维度 | 是 | 见上 |
| 归集周期 | 是 | 日 / 月 / 季 / 年 |
| 归集明细 | 自动 | 自动汇总消耗记录 |
| 分摊规则 | 是 | 直接归集 / 按系数分摊 / 按使用量分摊 |

**应用场景：**
- 科室成本核算
- 医生绩效考核
- DRG / DIP 病种成本测算
- 医保结算依据

#### 12.7.6 保证金 / 备货金 / 质保金

| 类型 | 说明 | 占用方 |
|------|------|--------|
| 投标保证金 | 招标时供应商缴纳，未中标退还 | 院方收取 |
| 履约保证金 | 中标后履约保障 | 院方收取 |
| 备货金 | 供应商在医院寄售/常备库存的资金占用 | 院方占用供应商 |
| 质保金 | 合同款扣留作质量保障 | 院方扣留 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 保证金单号 | 自动 | BZ + YYYYMMDD + 序号 |
| 保证金类型 | 是 | 见上 |
| 关联合同 / 招标 | 是 | - |
| 供应商 | 是 | - |
| 金额 | 是 | - |
| 缴纳 / 扣留时间 | 是 | - |
| 期限 | 是 | 保证期 |
| 退还条件 | 是 | 文本说明 |
| 状态 | - | 待缴纳 / 已缴纳 / 已扣留 / 已退还 / 已没收 |

#### 12.7.7 月结 / 反结 / 结账锁定

| 操作 | 说明 |
|------|------|
| 月结 | 每月结束后由财务发起月结，锁定本月数据 |
| 结账锁定 | 月结后该期间数据不允许修改 |
| 反结 | 特殊情况下解锁（须财务总监 + 审计 双签）|
| 跨月调整 | 锁定后的调整自动落到当前期间 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 月结单号 | 自动 | YJ + YYYYMM |
| 结账周期 | 是 | YYYY-MM |
| 结账账套 | 是 | - |
| 结账类型 | 是 | 普通月结 / 季结 / 年结 |
| 结账人 | 自动 | - |
| 反结记录 | - | 反结操作历史 |
| 状态 | - | 未结 / 已结 / 已锁定 / 已反结 |

#### 12.7.8 DRG / DIP 病种付费

按病种付费要求耗材成本归集到病种：

| DRG 字段 | 说明 |
|----------|------|
| DRG 组号 | HIS 提供 |
| 病案号 | HIS 患者标识 |
| 病种成本 | SPD 侧归集耗材 / 试剂 / 设备使用成本，其他医疗成本由外部系统提供 |
| 病种支付标准 | 医保规定 |
| 成本盈亏 | 实际成本 - 支付标准 |

**SPD 端配合：**
- 自动按 DRG 组归集耗材消耗
- 病种成本超支预警
- 病种内耗材使用合规检查（避免过度使用）
- 月度 DRG 病种耗材成本报表

#### 12.7.9 医保目录管理

| 字段 | 必填 | 说明 |
|------|------|------|
| 商品编码 | 是 | 关联商品目录 |
| 医保编码 | 是 | 国家 / 省级医保编码 |
| 医保类别 | 是 | 甲类（全报）/ 乙类（部分报）/ 丙类（自费）|
| 报销比例 | 是 | 按地区医保政策 |
| 自费比例 | 是 | 乙类专用 |
| 限价 | 是 | 医保限价 |
| 限额 | 否 | 单次 / 单年用量限额 |
| 限制使用条件 | 否 | 病种限制、年龄限制等 |
| 适用医保类型 | 是 | 城镇职工 / 城乡居民 / 离休 / 工伤 |
| 生效日期 | 是 | - |
| 失效日期 | 否 | - |

> 医保目录与商品目录、价格目录联动，HIS 计费时自动套用最新规则。

#### 12.7.10 医保驳回处理

医保审核驳回（耗材使用不合规）的处理流程：

| 阶段 | 操作 |
|------|------|
| 1. 接收驳回 | 医保平台推送驳回明细 |
| 2. 自动识别 | 系统按 UDI / 批号 / 患者匹配驳回耗材 |
| 3. 责任分析 | 主管医生 / 科室 / 收费 / 耗材使用 |
| 4. 整改方案 | 调整使用 / 退费 / 补费 / 申诉 |
| 5. 重新提交 | 整改后重新申报 |
| 6. 闭环跟踪 | 跟踪重新审核结果 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 驳回单号 | 自动 | YB + 医保平台单号 |
| 驳回原因 | 是 | 不合理使用 / 超限额 / 超限价 / 资质不符 等 |
| 驳回金额 | 是 | - |
| 涉及患者 | 是 | - |
| 责任部门 | 是 | - |
| 处理方案 | 是 | - |
| 处理结果 | - | 已申诉成功 / 已退费 / 已补费 / 已闭环 |

---

### 12.8 决策支持扩展

#### 12.8.1 功能概述

补全数据大屏、移动审批、自助报表设计器、指标口径字典、数据血缘、报表订阅推送等决策支持节点，与 7.1 报表中心、9.4.13/9.4.14 数据 API、10.14 仪表盘配置形成完整链条。

#### 12.8.2 功能架构

```
决策支持扩展
├── 数据大屏 / 驾驶舱
│   ├── 院领导驾驶舱
│   ├── 科室主任看板
│   ├── 财务大屏
│   └── 物流大屏
├── 移动审批 APP / 小程序
├── 自助报表设计器
├── 指标口径字典
├── 数据血缘追溯
└── 报表订阅 / 邮件 / 钉钉推送
```

#### 12.8.3 数据大屏 / 驾驶舱

| 大屏类型 | 受众 | 核心组件 |
|----------|------|----------|
| 院领导驾驶舱 | 院长 / 副院长 | 总览 KPI、库存周转、采购趋势、消耗排行、合规预警 |
| 科室主任看板 | 科室主任 | 本科室申领、消耗、库存、预算、医生使用 |
| 财务大屏 | 财务总监 | 应付应收、月度账期、发票认证率、付款执行 |
| 物流大屏 | 物流主管 | 配送实时、车辆位置、签收率、异常 |
| 集团驾驶舱 | 集团领导 | 多院区合并、对比、排名 |

| 大屏组件 | 说明 |
|----------|------|
| KPI 数字 | 大字突出关键指标 |
| 趋势图 | 同比 / 环比 / 实时 |
| 排行榜 | TopN 物品 / 科室 / 医生 |
| 地图 | 多院区地理分布、配送车辆位置 |
| 实时滚动 | 异常 / 召回 / 预警等告警流 |
| 钻取 | 一键下钻到明细 |

**技术规格：**
- 适配 4K / 拼接屏 / 普通显示器
- 实时刷新间隔可配置（默认 5 分钟）
- 支持导出图片 / PDF
- 支持全屏 / 自动轮播多个大屏

#### 12.8.4 移动审批 APP / 小程序

| 平台 | 说明 |
|------|------|
| 独立 APP | iOS / Android 原生，含离线队列 |
| 微信小程序 | 轻量入口，扫码 / 推送 |
| 钉钉 / 企微 | 集成审批 OA |

| 功能 | 说明 |
|------|------|
| 待办审批 | 全部业务审批入口聚合（采购 / 申领 / 调拨 / 报损 / 结算 / 付款 等） |
| 审批 | 一键通过 / 驳回 / 加签 / 转交 |
| 历史审批 | 我已审批的查询 |
| 业务查询 | 库存 / 报表 / 预警 / 召回 |
| 消息中心 | 待办通知 / 系统通知 / 公告 |
| 扫码 | 扫 UDI / 商品码 / 二维码 |
| 安全 | 指纹 / 人脸 / 密码 + 设备锁 |

#### 12.8.5 自助报表设计器

低代码报表设计：

| 功能 | 说明 |
|------|------|
| 数据源选择 | 已授权的主题数据集 / API 数据集 |
| 拖拽布局 | 表格 / 图表 / KPI / 文本 |
| 字段映射 | 维度 / 度量 / 计算字段 |
| 筛选器 | 时间 / 科室 / 商品分类 等 |
| 排序 / 分组 | 多级 |
| 计算字段 | 公式编辑器（支持 SUM / AVG / COUNT 等）|
| 模板库 | 预置常用报表模板 |
| 权限控制 | 按角色 / 科室授权 |
| 发布 | 发布给指定用户 / 角色 |
| 订阅 | 用户可订阅，定时邮件 / 钉钉推送 |

#### 12.8.6 指标口径字典

统一指标定义，避免歧义：

| 字段 | 必填 | 说明 |
|------|------|------|
| 指标编码 | 自动 | ZB + 3 位 |
| 指标名称 | 是 | 如"库存周转率" |
| 指标类型 | 是 | 财务 / 库存 / 采购 / 消耗 / 质量 |
| 业务定义 | 是 | 业务侧描述 |
| 计算口径 | 是 | 数学公式 |
| 数据来源 | 是 | 表 / 字段路径 |
| 计算频率 | 是 | 实时 / 日 / 月 / 季 / 年 |
| 单位 | 是 | 次 / 元 / % / 天 |
| 主管部门 | 是 | 财务 / 运营 / 采购 等 |
| 版本历史 | - | 口径变更留痕 |

> 同名指标在不同报表中必须使用相同口径定义，强制引用本字典。

#### 12.8.7 数据血缘追溯

| 维度 | 说明 |
|------|------|
| 字段血缘 | 报表字段 → 主题字段 → 中间表字段 → 源表字段 |
| 表血缘 | 数据集 → 数据集市 → 数据仓库 → 业务库 |
| ETL 血缘 | ETL 任务依赖关系 |
| 应用血缘 | 哪些报表 / 大屏使用了某指标 |

**展示方式：**
- 图谱式血缘可视化（支持上钻 / 下钻）
- 影响分析：源表字段变更时自动列出受影响的下游报表
- 依赖分析：报表运行慢时自动定位上游瓶颈

#### 12.8.8 报表订阅 / 推送

| 推送渠道 | 说明 |
|----------|------|
| 邮件 | PDF / Excel 附件 |
| 钉钉 | 卡片消息 + 链接 |
| 企微 | 应用消息 |
| 短信 | 简短指标 |
| APP 推送 | 移动端 APP |

| 字段 | 必填 | 说明 |
|------|------|------|
| 订阅单号 | 自动 | DY + 序号 |
| 订阅人 | 是 | - |
| 报表 / 大屏 | 是 | - |
| 推送频率 | 是 | 每日 / 每周 / 每月 / 实时 |
| 推送时间 | 是 | 如每日 9:00 |
| 推送渠道 | 是 | 见上 |
| 数据范围 | 是 | 默认按订阅人权限 |
| 异常告警 | 否 | 指标超阈值时立即推送 |

---

### 12.9 系统集成扩展

#### 12.9.1 功能概述

补全 SSO 单点登录、LDAP/AD、微信/钉钉/企微、通知中台、HIS EMPI 患者主索引、LIS/HRP/ERP 对接、省/国家监管平台对接、审计日志七要素标准等系统集成节点，与第 2 章系统架构、第 9 章接口规范形成完整的对外集成能力。

#### 12.9.2 功能架构

```
系统集成扩展
├── 单点登录 SSO
│   ├── CAS / OAuth2 / SAML
│   ├── LDAP / AD 集成
│   └── 双因子认证 2FA
├── 即时通讯集成
│   ├── 微信公众号 / 服务号
│   ├── 钉钉
│   └── 企业微信
├── 通知中台
├── HIS 集成
│   ├── EMPI 患者主索引
│   ├── HIS 计费回写
│   ├── HIS 手术单同步
│   └── HIS 患者档案对接
├── LIS / HRP / ERP 对接
├── 监管平台对接
│   ├── 国家不良事件监测平台
│   ├── 国家 / 省级 UDI 上传
│   ├── 省级集采平台
│   └── 医保局对接
└── 审计日志七要素标准
```

#### 12.9.3 SSO 单点登录

支持多种 SSO 协议，满足医院已有 IAM 体系：

| 协议 | 适用场景 |
|------|----------|
| CAS 3.0 | 大多数医院通用 |
| OAuth 2.0 | 现代 Web / 移动应用 |
| SAML 2.0 | 集团企业级 SSO |
| OIDC（OpenID Connect） | 含身份信息的 OAuth 扩展 |
| JWT Token 互信 | 简易内部互信 |

| 字段 | 必填 | 说明 |
|------|------|------|
| SSO 配置编号 | 自动 | SSO + 序号 |
| 协议类型 | 是 | 见上 |
| IdP 地址 | 是 | 身份提供方 URL |
| Client ID / App Key | 是 | - |
| Client Secret / App Secret | 是 | 加密存储 |
| 回调地址 | 是 | - |
| 用户映射规则 | 是 | IdP 用户 → SPD 用户的字段映射 |
| 自动开户 | 是 | 是否首次登录自动创建 SPD 账户 |
| 默认角色 | 是 | 自动开户时的默认角色 |
| 状态 | - | 启用 / 停用 |

**双因子认证 2FA：**
- 短信验证码
- TOTP（Google Authenticator / 微信令牌）
- U-Key / CA 证书
- 生物识别（指纹 / 人脸，移动端）
- 适用范围：高敏操作（付款、删除、权限授予、医保结算等）必须 2FA

#### 12.9.4 LDAP / AD 集成

| 字段 | 必填 | 说明 |
|------|------|------|
| LDAP 配置编号 | 自动 | LDAP + 序号 |
| 服务器地址 | 是 | LDAP / AD URL |
| 端口 | 是 | 默认 389 / 636（LDAPS）|
| 加密 | 是 | 无 / SSL / TLS |
| 绑定 DN | 是 | 系统账户 |
| 绑定密码 | 是 | 加密存储 |
| 搜索 Base | 是 | 用户搜索根 |
| 用户对象类 | 是 | 默认 inetOrgPerson / user |
| 用户属性映射 | 是 | uid / mail / cn / displayName |
| 同步策略 | 是 | 实时 / 定时 / 手动 |
| 同步周期 | 否 | 定时模式必填 |

**同步策略：**
- 实时：登录时实时校验
- 定时：每日 / 每小时同步用户列表
- 手动：管理员手动触发

#### 12.9.5 微信 / 钉钉 / 企微集成

| 平台 | 集成场景 |
|------|----------|
| 微信公众号 | 公众号菜单、模板消息、扫码登录 |
| 微信小程序 | 移动审批、查询入口 |
| 钉钉 | 单点登录、待办推送、审批集成 |
| 企业微信 | 内部沟通、待办推送、审批集成 |
| 飞书 | 类似企微 |

| 字段 | 必填 | 说明 |
|------|------|------|
| 集成平台 | 是 | 见上 |
| AppID / CorpID | 是 | - |
| AppSecret / CorpSecret | 是 | 加密存储 |
| 消息模板 ID | 是 | 各类业务对应的模板 |
| 已绑定用户数 | 自动 | - |
| 启用功能 | 多选 | 登录 / 审批 / 通知 / 扫码 |

**审批同步：** SPD 审批节点同步到钉钉 / 企微待办，钉钉 / 企微完成审批后回写 SPD。

#### 12.9.6 通知中台

抽象统一的多渠道通知能力：

```
业务模块
   ↓
通知中台 API（统一接口）
   ↓
路由分发
   ↓
[短信] [邮件] [APP 推送] [钉钉] [企微] [微信] [系统消息] [电话]
```

| 字段 | 必填 | 说明 |
|------|------|------|
| 通知模板编号 | 自动 | TZ + 序号 |
| 模板名称 | 是 | - |
| 业务类型 | 是 | 审批待办 / 召回告警 / 资质到期 / 库存预警 等 |
| 默认渠道 | 是 | 多选 |
| 渠道优先级 | 是 | 失败后降级顺序 |
| 模板内容 | 是 | 支持变量占位 |
| 静默期 | 否 | 避免重复打扰，如 30 分钟内同主题不重发 |
| 紧急级别 | 是 | 一般 / 重要 / 紧急 |
| 接收人规则 | 是 | 角色 / 用户 / 自定义 |

**降级策略：** APP 推送失败 → 短信 → 电话（紧急级别）。

#### 12.9.7 HIS 集成

| 接口 | 方向 | 说明 |
|------|------|------|
| EMPI 患者主索引 | HIS → SPD | 患者基础信息（含 ID / 姓名 / 身份证 / 住院号 / 床位） |
| 手术单推送 | HIS → SPD | 排班手术单同步给 SPD（与 8.8.7 临时单合并联动） |
| 计费回写 | SPD → HIS | 高值耗材使用、消耗、押金等费用项推送至 HIS 计费 |
| 患者档案查询 | SPD → HIS | 查询历史病案、过敏史、植入史 |
| 医生人员同步 | HIS → SPD | 医生 / 护士基础信息 |
| 病案同步 | HIS → SPD | 出院病案号、ICD-10 / DRG / DIP 编码（用于 12.7.5 / 12.7.8） |

**通信协议：** RESTful JSON / WebService SOAP / HL7 FHIR（推荐）

**异常处理：**
- 网络断开：本地队列缓存，恢复后补发
- HIS 不可用：降级到本地手工录入，事后核对
- 数据不一致：每日定时核对，差异行人工确认

#### 12.9.8 LIS / HRP / ERP 对接

| 系统 | 对接内容 |
|------|----------|
| LIS（检验）| 试剂消耗自动回写、检验项目-试剂对照、试剂效期联动 |
| HRP（医院资源规划）| 资产、人力、财务的横向同步 |
| ERP（金蝶 / 用友 / SAP）| 凭证生成、应付应收、银行回单 |
| OA（钉钉 / 企微 / 用友 OA）| 审批流集成、公文流转 |
| PACS（影像）| 高值耗材植入影像关联（仅高值场景） |

#### 12.9.9 监管平台对接

| 平台 | 对接内容 | 频率 |
|------|----------|------|
| 国家不良事件监测平台 | 不良事件上报（关联 8.6.6 / 12.5.4）| 实时 |
| 国家药监局 UDI 数据库 | 三级 / 二级 UDI 上传 | 入库即上报 |
| 省级集采平台 | 中标结果同步、采购量上报 | 实时 |
| 国家卫健委医院资源系统 | 医院基础信息、耗材使用统计 | 月度 |
| 省级医保局平台 | 医保耗材结算、DRG / DIP 病种成本上报、驳回处理 | 实时 |
| 海关 / 检验检疫（进口耗材）| 进口耗材报关数据同步 | 按批次 |
| 应急管理 / 战略储备平台 | 紧急物资库存上报 | 月度 + 应急 |

**安全要求：**
- 所有监管平台对接须使用国密算法 SM2 / SM3 / SM4
- VPN 专线 + 数字证书双向认证
- 关键操作纳入区块链存证（可选）

#### 12.9.10 审计日志七要素标准

所有关键操作必须记录七要素，方便审计追溯：

| 要素 | 字段名 | 必填 | 说明 |
|------|--------|------|------|
| Who（人） | user_id / username | 是 | 操作人 |
| When（时） | event_time | 是 | 精确到毫秒 |
| Where（地） | client_ip / location | 是 | IP + 地理位置（可选）|
| What（事） | event_type / biz_type / biz_id | 是 | 业务对象 |
| How（手段） | event_action | 是 | CREATE / UPDATE / DELETE / VIEW / EXPORT / APPROVE 等 |
| Why（原因） | event_reason | 否 | 用户填写或系统自动 |
| Result（结果） | event_result / before_value / after_value | 是 | 成功 / 失败 + 变更前后 JSON |

**审计日志独立存储：**
- 独立日志库（不与业务库混合）
- 防篡改：行级哈希 + 链式追加
- 留档期限：≥ 5 年（关键业务）/ ≥ 10 年（医保 / 不良事件）
- 审计员只读权限，禁止删除 / 修改
- 支持按七要素任意维度组合查询

| 关键操作场景 | 必须记录 |
|--------------|----------|
| 用户登录 / 注销 | Who / When / Where / Result |
| 权限授予 / 撤销 | Who / When / What / Result |
| 业务数据变更 | 全要素 + before/after |
| 敏感数据导出 / 打印 | 全要素 + 数据范围 |
| 召回 / 不良事件 | 全要素 + 时序 |
| 医保结算 | 全要素 + 金额校验 |
| 财务月结 / 反结 | 全要素 + 多人复核 |

#### 12.9.11 安全运行期支撑

安全运行期支撑用于承接 PHI 加密、KMS/Vault 密钥引用、审计双写、哈希链和签名验签诊断能力。

| 能力 | 要求 |
|------|------|
| PHI 字段保护 | 患者姓名、证件号、病案号、诊断等字段采用字段级加密，检索字段使用 HMAC Hash |
| KMS / Vault | 明文密钥不得落库，仅保存 `kid`、`key_ref`、算法、状态和轮转信息 |
| 审计双写 | 关键审计日志写入主审计表后进入双写队列，失败可重试、可告警、可追溯 |
| 签名验签 | 审计日志记录 `signing_kid`、签名算法和哈希链信息，支持运行期验签诊断 |
| API 契约 | KMS 与审计运行期诊断接口分别见 `docs/api/paths/kms.yaml`、`docs/api/paths/audit-runtime.yaml` |

---

> **第 12 章结束。** 完成本轮 P0 + P1 全部 25 项扩展业务节点的补章工作。

---

**文档结束**
