# 院内 SPD 系统开发规范

> 版本：v1.0 · 最后更新：2026-06-01  
> 适用范围：院内SPD供应链管理平台（Spring Boot + Vue 3 + MySQL 8.0）

---

## 目录

1. [总体原则](#1-总体原则)
2. [项目目录结构](#2-项目目录结构)
3. [后端开发规范](#3-后端开发规范)
4. [前端开发规范](#4-前端开发规范)
5. [数据库设计规范](#5-数据库设计规范)
6. [接口规范](#6-接口规范)
7. [Git 工作流与提交规范](#7-git-工作流与提交规范)
8. [测试规范](#8-测试规范)
9. [代码审查规范](#9-代码审查规范)
10. [开发环境与工具](#10-开发环境与工具)

---

## 1. 总体原则

### 1.1 核心准则

- **可读性优先**：代码是写给人读的，其次才是给机器执行。优先清晰的命名和结构，而非过度追求技巧性写法。
- **约定优于配置**：遵循框架和语言的常规做法，不引入不必要的抽象层。
- **渐进式重构**：现有代码如果正常工作，不强制重写；新增代码时逐步对齐规范。
- **DRY 但不过早抽象**：出现三次以上重复时才考虑提取公共逻辑。

### 1.2 技术决策记录

凡是涉及以下决策，需在代码注释或独立文档中记录理由：

- 引入新的依赖库或框架
- 偏离常规架构模式（如跳过 Service 层、直接操作数据库等）
- 非标准的数据库设计选择
- 性能优化的特殊处理

---

## 2. 项目目录结构

### 2.1 顶层结构

```
SPD-PR/
├── frontend/           # 前端（Vue 3 + TypeScript）
├── backend/            # 后端（Spring Boot + Java）
├── docs/               # 文档（设计文档、API 文档等）
├── docker/             # Docker 部署配置
├── scripts/            # 辅助脚本
└── package.json        # 项目根脚本
```

### 2.2 后端结构

```
backend/
├── pom.xml
└── src/
    └── main/
        ├── java/com/hospital/spd/
        │   ├── SpdApplication.java
        │   ├── common/              # 公共基础设施
        │   │   ├── ApiResponse.java      # 统一响应封装
        │   │   ├── GlobalExceptionHandler.java  # 全局异常处理
        │   │   ├── CorsConfig.java        # 跨域配置
        │   │   └── WebMvcConfig.java      # Web MVC 配置
        │   ├── system/              # 系统管理模块
        │   │   ├── controller/           # Controller 层
        │   │   ├── service/              # Service 层（新增代码需创建）
        │   │   └── model/                # 数据模型/DTO
        │   ├── foundation/          # 基础管理（用户、角色、权限）
        │   ├── masterdata/          # 主数据管理（商品、供应商、厂家等）
        │   ├── supplychain/         # 供应链业务（采购、库存、收货等）
        │   ├── specialty/           # 专项管理（UDI、冷链、高值耗材）
        │   ├── operations/          # 运营支撑（报表、系统配置）
        │   └── extension/           # 扩展业务模块
        └── resources/
            ├── application.yml          # 主配置
            ├── application-dev.yml      # 开发环境配置
            ├── application-prod.yml     # 生产环境配置
            └── db/
                ├── migrations/              # Flyway 迁移脚本
                └── init/                    # 初始化脚本
```

### 2.3 前端结构

```
frontend/
├── index.html
├── vite.config.ts
├── tsconfig.json
└── src/
    ├── main.ts                  # 应用入口
    ├── App.vue                  # 根组件（布局 + 路由容器）
    ├── router/
    │   └── index.ts             # 路由配置
    ├── config/
    │   └── menu.ts              # 菜单配置
    ├── api/                     # API 请求层
    │   ├── http.ts              # Axios 实例 + 拦截器
    │   ├── modules.ts
    │   ├── features.ts
    │   ├── masterData.ts
    │   ├── purchaseOrders.ts
    │   └── ...                  # 按业务领域拆分
    ├── stores/                  # Pinia 状态管理
    │   ├── auth.ts
    │   └── modules.ts
    ├── views/                   # 视图组件
    │   ├── LoginView.vue
    │   ├── DashboardView.vue
    │   ├── master-data/
    │   ├── catalog/
    │   ├── supply-chain/
    │   └── system/
    ├── components/              # 公共组件（新增代码需创建）
    │   └── ...
    └── styles/
        └── main.css             # 全局样式
```

#### 前端目录规范说明

| 目录 | 用途 | 说明 |
|------|------|------|
| `api/` | 请求层 | 按业务模块拆分文件，每个文件只导出请求函数和类型 |
| `stores/` | 状态管理 | 按业务领域拆分，使用 Pinia `defineStore` |
| `views/` | 页面视图 | 按业务子域分文件夹，文件名以 `XxxView.vue` 结尾 |
| `components/` | 公共组件 | 跨页面复用的组件放在此处，页面内私有组件放在对应 views 子目录 |
| `composables/` | 组合式函数 | 可复用的逻辑抽象（新增时创建） |
| `config/` | 静态配置 | 菜单、常量、枚举等 |
| `router/` | 路由定义 | 定义路由表与导航守卫 |

---

## 3. 后端开发规范

### 3.1 分层架构

新代码遵循以下分层结构，已有代码逐步重构：

```
Controller → Service → Repository (JdbcTemplate/DAO)
```

| 层 | 职责 | 约束 |
|----|------|------|
| **Controller** | 请求路由、参数校验、响应返回 | 不包含业务逻辑，不直接操作数据库 |
| **Service** | 业务逻辑编排、事务管理 | 所有 `@Transactional` 放在此层 |
| **Repository** | 数据访问（JdbcTemplate、JPA Repository） | 只做数据 CRUD，不含业务判断 |

> **历史代码说明**：当前项目中 Controller 层直接注入了 `JdbcTemplate` 并包含了业务逻辑。新增功能时，请按三层架构编写；重构历史代码时，优先抽取 Repository/DAO 层，再抽取 Service 层。

### 3.2 命名规范

| 元素 | 规范 | 示例 |
|------|------|------|
| 类名 | UpperCamelCase | `PurchaseOrderController` |
| 方法名 | lowerCamelCase | `createHospitalProduct` |
| 变量名 | lowerCamelCase | `orderNo` |
| 常量 | UPPER_SNAKE_CASE | `MAX_RETRY_COUNT` |
| 包名 | 全小写 | `com.hospital.spd.masterdata` |
| Controller 统一响应 | `ApiResponse<T>` | `ApiResponse.ok(data)` |

#### Controller 命名

- **资源 Controller**：使用资源复数命名，如 `PurchaseOrderController`、`MasterDataController`
- **请求方法映射**：
  - `GET /resource` → `list(params)` 或 `query(params)`
  - `GET /resource/{id}` → `detail(id)` 或 `get(id)`
  - `POST /resource` → `create(request)`
  - `PUT /resource/{id}` → `update(id, request)`
  - `DELETE /resource/{id}` → `delete(id)`

### 3.3 代码风格

- **缩进**：4 个空格，不使用 Tab
- **最大行宽**：120 字符
- **大括号**：K&R 风格（左大括号不换行）
- **Record 优先**：纯数据传输使用 Java `record` 而非 class
- **工具方法**：`static` 私有辅助方法放在类的底部
- **依赖注入**：使用构造器注入，不使用 `@Autowired` 字段注入

```java
// ✅ 推荐：Record + 构造器注入
public record ProductCreateRequest(
    String productCode,
    String productName,
    String specModel
) {}

@RestController
@RequestMapping("/products")
public class ProductController {
    private final ProductService productService;

    public ProductController(ProductService productService) {
        this.productService = productService;
    }
}
```

- **SQL 构建**：推荐方式（使用 StringBuilder 拼接参数化查询）

```java
// ✅ 推荐：参数化查询，防 SQL 注入
String sql = "SELECT * FROM product WHERE product_code = ? AND deleted = 0";
List<Map<String, Object>> rows = jdbcTemplate.queryForList(sql, productCode);
```

### 3.4 异常处理

- **业务异常**：使用 `IllegalArgumentException` 配合有意义的错误消息（中英文均可，建议中文）
- **自定义异常**：业务复杂到需要区分异常类型时，创建 `BizException` 子类
- **全局处理**：在 `GlobalExceptionHandler` 中集中处理，不在 Controller 中 try-catch

```java
// ✅ 推荐：全局异常处理器统一处理
@ExceptionHandler(IllegalArgumentException.class)
public ResponseEntity<ApiResponse<Void>> handleIllegalArgument(IllegalArgumentException ex) {
    return ResponseEntity
            .status(HttpStatus.BAD_REQUEST)
            .body(ApiResponse.error(400, ex.getMessage()));
}
```

### 3.5 API 响应格式

统一使用 `ApiResponse<T>` record：

```java
// 成功
ApiResponse.ok(data)        // → { "code": 0, "message": "success", "data": {...}, "timestamp": "..." }

// 失败
ApiResponse.error(400, "参数错误")  // → { "code": 400, "message": "参数错误", "data": null, "timestamp": "..." }
```

---

## 4. 前端开发规范

### 4.1 命名规范

| 元素 | 规范 | 示例 |
|------|------|------|
| 组件名 | PascalCase | `PurchaseManagementView.vue` |
| 文件名 | PascalCase | `ProductDetailView.vue` |
| API 文件 | lowerCamelCase | `masterData.ts`、`purchaseOrders.ts` |
| 变量/函数 | lowerCamelCase | `fetchModules()`、`authStore` |
| 类型/接口 | PascalCase | `ApiResponse<T>`、`SpdModule` |
| Pinia Store | 以 `use` 开头 + 驼峰 | `useAuthStore`、`useModuleStore` |

### 4.2 Vue 组件规范

- **使用 `<script setup lang="ts">`** 组合式 API
- **模板中**：
  - 使用 `v-if` / `v-else` 而非 `v-show` 控制条件渲染
  - 使用 `v-for` 时始终绑定 `:key`
  - 避免在模板中写复杂表达式（超过一个三元运算符则提取到计算属性）
- **Props**：定义类型和默认值

```typescript
// ✅ 推荐
const props = withDefaults(defineProps<{
  title: string
  count?: number
}>(), {
  count: 0
})
```

- **Emits**：显式声明

```typescript
const emit = defineEmits<{
  submit: [value: string]
  close: []
}>()
```

### 4.3 API 层规范

- 每个 API 文件导出请求函数，返回类型化结果
- 使用 `http` 实例（已配置 baseURL 和拦截器）
- 响应类型统一使用 `ApiResponse<T>`

```typescript
// ✅ 推荐：api/products.ts
import { http, type ApiResponse } from './http'

export interface Product {
  productCode: string
  productName: string
  specModel: string
}

export async function fetchProducts(params: Record<string, string>) {
  const res = await http.get<ApiResponse<Product[]>>('/master-data/hospital-products', { params })
  return res.data.data
}
```

### 4.4 TypeScript 使用规范

- `tsconfig.json` 中 `strict: true` 已开启，不要关闭
- **避免使用 `any`**，优先使用 `unknown` 配合类型守卫
- 接口定义使用 `interface` 而非 `type`（对象类型）；联合类型使用 `type`
- 函数返回值始终标注类型

### 4.5 样式规范

- 使用 `main.css` 中的 CSS 变量（`--primary`、`--line`、`--text-muted` 等）
- 组件内样式使用 `<style scoped>`
- 避免 !important
- 新增主色调遵循现有设计系统变量

---

## 5. 数据库设计规范

### 5.1 命名规范

| 元素 | 规范 | 示例 |
|------|------|------|
| 数据库名 | UPPER_SNAKE_CASE | `ISPD` |
| 表名 | snake_case | `purchase_order`、`sys_dept` |
| 字段名 | snake_case | `product_code`、`create_time` |
| 主键 | 统一为 `表名_id` | `product_id`、`order_id` |
| 外键字段 | 引用表名 + `_id` | `supplier_id`、`category_id` |
| 索引 | `idx_表名_字段名` | `idx_purchase_order_status` |
| 唯一约束 | `uk_表名_字段名` | `uk_purchase_demand_no` |

### 5.2 建表规范

- **统一引擎**：`ENGINE=InnoDB`
- **统一字符集**：`DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci`
- **必须字段**：每张表包含以下字段

```sql
CREATE TABLE IF NOT EXISTS xxx (
  xxx_id    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  -- ...业务字段...
  create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted     TINYINT UNSIGNED NOT NULL DEFAULT 0,
  PRIMARY KEY (xxx_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;
```

- 逻辑删除使用 `deleted = 0`（有效）/ `deleted = 1`（删除）
- 涉及排序使用 `sort_order INT`
- 状态字段使用 `status TINYINT` 或带含义的 `VARCHAR`
- 金额字段使用 `DECIMAL(18,4)` 或 `DECIMAL(18,2)`
- 数量字段使用 `DECIMAL(18,4)`

### 5.3 SQL 规范

- 所有用户输入必须使用参数化查询（PreparedStatement），禁止拼接 SQL
- 关键词大写（SELECT、FROM、WHERE、JOIN 等），表名字段名小写
- 复杂查询建议在 SQL 文件中维护（`resources/db/`），而非内嵌在 Java 代码中
- 多表 JOIN 必须使用表别名

### 5.4 迁移管理

- 新建表/修改表结构使用 Flyway（新增后需引入）
- 迁移脚本放在 `resources/db/migrations/`
- 命名格式：`V{版本号}__{描述}.sql`，如 `V1__create_supplier_table.sql`

---

## 6. 接口规范

### 6.1 RESTful 风格

- 使用名词复数表示资源：`/master-data/hospital-products`
- HTTP 方法对应 CRUD：
  - `GET` → 查询（列表 / 详情）
  - `POST` → 创建
  - `PUT` → 全量更新
  - `PATCH` → 部分更新
  - `DELETE` → 删除
- 查询参数使用 `@RequestParam` 接收；请求体使用 `@RequestBody`
- 路径参数使用 `@PathVariable`

### 6.2 URL 路径约定

```
/{domain}/{resource}
/{domain}/{resource}/{id}
/{domain}/{resource}/{id}/action            # 状态变更
/{domain}/{resource}/export                 # 导出
/{domain}/{resource}/import                 # 导入
/{domain}/{resource}/options                # 下拉选项
```

### 6.3 统一响应

所有接口统一返回 `ApiResponse<T>`：

```json
{
  "code": 0,
  "message": "success",
  "data": { ... },
  "timestamp": "2026-06-01T10:00:00+08:00"
}
```

### 6.4 错误码约定

| code | 含义 |
|------|------|
| 0 | 成功 |
| 400 | 请求参数错误（业务异常） |
| 401 | 未认证 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

---

## 7. Git 工作流与提交规范

### 7.1 分支策略

```
main          ← 生产就绪代码
  ├── develop  ← 开发主分支
  │   ├── feature/xxx  ← 功能开发分支
  │   ├── bugfix/xxx   ← Bug 修复分支
  │   └── refactor/xxx ← 重构分支
  └── release/x.x.x   ← 发布准备分支
```

| 分支类型 | 源分支 | 合并目标 | 命名规范 |
|----------|--------|----------|----------|
| feature | develop | develop | `feature/模块-简要描述` |
| bugfix | develop | develop | `bugfix/问题描述` |
| refactor | develop | develop | `refactor/模块-描述` |
| hotfix | main | main + develop | `hotfix/紧急修复描述` |

### 7.2 提交信息规范

使用 Conventional Commits 格式：

```
<type>(<scope>): <description>

[optional body]
[optional footer]
```

**type 类型：**

| type | 含义 |
|------|------|
| feat | 新功能 |
| fix | Bug 修复 |
| refactor | 重构（不新增功能也不修 Bug） |
| style | 代码格式调整（不影响逻辑） |
| docs | 文档变更 |
| test | 测试相关 |
| chore | 构建/工具/依赖变更 |
| db | 数据库迁移或数据结构变更 |

**示例：**

```
feat(purchase): 采购订单支持批量审批功能

- 新增 PurchaseOrderBatchApproveRequest
- 批量更新订单状态并记录操作日志

Closes #42
```

```
fix(product): 修复商品目录查询时价格字段为空指针异常

validateQuotaEligibility 中 highValue 为 null 时触发了 NPE

Closes #57
```

```
db: 新增采购计划表 purchase_plan

- 创建 purchase_plan 表结构
- 添加相关索引
```

### 7.3 提交粒度

- **一个提交只做一件事**：功能开发、重构、修复应分开提交
- **每个提交保持可构建**：`npm run build` 和 `mvn test` 应能通过
- **不要混合**：格式化更改和逻辑更改应在不同提交中

### 7.4 避免提交的文件

```
# .gitignore 已涵盖
node_modules/
target/
*.log
.env
.env.local
application-local.yml
.idea/
.vscode/
*.iml
.DS_Store
```

---

## 8. 测试规范

### 8.1 测试策略

| 层级 | 覆盖目标 | 工具 |
|------|----------|------|
| 单元测试 | Service 层核心逻辑 | JUnit 5 + Mockito |
| 集成测试 | Repository 层数据访问 | `@SpringBootTest` + H2/Testcontainers |
| API 测试 | Controller 层接口 | `@WebMvcTest` + MockMvc |
| 前端测试 | 组件/Store/API | Vitest + Vue Test Utils（后续引入） |

### 8.2 后端测试

- 测试类放在 `src/test/java/` 对应的包下
- 测试类名：`{被测类名}Test`（如 `ProductServiceTest`）
- 方法名：`{测试场景}`（如 `shouldThrowExceptionWhenProductCodeIsEmpty`）
- 推荐使用 AssertJ 断言风格

```java
@Test
void shouldThrowExceptionWhenProductCodeIsEmpty() {
    assertThatThrownBy(() -> productService.detail(""))
            .isInstanceOf(IllegalArgumentException.class)
            .hasMessageContaining("商品编码为必填项");
}
```

### 8.3 测试覆盖要求

- Service 层核心业务逻辑覆盖率达到 80%+
- Controller 层覆盖主要成功/失败路径
- 每次提交前运行 `mvn test` 确保不破坏现有测试

---

## 9. 代码审查规范

### 9.1 审查清单

提交代码前自查以下问题：

#### 功能正确性
- [ ] 功能是否按 PRD 实现？
- [ ] 是否考虑边界情况（空值、超大值、重复值等）？
- [ ] 错误处理是否完善且不影响系统稳定性？

#### 安全性
- [ ] 所有用户输入是否参数化（防止 SQL 注入）？
- [ ] 敏感数据是否脱敏或加密？
- [ ] API 是否需要鉴权？

#### 代码质量
- [ ] 命名是否清晰表达意图？
- [ ] 是否有过度复杂/难以维护的代码？
- [ ] 重复代码是否有必要提取公共方法？
- [ ] 中文注释是否准确、英文拼写是否正确？

#### 性能
- [ ] SQL 查询是否有合适的索引？
- [ ] N+1 查询问题？
- [ ] 批量操作是否有批量处理的能力？

### 9.2 审查流程（后续启用）

1. 开发者创建 Pull Request（从 feature 到 develop）
2. 至少一名团队成员做 Code Review
3. CI 通过 + Review 通过后方可合并
4. 合并使用 Squash Merge 保持主分支历史整洁

---

## 10. 开发环境与工具

### 10.1 环境要求

| 组件 | 版本 |
|------|------|
| JDK | 17+ |
| Node.js | 20+ |
| MySQL | 8.0+ |
| Maven | 3.8+ |
| IDE | VS Code / IntelliJ IDEA |

### 10.2 本地启动

```bash
# 后端
cd backend
mvn spring-boot:run

# 前端
cd frontend
npm install
npm run dev
```

### 10.3 代码格式化（推荐工具）

- **后端**：安装 Spotless Maven Plugin 或 IDE 内置格式化（Google Java Style）
- **前端**：安装 Prettier + ESLint
- **编辑器**：推荐 VS Code 配合 Vue Language Features 插件

### 10.4 环境配置管理

- 数据库连接、第三方密钥等敏感配置通过环境变量注入，不硬编码
- 配置项规范：`${SPD_DB_URL:默认值}`（提供合理默认值便于本地开发）
- 不同环境使用 `application-{profile}.yml` 文件区分

---

## 附录 A：代码质量检查清单（Git Hooks 预配置目标）

以下检查项后续通过 Husky + lint-staged 自动执行：

- [ ] `mvn test` 或 `npm run build` 通过
- [ ] 代码格式化检查通过
- [ ] TypeScript 编译无错误
- [ ] 无 TODO / FIXME 未处理

---

## 附录 B：常用命令速查

```bash
# 前端开发
cd frontend && npm run dev          # 启动开发服务器（端口 1820）

# 前端构建
cd frontend && npm run build        # 生产构建

# 后端开发
cd backend && mvn spring-boot:run   # 启动后端（端口 1818）

# 后端构建+测试
cd backend && mvn clean test        # 运行测试
cd backend && mvn clean package     # 打包

# 全栈启动
npm run dev:frontend                # 前端（项目根目录）
npm run dev:backend                 # 后端（项目根目录）
```
