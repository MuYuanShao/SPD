# Issue tracker: GitHub

Issues and PRDs live in GitHub Issues for `MuYuanShao/SPD`.
Use the GitHub integration or `gh` CLI to create, read, comment on,
label, and close issues. Infer the repository from `git remote -v`.
