# Domain Docs

This is a single-context repository.

Before exploring or changing behavior, read:

- `CONTEXT.md`
- relevant ADRs under `docs/adr/`

Use the glossary''s domain vocabulary in test names and implementation.
Surface any conflict with an ADR explicitly instead of silently overriding it.
Missing optional domain documents do not block work.
